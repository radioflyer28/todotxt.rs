---
phase: 38-verification-evidence-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md
autonomous: true
requirements:
  - CAP-04
  - CAP-05
  - TAG-03
  - BULK-01
  - BULK-02
  - BULK-03

must_haves:
  truths:
    - "34-VERIFICATION.md exists at .planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md"
    - "All 6 requirements (CAP-04, CAP-05, TAG-03, BULK-01/02/03) have a row in the Observable Truths table"
    - "CAP-05 and TAG-03 evidence cites the 6 metadata preservation tests in task.rs"
    - "BULK-01/02/03 evidence cites AppendTextConfirm mode in app.rs"
  artifacts:
    - path: ".planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md"
      provides: "Phase 34 verification evidence covering 6 requirements"
      contains: "CAP-04"
  key_links:
    - from: ".planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md"
      to: "crates/todotxt-core/src/task.rs"
      via: "metadata test citation evidence"
      pattern: "task.rs"
---

<objective>
Create `34-VERIFICATION.md` for Phase 34 (Bulk Action Safety + Metadata Preservation) by inspecting existing SUMMARY files and source code, then writing a standard verification report that formally evidences all 6 requirements.

Purpose: Phase 38 exists to backfill this missing artifact so that the v1.5 milestone audit passes.
Output: `.planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md` (new file)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-bulk-action-safety-metadata-preservation/34-01-SUMMARY.md
@.planning/phases/34-bulk-action-safety-metadata-preservation/34-02-SUMMARY.md
@.planning/phases/34-bulk-action-safety-metadata-preservation/34-03-SUMMARY.md

<!-- Reference examples for verification format -->
@.planning/phases/35-basic-clipboard-workflows/35-VERIFICATION.md
@.planning/phases/36-recovery-path-short-horizon-undo/36-VERIFICATION.md

<interfaces>
<!-- Requirements being verified (from REQUIREMENTS.md) -->
CAP-04: Pressing `i` opens a priority picker that can set or overwrite priority (A-Z) on active or selected tasks.
CAP-05: Property edits preserve non-target metadata (@context, +project, creation/completion fields).
TAG-03: Quick tag setters avoid duplicate tokens and preserve all non-target metadata.
BULK-01: High-impact bulk actions (overwrite, cut, delete) display affected-count preview before execution.
BULK-02: Bulk actions provide a clear cancel path and leave data unchanged on cancel.
BULK-03: Bulk action targeting remains stable for multi-selection and grouped/pane views.

<!-- Key code locations (from SUMMARY.md files) -->
Plan 34-01 delivered (state.rs): PriorityPickerState struct with items A–Z + "— (no priority)", selected_idx, type_char
Plan 34-01 delivered (app.rs): AppMode::PriorityPicker variant, priority_picker: Option<PriorityPickerState>, handle_priority_picker_key (Esc cancels, Up/Down nav, char jump, Enter/Tab apply via with_priority()), render_priority_picker_overlay, `i` binding in handle_normal_key with has_quick_setter_targets() guard
Plan 34-02 delivered (task.rs): 6 test functions — test_with_priority_preserves_metadata, test_with_priority_clears_priority, test_with_priority_on_completed_task, test_with_due_date_no_duplicate, test_with_due_date_removes_due_token, test_with_priority_preserves_projects_contexts
Plan 34-03 delivered (app.rs): AppMode::AppendTextConfirm variant, append_confirm_count field, T gate on selected_tasks.len() > 1, handle_append_text_confirm_key (Enter proceeds, Esc returns to Normal with selection preserved), render_append_text_confirm "Appending to N tasks — Enter to continue, Esc to cancel", D wording for count 1/N/0
Plan 34-03 delivered (app.rs): handle_date_picker_key accept branch refactored to use with_due_date() builder (D-13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan Phase 34 source artifacts</name>
  <files>crates/todotxt-tui/src/state.rs, crates/todotxt-tui/src/app.rs, crates/todotxt-core/src/task.rs</files>
  <action>
Grep the source files to locate exact line numbers and function signatures for Phase 34 artifacts.

Run these greps:

```
grep -n "PriorityPickerState\|priority_picker\|AppMode::PriorityPicker\|handle_priority_picker_key\|render_priority_picker_overlay" crates/todotxt-tui/src/state.rs
grep -n "PriorityPickerState\|AppMode::PriorityPicker\|priority_picker\|handle_priority_picker_key\|render_priority_picker_overlay\|KeyCode::Char('i')" crates/todotxt-tui/src/app.rs
grep -n "AppMode::AppendTextConfirm\|AppendTextConfirm\|append_confirm_count\|handle_append_text_confirm_key\|render_append_text_confirm" crates/todotxt-tui/src/app.rs
grep -n "test_with_priority\|test_with_due_date" crates/todotxt-core/src/task.rs
grep -n "with_priority\|with_due_date" crates/todotxt-core/src/task.rs
```

Also run `cargo test -p todotxt-core` to get the test pass count. Note the 6 metadata preservation tests pass green.
  </action>
  <verify>
    <automated>grep -c "PriorityPickerState\|AppendTextConfirm" crates/todotxt-tui/src/app.rs</automated>
  </verify>
  <done>Line numbers identified for PriorityPickerState, AppMode::AppendTextConfirm, 6 metadata tests, with_priority/with_due_date builders.</done>
</task>

<task type="auto">
  <name>Task 2: Write 34-VERIFICATION.md</name>
  <files>.planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md</files>
  <action>
Create `.planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md` following the standard format.

**Frontmatter:**
```yaml
---
phase: 34-bulk-action-safety-metadata-preservation
verified: <today's date as ISO 8601>
status: complete
score: 6/6 must-haves verified
overrides_applied: 0
---
```

**Observable Truths table** — 6 rows mapping requirements to evidence:
- CAP-04: `i` key in normal mode with `has_quick_setter_targets()` guard opens PriorityPickerState overlay; Enter/Tab accepts via `with_priority()` builder; applies to all selected tasks; Esc cancels without mutation
- CAP-05: `with_priority()` and `with_due_date()` builders rebuild task from parts — 6 tests in task.rs confirm all non-target metadata survives (contexts, projects, creation date, completion state, completion date, existing due tokens)
- TAG-03: `apply_token_to_tasks()` checks for duplicate tokens before applying (idempotent per Phase 33 evidence); metadata preserved via D-13 structured mutation; CAP-05 tests also cover TAG-03 duplication guard
- BULK-01: Bulk append `T` with `selected_tasks.len() > 1` enters `AppendTextConfirm` mode showing "Appending to N tasks — Enter to continue, Esc to cancel"; bulk delete `D` shows count via `render_delete_confirm` ("Delete N tasks? y=confirm any=cancel"); bulk priority picker `i` shows "Setting priority — N tasks" in header
- BULK-02: `AppendTextConfirm` Esc arm returns to Normal mode with selection preserved (D-03); date picker Esc arm cancels without mutation; priority picker Esc cancels without mutation; delete confirm non-y key cancels
- BULK-03: All bulk handlers iterate `selected_tasks` list which is stable (descending-index ordered per D-03 from Phase 20); pane view rebuild preserves selection indices

**Required Artifacts table:** cite state.rs, app.rs, task.rs with line numbers from Task 1.

**Key Link Verification table:**
- `i` binding → `priority_picker = Some(PriorityPickerState::new())` → overlay render
- `handle_priority_picker_key` Enter arm → `with_priority(priority)` builder → `task_list.update()`
- `T` gate → `AppendTextConfirm` mode → `render_append_text_confirm` shows count
- `handle_append_text_confirm_key` Enter → `AppendText` editor mode → `handle_append_text_key` mutation
- `test_with_priority_preserves_metadata` → `with_priority()` round-trip → all non-target fields present

**Data-Flow Trace:**
- `with_priority()` — reads all existing task fields, applies only priority change
- `with_due_date()` — reads all existing task fields, applies only due: change
- `append_confirm_count` sourced from `selected_tasks.len()` at T key press — real selection data

**Behavioral Spot-Checks** from Task 1 cargo test output.
  </action>
  <verify>
    <automated>grep -c "CAP-04\|CAP-05\|TAG-03\|BULK-01\|BULK-02\|BULK-03" .planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md</automated>
  </verify>
  <done>34-VERIFICATION.md exists with frontmatter `status: complete`, all 6 requirement IDs referenced, metadata test names cited as evidence, BULK-01 AppendTextConfirm evidence present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates only planning documentation. No user input processed. No system boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Tampering | 34-VERIFICATION.md | accept | Documentation file — no executable code, no attack surface. Low-value target. |
</threat_model>

<verification>
- `34-VERIFICATION.md` file exists
- `grep -c "VERIFIED" .planning/phases/34-bulk-action-safety-metadata-preservation/34-VERIFICATION.md` returns ≥ 6
- All 6 requirement IDs (CAP-04, CAP-05, TAG-03, BULK-01/02/03) appear in the file
- `status: complete` in frontmatter
</verification>

<success_criteria>
34-VERIFICATION.md created at the correct path, follows standard format, evidences all 6 Phase 34 requirements with citations to real file/function locations.
</success_criteria>

<output>
After completion, create `.planning/phases/38-verification-evidence-backfill/38-02-SUMMARY.md`
</output>
