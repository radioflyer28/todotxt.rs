---
phase: 38-verification-evidence-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - TAG-01
  - TAG-02
  - TAG-04
  - TAG-05
  - DATE-01
  - DATE-02
  - DATE-03
  - DATE-04

must_haves:
  truths:
    - "33-VERIFICATION.md exists at .planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md"
    - "All 11 requirements (CAP-01/02/03, TAG-01/02/04/05, DATE-01/02/03/04) have a row in the Observable Truths table"
    - "Every artifact row points to a real file and real function/struct name"
    - "All key links trace actual call paths from hotkey handlers to state mutations"
  artifacts:
    - path: ".planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md"
      provides: "Phase 33 verification evidence covering 11 requirements"
      contains: "CAP-01"
  key_links:
    - from: ".planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md"
      to: "crates/todotxt-tui/src/app.rs"
      via: "evidence citations in verification tables"
      pattern: "app.rs"
---

<objective>
Create `33-VERIFICATION.md` for Phase 33 (Fast Capture + Property Pickers) by inspecting existing SUMMARY files and source code, then writing a standard verification report that formally evidences all 11 requirements.

Purpose: Phase 38 exists to backfill this missing artifact so that the v1.5 milestone audit passes.
Output: `.planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md` (new file)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-fast-capture-property-pickers/33-01-SUMMARY.md
@.planning/phases/33-fast-capture-property-pickers/33-02-SUMMARY.md

<!-- Reference examples for verification format -->
@.planning/phases/35-basic-clipboard-workflows/35-VERIFICATION.md
@.planning/phases/36-recovery-path-short-horizon-undo/36-VERIFICATION.md

<interfaces>
<!-- Requirements being verified (from REQUIREMENTS.md) -->
CAP-01: Add-task flow remains instant with minimal mode switching and predictable keybindings.
CAP-02: Edit-task flow remains fast and consistent with add-task key behavior.
CAP-03: Pressing `s` opens a due-date picker that can set or overwrite `due:` on active or selected tasks.
TAG-01: Pressing `@` in normal mode opens a quick context setter for active or selected tasks.
TAG-02: Pressing `+` in normal mode opens a quick project setter for active or selected tasks.
TAG-04: Context/project setters show autocomplete matches while typing, including potentially redundant near-matches.
TAG-05: Autocomplete supports arrow-key selection and tab-to-complete for fast token entry.
DATE-01: Typing partial due tokens such as `due:2026-` or `t:2026-07-` shows valid numeric day suggestions for the target month.
DATE-02: Date suggestions display weekday labels next to each suggested day.
DATE-03: Date autocomplete supports arrow-key selection and tab-to-complete in date entry flows.
DATE-04: The `s` due-date setter uses the same month-aware day options and weekday labels as typed date autocomplete.

<!-- Key code locations (from SUMMARY.md files) -->
Plan 33-01 delivered (state.rs): DatePickerState struct, generate_date_suggestions(), extract_date_pattern()
Plan 33-01 delivered (app.rs): date autocomplete in editor mode, `s` hotkey → date picker mode overlay, handle_date_picker_key
Plan 33-02 delivered (state.rs): AutocompleteState extended for QuickSetter mode, rank_matches(), token collection helpers
Plan 33-02 delivered (app.rs): @ and + hotkey handlers, QuickSetter mode, token application (idempotent, no duplicates), overlay rendering
Plan 33-02 delivered (config.rs): keymap registration for quick context/project actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan Phase 33 source artifacts</name>
  <files>crates/todotxt-tui/src/state.rs, crates/todotxt-tui/src/app.rs, crates/todotxt-tui/src/config.rs</files>
  <action>
Grep the source files to locate exact line numbers and function signatures for Phase 33 artifacts. This evidence will be embedded in the verification report.

Run these greps (adapt path separators for Windows / PowerShell as needed):

```
grep -n "DatePickerState\|generate_date_suggestions\|extract_date_pattern" crates/todotxt-tui/src/state.rs
grep -n "DatePickerState\|generate_date_suggestions\|extract_date_pattern\|handle_date_picker_key\|render_date_picker" crates/todotxt-tui/src/app.rs
grep -n "QuickSetter\|AutocompleteState\|rank_matches\|quick_setter" crates/todotxt-tui/src/state.rs
grep -n "QuickSetter\|handle_quick_setter\|render_quick_setter\|apply_token_to_tasks\|KeyCode::Char('@')\|KeyCode::Char('+')" crates/todotxt-tui/src/app.rs
grep -n "due:\|t:.*2026\|extract_date_pattern\|date_suggestions" crates/todotxt-tui/src/app.rs
```

Note key line numbers for use as evidence citations.

Also run `cargo test -p todotxt-tui` and `cargo test -p todotxt-core` to confirm the existing test suite is green. Record the pass counts.
  </action>
  <verify>
    <automated>grep -c "DatePickerState\|QuickSetter\|generate_date_suggestions" crates/todotxt-tui/src/state.rs</automated>
  </verify>
  <done>Line numbers and function names for all Phase 33 artifacts identified from grep output.</done>
</task>

<task type="auto">
  <name>Task 2: Write 33-VERIFICATION.md</name>
  <files>.planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md</files>
  <action>
Create `.planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md` following the standard format established by `35-VERIFICATION.md` and `36-VERIFICATION.md`.

The file MUST include:

**Frontmatter:**
```yaml
---
phase: 33-fast-capture-property-pickers
verified: <today's date as ISO 8601>
status: complete
score: 11/11 must-haves verified
overrides_applied: 0
---
```

**Sections required:**
1. Phase Goal, Status line, Re-verification: No
2. `## Goal Achievement` with:
   - `### Observable Truths` table — one row per requirement (11 rows):
     | # | Truth | Status | Evidence |
     Truths to verify (map requirement → observable behavior → code evidence):
     - CAP-01: Add task flow uses same `n` editor flow, no new modes or keybinding changes
     - CAP-02: Edit task flow uses same `u` editor flow, unchanged
     - CAP-03: `s` key in normal mode with `has_quick_setter_targets()` guard opens DatePicker overlay; accepts via Enter → `with_due_date()` builder
     - TAG-01: `@` key in normal mode with guard opens QuickSetter overlay populated with existing @context tokens
     - TAG-02: `+` key in normal mode with guard opens QuickSetter overlay populated with existing +project tokens
     - TAG-04: Typing in QuickSetter overlay calls `rank_matches()` which returns prefix + substring/fuzzy matches
     - TAG-05: Arrow keys Up/Down scroll suggestion list; Tab/Enter apply selected token; Escape cancels
     - DATE-01: Partial `due:YYYY-MM-` input triggers `extract_date_pattern()` → `generate_date_suggestions()` with valid day options for that month
     - DATE-02: `generate_date_suggestions()` uses `chrono::Datelike` to produce `"DD (Weekday)"` labels (3-letter abbreviations: Mon, Tue, etc.)
     - DATE-03: Date suggestion popup supports same Up/Down/Tab/Enter/Escape navigation as token autocomplete
     - DATE-04: `s` date picker calls `generate_date_suggestions()` — same function as typed date autocomplete — so suggestions are identical

   - `### Required Artifacts` table:
     | Artifact | Expected | Status | Details |
     Artifacts:
     - `crates/todotxt-tui/src/state.rs` | `DatePickerState` struct | VERIFIED | cite line numbers
     - `crates/todotxt-tui/src/state.rs` | `generate_date_suggestions()` function | VERIFIED | cite line numbers
     - `crates/todotxt-tui/src/state.rs` | `AutocompleteState` with QuickSetter mode and `rank_matches()` | VERIFIED | cite line numbers
     - `crates/todotxt-tui/src/app.rs` | `handle_date_picker_key` + `s` binding | VERIFIED | cite line numbers
     - `crates/todotxt-tui/src/app.rs` | `@` and `+` hotkey bindings in `handle_normal_key` | VERIFIED | cite line numbers
     - `crates/todotxt-tui/src/app.rs` | `apply_token_to_tasks()` (idempotent token application) | VERIFIED | cite line numbers

   - `### Key Link Verification` table:
     | From | To | Via | Status | Details |
     - `s` keybinding → `DatePickerState::new()` → overlay render
     - `@`/`+` keybinding → QuickSetter mode → `rank_matches()` → `apply_token_to_tasks()`
     - date autocomplete trigger → `extract_date_pattern()` → `generate_date_suggestions()` → popup render
     - date picker accept → `with_due_date()` builder (D-13 structured mutation)

   - `### Data-Flow Trace (Level 4)` table:
     - `generate_date_suggestions()` uses real `chrono::Local::now()` for current date context
     - `apply_token_to_tasks()` reads from `task_list` — real task data, not hardcoded

3. `## Behavioral Spot-Checks` table with `cargo test` results from Task 1

Fill in all line numbers from the grep output collected in Task 1.
  </action>
  <verify>
    <automated>grep -c "CAP-01\|CAP-02\|CAP-03\|TAG-01\|TAG-02\|TAG-04\|TAG-05\|DATE-01\|DATE-02\|DATE-03\|DATE-04" .planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md</automated>
  </verify>
  <done>33-VERIFICATION.md exists with frontmatter `status: complete`, all 11 requirement IDs referenced, all Observable Truths rows have evidence citations pointing to real file/line locations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates only planning documentation. No user input processed. No system boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | 33-VERIFICATION.md | accept | Documentation file — no executable code, no attack surface. Low-value target. |
</threat_model>

<verification>
- `33-VERIFICATION.md` file exists
- `grep -c "VERIFIED" .planning/phases/33-fast-capture-property-pickers/33-VERIFICATION.md` returns ≥ 11
- All 11 requirement IDs (CAP-01/02/03, TAG-01/02/04/05, DATE-01/02/03/04) appear in the file
- `status: complete` in frontmatter
</verification>

<success_criteria>
33-VERIFICATION.md created at the correct path, follows standard format, evidences all 11 Phase 33 requirements with citations to real file/function locations from the grep scan.
</success_criteria>

<output>
After completion, create `.planning/phases/38-verification-evidence-backfill/38-01-SUMMARY.md`
</output>
