---
phase: 38-verification-evidence-backfill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md
autonomous: true
requirements:
  - META-01
  - META-02
  - VIEW-03

must_haves:
  truths:
    - "37-VERIFICATION.md exists at .planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md"
    - "META-01 has an explicit VERIFIED row with evidence that context/project remain plain todo.txt tokens"
    - "META-02 evidence cites ContextPrefix/ProjectPrefix FilterTerm variants and the 16 filter tests"
    - "VIEW-03 evidence cites the 8 regression tests in view_continuity_test.rs"
  artifacts:
    - path: ".planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md"
      provides: "Phase 37 verification evidence covering 3 requirements including META-01 orphan"
      contains: "META-01"
  key_links:
    - from: ".planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md"
      to: "crates/todotxt-core/src/filter.rs"
      via: "META-02 FilterTerm evidence citation"
      pattern: "filter.rs"
    - from: ".planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md"
      to: "crates/todotxt-tui/tests/view_continuity_test.rs"
      via: "VIEW-03 regression test evidence citation"
      pattern: "view_continuity_test.rs"
---

<objective>
Create `37-VERIFICATION.md` for Phase 37 (Metadata Flexibility + View Continuity) by inspecting existing SUMMARY files and source code, then writing a standard verification report that formally evidences all 3 requirements — with special attention to META-01 which was flagged as an orphaned requirement in the milestone audit.

Purpose: Phase 38 exists to backfill this missing artifact. META-01 must be explicitly evidenced to close the orphan gap identified in `v1.5-MILESTONE-AUDIT.md`.
Output: `.planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md` (new file)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-metadata-flexibility-view-continuity/37-01-SUMMARY.md
@.planning/phases/37-metadata-flexibility-view-continuity/37-02-SUMMARY.md
@.planning/v1.5-MILESTONE-AUDIT.md

<!-- Reference examples for verification format -->
@.planning/phases/35-basic-clipboard-workflows/35-VERIFICATION.md
@.planning/phases/36-recovery-path-short-horizon-undo/36-VERIFICATION.md

<interfaces>
<!-- Requirements being verified (from REQUIREMENTS.md) -->
META-01: Context and project metadata remain plain todo.txt tokens (`@context`, `+project`) with no new custom schema.
META-02: Hierarchical tag conventions like `@email/waiting` and `+client/acme` are accepted as ordinary tokens and remain queryable.
VIEW-03: Existing filter/sort/group views continue to work consistently after capture, bulk, clipboard, and undo flows.

<!-- Key code locations (from SUMMARY.md files) -->
Plan 37-01 (filter.rs):
- FilterTerm enum: ContextPrefix(String), ProjectPrefix(String), NegContextPrefix(String), NegProjectPrefix(String) — 4 new variants
- Filter::from_query parser: @foo → ContextPrefix("foo"), @foo/bar → Include("@foo/bar") exact, +client → ProjectPrefix("client"), -@foo → NegContextPrefix
- matches_with_date: prefix matching logic for ContextPrefix/ProjectPrefix using starts_with check
- 16 new tests: parsing tests + matching tests + edge cases
- All 38 todotxt-core tests PASS

Plan 37-02 (view_continuity_test.rs):
- 8 regression tests at crates/todotxt-tui/tests/view_continuity_test.rs (351 lines)
- test_add_task_preserves_filter_state
- test_edit_task_preserves_filter_state
- test_delete_task_preserves_filter_state
- test_toggle_task_preserves_filter_state
- test_multiple_mutations_preserve_filter_state
- test_undo_entry_captures_original_state
- test_hierarchical_filter_state_preserved
- test_project_hierarchical_filter_preserved
- All 8 pass (0 failures, 0.04s)

META-01 evidence rationale:
The Phase 37 implementation added FilterTerm variants (ContextPrefix/ProjectPrefix) which are FILTER-LAYER constructs — they affect how queries are parsed and matched, but they do NOT change how tasks are stored. @context and +project tokens continue to be stored as plain space-separated text in todo.txt format. The new filter variants query against the existing token structure without modifying it. This is the evidence that META-01 is satisfied: no new schema was introduced — only a smarter query parser.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan Phase 37 source artifacts</name>
  <files>crates/todotxt-core/src/filter.rs, crates/todotxt-tui/tests/view_continuity_test.rs</files>
  <action>
Grep source files to locate exact line numbers for Phase 37 artifacts.

Run these greps:

```
grep -n "ContextPrefix\|ProjectPrefix\|NegContextPrefix\|NegProjectPrefix" crates/todotxt-core/src/filter.rs
grep -n "from_query\|ContextPrefix\|ProjectPrefix" crates/todotxt-core/src/filter.rs
grep -n "fn test_" crates/todotxt-core/src/filter.rs
grep -n "fn test_" crates/todotxt-tui/tests/view_continuity_test.rs
grep -n "filter_query\|sort_order\|grouping" crates/todotxt-tui/tests/view_continuity_test.rs
```

Also run:
- `cargo test -p todotxt-core` — note total test count and that all 38 pass
- `cargo test -p todotxt-tui` — note that the 8 view continuity tests pass

Specifically note:
1. The line range where the 4 new FilterTerm variants are defined in filter.rs (they are purely enum variants — no struct fields, no storage changes)
2. Line range of the Filter::from_query parser showing @foo → ContextPrefix routing
3. All 8 test function names in view_continuity_test.rs
4. Presence of filter_query/sort_order field access in the test helpers (proves real pane state, not mock)
  </action>
  <verify>
    <automated>grep -c "ContextPrefix\|ProjectPrefix" crates/todotxt-core/src/filter.rs</automated>
  </verify>
  <done>Line numbers identified for all FilterTerm variants, parser, 16 filter tests, and 8 view continuity tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write 37-VERIFICATION.md (with explicit META-01 evidence)</name>
  <files>.planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md</files>
  <action>
Create `.planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md` following the standard format. This file MUST explicitly evidence META-01 — the orphaned requirement from the milestone audit.

**Frontmatter:**
```yaml
---
phase: 37-metadata-flexibility-view-continuity
verified: <today's date as ISO 8601>
status: complete
score: 3/3 must-haves verified
overrides_applied: 0
meta01_orphan_resolved: true
meta01_orphan_rationale: |
  META-01 was flagged as orphaned in v1.5-MILESTONE-AUDIT.md because no SUMMARY
  frontmatter explicitly cited it. The requirement is satisfied by the fact that
  Phase 37's FilterTerm additions are query-layer constructs only — they parse
  @context and +project tokens from existing plain todo.txt text. No new storage
  format, schema, or metadata key was introduced. The todo.txt file format is
  unchanged.
---
```

**Observable Truths table — 3 rows (META-01 first, then META-02, VIEW-03):**

META-01 truth: "Context and project metadata remain stored as plain todo.txt tokens — no new schema introduced by Phase 37. ContextPrefix and ProjectPrefix are filter-query terms that match against existing @context and +project tokens; they do not modify how tasks are stored or add new fields to the todo.txt format."
Evidence: 
- filter.rs `FilterTerm::ContextPrefix(String)` is an enum variant in the FILTER layer only (lines X–Y)
- `Filter::from_query` parser reads `@` tokens from the query string; task storage is unaffected
- `task.rs` and `task_list.rs` were not modified in Phase 37 (grep confirms no Phase 37 commits to these files)
- `cargo test -p todotxt-core` — all existing storage tests pass with no schema changes

META-02 truth: "`@email/waiting` and `+client/acme` tokens are accepted as ordinary task text and can be queried via parent-prefix matching. Entering `@email` as a filter query matches tasks tagged `@email` AND `@email/waiting` through `ContextPrefix` matching logic."
Evidence:
- 16 tests in filter.rs covering parsing (@foo → ContextPrefix, @foo/bar → Include) and matching
- `test_hierarchical_context_match` — @email query matches @email/waiting task
- `matches_with_date` ContextPrefix arm uses `starts_with(format!("{prefix}/"))` logic (line X)

VIEW-03 truth: "All 8 regression tests in view_continuity_test.rs pass: filter_query, sort_order, and grouping state are unchanged after add/edit/delete/toggle mutations, multi-mutation sequences, and undo operations."
Evidence:
- `crates/todotxt-tui/tests/view_continuity_test.rs` — 8 test functions (list all names)
- All 8 pass: `cargo test -p todotxt-tui` (0 failures, 0.04s)
- `test_undo_entry_captures_original_state` validates D-08: undo entry preserves original raw text including tag order

**Required Artifacts table:**
- `crates/todotxt-core/src/filter.rs` | 4 new FilterTerm variants + parser + 16 tests | VERIFIED | lines X–Y
- `crates/todotxt-tui/tests/view_continuity_test.rs` | 8 view continuity regression tests | VERIFIED | 351 lines

**Key Link Verification table:**
- Query `@email` → `Filter::from_query` → `ContextPrefix("email")` → `matches_with_date` prefix arm → matches both `@email` and `@email/waiting` tasks
- View mutation path → pane state fields `filter_query`/`sort_order`/`grouping` are NOT mutated → regression tests confirm stable

**Data-Flow Trace:**
- `ContextPrefix` matching reads `task.contexts` — sourced from parsed todo.txt tokens (real data)
- View state persists in `App::panes[i].filter_query` — set once at pane init, not overwritten by mutation handlers

**Behavioral Spot-Checks:** cargo test results from Task 1 (both todotxt-core and todotxt-tui).

IMPORTANT: The META-01 evidence must be detailed and explicit — this requirement was the primary gap identified in the milestone audit. The evidence must make clear that:
1. Phase 37 only changed how the FILTER LAYER parses queries
2. Task storage format (todo.txt text) was not changed
3. No new schema keys were introduced
  </action>
  <verify>
    <automated>grep -c "META-01\|META-02\|VIEW-03" .planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md</automated>
  </verify>
  <done>37-VERIFICATION.md exists with `status: complete` frontmatter, `meta01_orphan_resolved: true`, META-01 row contains explicit storage-unchanged evidence, all 3 requirement IDs referenced, 8 test names listed for VIEW-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates only planning documentation. No user input processed. No system boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Tampering | 37-VERIFICATION.md | accept | Documentation file — no executable code, no attack surface. Low-value target. |
</threat_model>

<verification>
- `37-VERIFICATION.md` file exists
- `grep "meta01_orphan_resolved: true" .planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md` finds match
- All 3 requirement IDs (META-01, META-02, VIEW-03) appear in the file
- `grep -c "VERIFIED" .planning/phases/37-metadata-flexibility-view-continuity/37-VERIFICATION.md` returns ≥ 3
- `status: complete` in frontmatter
</verification>

<success_criteria>
37-VERIFICATION.md created at the correct path, follows standard format, evidences all 3 Phase 37 requirements, and explicitly resolves the META-01 orphan gap with storage-layer evidence (filter layer changes only, task format unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/38-verification-evidence-backfill/38-03-SUMMARY.md`
</output>
