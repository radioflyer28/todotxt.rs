---
phase: 19-selection-model-multi-select-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - SEL-02
  - SEL-04
must_haves:
  truths:
    - "User can enter and exit disjoint selection mode with keyboard-only controls."
    - "Group header rows cannot be selected or mutated."
    - "Selected rows are visually distinct from cursor-only rows."
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "Selection state fields, disjoint mode toggles, row-style rendering rules"
      contains: "HashSet<usize>, disjoint mode key handling, selected-row styling"
  key_links:
    - from: "handle_normal_key"
      to: "display_rows / canonical_selected"
      via: "v, Space, Esc handlers"
      pattern: "KeyCode::Char('v')|KeyCode::Char(' ')|KeyCode::Esc"
    - from: "render_task_list"
      to: "selected set membership"
      via: "row style + glyph prefix"
      pattern: "Modifier::BOLD"
---

<objective>
Establish canonical multi-selection state and safe row semantics as the base for all later multi-select operations.

Purpose: Implement the non-negotiable state model and safety constraints first so range logic and persistence can build on stable contracts.
Output: App state fields, disjoint-mode key flow, and selected-row rendering behavior in the TUI.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-selection-model-multi-select-foundation/19-CONTEXT.md
@crates/todotxt-tui/src/app.rs

<interfaces>
From crates/todotxt-tui/src/app.rs:
```rust
pub enum AppMode {
    Normal,
    Adding,
    Editing { original_idx: usize },
    DeleteConfirm,
    Filtering,
    FilterDefining,
}

pub(crate) enum DisplayRow {
    Task(usize),
    GroupHeader(String),
}

pub struct App {
    pub selected: usize,
    pub display_indices: Vec<usize>,
    pub display_rows: Vec<DisplayRow>,
    // new selection fields for this plan should be added here
}

fn canonical_selected(&self) -> Option<usize>
fn handle_normal_key(&mut self, key: KeyEvent) -> color_eyre::Result<()>
fn render_task_list(&self, frame: &mut Frame, area: Rect)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical selection state and helper contracts</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - Selection state is stored as canonical task indices per D-01.
    - Anchor is tracked as Option<usize> per D-02.
    - Disjoint mode is a bool flag on App (not new AppMode) per D-04.
  </behavior>
  <action>Add `selected_tasks: HashSet<usize>`, `selection_anchor: Option<usize>`, and `disjoint_select: bool` to `App` (per D-01, D-02, D-04), initialize them in `App::new`, and add focused helper methods for selecting/unselecting canonical task rows that reject `DisplayRow::GroupHeader` inputs per D-08 and D-17.</action>
  <verify>
    <automated>cargo build -p todotxt-tui</automated>
  </verify>
  <done>App state compiles with canonical selection fields and helper methods that never admit header rows into selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement disjoint selection mode keys with header-row no-op behavior</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - `v` toggles disjoint mode per D-05.
    - `Space` toggles current task membership only when disjoint mode is active per D-06.
    - `Esc` clears entire selection and exits disjoint mode per D-07.
  </behavior>
  <action>In `handle_normal_key`, add disjoint selection key paths: `v` toggles `disjoint_select`, `Space` toggles selected membership only for task rows (no-op on group headers per D-08), and `Esc` (while in disjoint mode) clears `selected_tasks` and resets mode flag per D-07. Keep normal navigation behavior unchanged because disjoint mode is a flag, not a mode transition (D-04).</action>
  <verify>
    <automated>cargo test -p todotxt-tui</automated>
  </verify>
  <done>Disjoint selection can be toggled on/off, Space marks/unmarks only task rows, and Esc clears + exits disjoint mode exactly as defined.</done>
</task>

<task type="auto">
  <name>Task 3: Render selected rows with explicit visual precedence rules</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <action>Update `render_task_list` row composition so selected non-cursor tasks render with `>` prefix and `Modifier::BOLD` per D-14, cursor-only row remains `Modifier::REVERSED` per D-13, cursor+selected combines `REVERSED | BOLD` per D-15, and group headers remain unchanged per D-17.</action>
  <verify>
    <automated>cargo build -p todotxt-tui && cargo test -p todotxt-tui</automated>
  </verify>
  <done>Row visuals clearly distinguish cursor and selected states without altering group-header appearance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard input -> key handler | Untrusted key sequences drive selection state transitions. |
| display row model -> mutation paths | Group headers and task rows share rendering pipeline; only task rows are mutable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T | `handle_normal_key` selection branches | mitigate | Guard all mark/unmark paths by resolving `DisplayRow::Task(idx)` only; treat header rows as no-op (D-08, D-17). |
| T-19-02 | D | Selection clear/toggle logic | mitigate | Keep `Esc` behavior idempotent and bounded to in-memory set operations; no panics on empty sets or empty display. |
| T-19-03 | E | Mode transitions | accept | `disjoint_select` is a local UI flag with no privilege boundary; risk limited to local UX confusion and handled by explicit status semantics in later phase. |
</threat_model>

<verification>
- `cargo build -p todotxt-tui`
- `cargo test -p todotxt-tui`
- Manual smoke in terminal: `v`, `Space`, `Esc` flow on grouped lists verifies no header selection
</verification>

<success_criteria>
- Canonical selection state exists on `App` and compiles cleanly.
- Disjoint mode hotkeys behave exactly per D-04 through D-08.
- Selected row rendering follows D-13 through D-17 without regression to existing cursor behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/19-selection-model-multi-select-foundation/19-01-SUMMARY.md`
</output>
