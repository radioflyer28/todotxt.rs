---
phase: 19-selection-model-multi-select-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - SEL-01
  - SEL-02
must_haves:
  truths:
    - "Shift navigation extends contiguous selection from a stable anchor."
    - "Disjoint selection mode coexists with normal movement keys."
    - "Half-page range extension works with Shift+Ctrl+D/U."
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "Anchor lifecycle + range extension handlers for all required key combos"
      contains: "Shift+j/k, Shift+Up/Down, Shift+Ctrl+D/U"
  key_links:
    - from: "selection_anchor"
      to: "range selection mutators"
      via: "first shift-nav initializes anchor then extends"
      pattern: "selection_anchor"
    - from: "non-shift navigation"
      to: "anchor reset"
      via: "clear anchor without clearing selected set"
      pattern: "anchor = None"
---

<objective>
Implement contiguous range selection semantics so keyboard users can grow selection blocks predictably.

Purpose: Deliver SEL-01 behavior and complete key-level parity decisions for shift-based selection.
Output: Anchor-aware shift handlers for line and half-page navigation, plus anchor reset rules.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-selection-model-multi-select-foundation/19-CONTEXT.md
@.planning/phases/19-selection-model-multi-select-foundation/19-01-SUMMARY.md
@crates/todotxt-tui/src/app.rs

<interfaces>
From crates/todotxt-tui/src/app.rs (after 19-01):
```rust
// expected from prior plan
selected_tasks: HashSet<usize>
selection_anchor: Option<usize>
disjoint_select: bool

fn canonical_selected(&self) -> Option<usize>
fn clamp_selection(&mut self)
fn handle_normal_key(&mut self, key: KeyEvent) -> color_eyre::Result<()>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anchor lifecycle helpers for contiguous selection</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - First shift navigation sets anchor from current cursor if unset (D-11).
    - Non-shift navigation clears anchor only, not selected set (D-12).
  </behavior>
  <action>Create small internal helpers to initialize anchor lazily and to apply range selection between anchor and current canonical row. Ensure non-shift `j/k/Up/Down/Ctrl+D/Ctrl+U` paths clear `selection_anchor` while leaving `selected_tasks` unchanged per D-12.</action>
  <verify>
    <automated>cargo build -p todotxt-tui</automated>
  </verify>
  <done>Anchor behavior is deterministic and independent from disjoint mode selection contents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shift-range key matrix for line and arrow navigation</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - Shift+j/k and Shift+Down/Up extend contiguous selection per D-09.
    - Group header rows are skipped and never selected per D-08 and D-17.
  </behavior>
  <action>Extend `handle_normal_key` match branches to detect Shift-modified char and arrow movement. Reuse existing row-skip logic so movement lands on task rows, then select all canonical task indices in the visible range between anchor and cursor, excluding headers. Keep existing unshifted movement semantics unchanged.</action>
  <verify>
    <automated>cargo test -p todotxt-tui</automated>
  </verify>
  <done>All four shift line-navigation combos grow/shrink contiguous selection exactly from anchor to current cursor row.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Shift+Ctrl half-page range extension</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <action>Add Shift+Ctrl+D and Shift+Ctrl+U handling per D-10 by reusing existing half-page movement calculations and then applying contiguous range selection from anchor to resulting cursor task row. Preserve current list-height-based movement math and header-skip behavior.</action>
  <verify>
    <automated>cargo build -p todotxt-tui && cargo test -p todotxt-tui</automated>
  </verify>
  <done>Half-page shift navigation extends selection ranges consistently with line-level shift range logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key modifiers -> branch routing | Combined Shift/Ctrl modifiers can route to wrong branch if precedence is incorrect. |
| visible-row navigation -> canonical-index range | Selection range must map task rows only, not decorative headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | T | Shift/Ctrl key dispatch order | mitigate | Match Shift+Ctrl branches before plain Ctrl branches and plain shift branches; add explicit modifier guards for deterministic routing (D-09, D-10). |
| T-19-05 | I | Range computation over grouped rows | mitigate | Build ranges from canonical task indices resolved from `DisplayRow::Task` only; skip all `GroupHeader` rows. |
| T-19-06 | D | Anchor misuse on empty/short lists | mitigate | Guard all range operations with `canonical_selected()` and row-count checks; no unwraps on optional rows. |
</threat_model>

<verification>
- `cargo build -p todotxt-tui`
- `cargo test -p todotxt-tui`
- Manual key smoke: `Shift+j/k`, `Shift+Up/Down`, `Shift+Ctrl+D/U` on grouped and ungrouped lists
</verification>

<success_criteria>
- SEL-01 key matrix is fully implemented per D-09 through D-12.
- Anchor behavior is stable and predictable across shift and non-shift movement.
- Range selection never includes header rows.
</success_criteria>

<output>
After completion, create `.planning/phases/19-selection-model-multi-select-foundation/19-02-SUMMARY.md`
</output>
