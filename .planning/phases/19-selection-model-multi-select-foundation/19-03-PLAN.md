---
phase: 19-selection-model-multi-select-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - SEL-03
  - SEL-04
must_haves:
  truths:
    - "Selected tasks remain selected across regroup, resort, and refilter if those tasks still exist."
    - "Reload drops only indices that no longer exist and keeps valid selections."
    - "Tasks hidden by filters remain selected and reappear selected when visible again."
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "Selection persistence hooks on rebuild and reload paths"
      contains: "selection pruning and re-anchor-preserving rebuild behavior"
  key_links:
    - from: "AppEvent::FileChanged + apply_pending_reload"
      to: "selected_tasks pruning"
      via: "retain idx < task_list.len()"
      pattern: "retain"
    - from: "rebuild_display_indices / rebuild_and_reanchor"
      to: "selection set persistence"
      via: "do not clear selected set during regroup/resort/refilter"
      pattern: "rebuild_display_indices"
---

<objective>
Finish selection persistence semantics so multi-selection remains trustworthy across all list rebuild and reload paths.

Purpose: Complete SEL-03 correctness and lock down behavior for hidden/removed tasks before Phase 20 bulk actions depend on this state.
Output: Selection retention/pruning logic integrated into rebuild and file-reload flows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-selection-model-multi-select-foundation/19-CONTEXT.md
@.planning/phases/19-selection-model-multi-select-foundation/19-01-SUMMARY.md
@.planning/phases/19-selection-model-multi-select-foundation/19-02-SUMMARY.md
@crates/todotxt-tui/src/app.rs

<interfaces>
From crates/todotxt-tui/src/app.rs:
```rust
fn rebuild_display_indices(&mut self)
fn rebuild_and_reanchor(&mut self)
fn apply_pending_reload(&mut self) -> color_eyre::Result<()>
match event {
    AppEvent::FileChanged => { /* reload + rebuild */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve canonical selection set through regroup/resort/refilter rebuilds</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - Rebuild operations do not clear selected set per D-18.
    - Filter-hidden selected tasks remain selected per D-20.
  </behavior>
  <action>Update rebuild paths so `selected_tasks` is treated as canonical state independent of visibility: `rebuild_display_indices()` and `rebuild_and_reanchor()` must not clear selected indices simply because rows are regrouped/sorted/filtered out. Ensure rendering checks set membership only for visible rows while preserving hidden selected indices for later reappearance (D-20).</action>
  <verify>
    <automated>cargo build -p todotxt-tui</automated>
  </verify>
  <done>Selection survives view-model changes without requiring re-selection when tasks still exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prune out-of-range selections on reload and pending-reload apply</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    - Reload keeps valid canonical indices and drops only missing ones per D-19.
    - No panic when file shrinks or tasks are deleted externally.
  </behavior>
  <action>After every task-list reload path (`AppEvent::FileChanged` immediate reload and `apply_pending_reload()` delayed reload), retain only selected indices `< task_list.len()` per D-19, keep anchor valid if possible (clear if it points to removed task), then call rebuild/reanchor logic.</action>
  <verify>
    <automated>cargo test -p todotxt-tui</automated>
  </verify>
  <done>External file changes preserve existing valid selection state and safely discard stale indices.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for persistence invariants</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <action>Add focused unit tests under `#[cfg(test)]` in `app.rs` for: (1) filter hide/show retains selected canonical indices, (2) regroup/sort rebuild retains selection membership, and (3) reload pruning drops out-of-range indices only. Keep tests deterministic with in-memory `TaskList` fixtures and no terminal I/O.</action>
  <verify>
    <automated>cargo test -p todotxt-tui</automated>
  </verify>
  <done>Persistence behaviors required by SEL-03 are enforced by automated tests and remain stable for future bulk-action work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external file updates -> in-memory selection | Reload may invalidate canonical indices due to file mutations outside the TUI. |
| view filtering/grouping -> user mental model | Hidden rows are not visible but remain selected by design. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | T | reload + selection synchronization | mitigate | On every reload path, prune selected indices and anchor with explicit bounds checks before rendering. |
| T-19-08 | R | hidden selection after filter change | mitigate | Preserve canonical selected set across filter toggles and expose deterministic reappearance when visibility returns (D-20). |
| T-19-09 | D | stale selection causing panic | mitigate | Avoid direct indexing from selected set without bounds validation; enforce invariant in tests. |
</threat_model>

<verification>
- `cargo build -p todotxt-tui`
- `cargo test -p todotxt-tui`
- Manual smoke: select tasks, change filter/sort/group, trigger file reload, verify selection retention/pruning
</verification>

<success_criteria>
- SEL-03 behavior is implemented and tested across rebuild and reload paths.
- Selection persistence semantics match D-18, D-19, and D-20 exactly.
- Phase 20 can safely consume selected canonical indices for bulk operations.
</success_criteria>

<output>
After completion, create `.planning/phases/19-selection-model-multi-select-foundation/19-03-SUMMARY.md`
</output>
