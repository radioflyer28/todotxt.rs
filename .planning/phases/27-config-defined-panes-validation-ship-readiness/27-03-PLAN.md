---
phase: 27-config-defined-panes-validation-ship-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
files_modified:
  - crates/todotxt-tui/tests/config_panes_test.rs
  - CHANGELOG.md
  - README.md
  - crates/todotxt-core/Cargo.toml
  - crates/todotxt-cli/Cargo.toml
  - crates/todotxt-tui/Cargo.toml
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - PATH-01
  - PATH-02
  - PATH-03
must_haves:
  truths:
    - "Config pane and path-override behavior is validated by automated tests tied to phase requirements (per D-16)."
    - "Release documentation describes [[panes]] schema and new CLI flags accurately (per D-17)."
    - "All crate versions are aligned to 1.4.0 for ship readiness (per D-18)."
  artifacts:
    - path: "crates/todotxt-tui/tests/config_panes_test.rs"
      provides: "config pane deserialization and invalid-entry fallback tests"
      contains: "valid/invalid/missing/empty panes coverage"
    - path: "README.md"
      provides: "user-facing docs for panes config and path flags"
      contains: "[[panes]] example + --todo/--archive/--config"
    - path: "CHANGELOG.md"
      provides: "v1.4 release notes"
      contains: "Phase 27 features"
  key_links:
    - from: "tests"
      to: "requirements"
      via: "explicit CFG/PATH behavior assertions"
      pattern: "requirement-linked test cases"
    - from: "version fields"
      to: "release docs"
      via: "1.4.0 consistency across crates and changelog"
      pattern: "release alignment"
---

<objective>
Finalize validation and release-readiness outputs for Phase 27, including focused tests, docs updates, and coordinated version bump.

Purpose: Close the milestone with verifiable behavior and consistent release artifacts.
Output: new/updated tests, updated docs, and 1.4.0 versions across all crates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-CONTEXT.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-RESEARCH.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-01-PLAN.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add/extend config pane deserialization tests for all D-16 scenarios</name>
  <files>crates/todotxt-tui/tests/config_panes_test.rs, crates/todotxt-tui/src/config.rs</files>
  <behavior>
    - Test 1: valid `[[panes]]` entries deserialize and map fields correctly.
    - Test 2: invalid sort entry is skipped with warning while other entries survive.
    - Test 3: missing panes section and empty panes section deserialize safely.
  </behavior>
  <action>Implement focused test coverage for pane config parsing and safe fallback behavior described in D-16, reusing existing warning/assertion patterns in the crate test suite.</action>
  <verify>
    <automated>cargo test -p todotxt-tui config_panes_test -- --nocapture</automated>
  </verify>
  <done>All D-16 pane-deserialization scenarios are codified in automated tests and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG and README for v1.4 pane config and CLI flags</name>
  <files>CHANGELOG.md, README.md</files>
  <action>Document v1.4 additions per D-17: include `[[panes]]` configuration example, supported pane fields/defaults, and the `--todo/--archive/--config` CLI options including PATH-02 fallback semantics.</action>
  <verify>
    <automated>rg "\[\[panes\]\]|--todo|--archive|--config|v1.4" README.md CHANGELOG.md</automated>
  </verify>
  <done>Docs explicitly describe shipped behavior without placeholders and match implementation semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Bump all crate versions to 1.4.0 and run full validation sweep</name>
  <files>crates/todotxt-core/Cargo.toml, crates/todotxt-cli/Cargo.toml, crates/todotxt-tui/Cargo.toml</files>
  <action>Set crate versions to 1.4.0 in all three Cargo.toml files (per D-18), then run full TUI-focused tests/checks to confirm release readiness after documentation and version updates.</action>
  <verify>
    <automated>cargo test -p todotxt-tui -- --nocapture ; cargo check --workspace</automated>
  </verify>
  <done>All target crates report version 1.4.0 and validation commands pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> user expectations | Docs shape usage and must reflect true behavior |
| Version metadata -> build/release tooling | Inconsistent versions can break release assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | R | release documentation | mitigate | Tie docs updates to tested behavior and include explicit flag/schema examples |
| T-27-08 | T | crate version metadata | mitigate | Update all crate manifests in one task and verify workspace builds afterward |
| T-27-09 | D | release readiness validation | mitigate | Run automated test/check sweep after all close-out changes |
</threat_model>

<verification>
- `cargo test -p todotxt-tui config_panes_test -- --nocapture`
- `cargo test -p todotxt-tui path_resolution_test -- --nocapture`
- `cargo test -p todotxt-tui -- --nocapture`
- `cargo check --workspace`
</verification>

<success_criteria>
- D-16 test scope is fully represented and passing.
- README/CHANGELOG accurately document Phase 27 behavior.
- All three crate manifests are versioned 1.4.0.
- Phase 27 meets ship-readiness standards for v1.4 milestone close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/27-config-defined-panes-validation-ship-readiness/27-03-SUMMARY.md`
</output>
