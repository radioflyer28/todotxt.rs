---
phase: 27-config-defined-panes-validation-ship-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-tui/src/config.rs
  - crates/todotxt-tui/src/state.rs
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
must_haves:
  truths:
    - "User-defined [[panes]] entries load at startup into runtime panes (per D-01, D-05, D-10)."
    - "Each pane respects defaults for missing label/filter/sort/group fields (per D-02, D-03)."
    - "Invalid pane entries are skipped with warning while app startup continues (per D-04)."
    - "Pane runtime changes persist back into config.toml on quit as pane blueprint state (per D-06, D-08, D-09)."
  artifacts:
    - path: "crates/todotxt-tui/src/config.rs"
      provides: "PaneConfig schema, serde defaults, tolerant config loading support"
      contains: "panes: Vec<PaneConfig>"
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "Quit-time pane state projection into config and atomic save call"
      contains: "pane runtime -> config panes sync"
    - path: "crates/todotxt-tui/src/state.rs"
      provides: "Runtime pane mapping compatibility with config pane fields"
      contains: "Pane::new + sort/group/filter state"
  key_links:
    - from: "config.rs PaneConfig"
      to: "app.rs runtime panes"
      via: "startup mapping from config panes to Pane"
      pattern: "PaneConfig -> Pane"
    - from: "app.rs quit path"
      to: "config.save()"
      via: "persist panes only on exit"
      pattern: "save\(path\)"
---

<objective>
Implement config-defined pane schema and tolerant loading/persistence behavior so pane layout and per-pane query defaults are bootstrapped from config and saved back on quit.

Purpose: Deliver CFG requirements with backward-compatible config evolution and safe fallback behavior.
Output: `TuiConfig.panes` schema, runtime pane bootstrap mapping, invalid-entry warning+skip behavior, quit-time pane persistence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-CONTEXT.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-RESEARCH.md

<interfaces>
From crates/todotxt-tui/src/config.rs:
- `pub struct TuiConfig`
- `pub fn load(path: &Path) -> color_eyre::Result<Self>`
- `pub fn save(&self, path: &Path) -> color_eyre::Result<()>`

From crates/todotxt-tui/src/state.rs:
- `pub struct Pane { label, filter_query, sort_order, grouping, ... }`
- `pub fn new(id: usize, label: String) -> Self`

From crates/todotxt-tui/src/app.rs:
- active pane model already supports per-pane filter/sort/group state and quit handling via existing save path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PaneConfig/TuiConfig schema for [[panes]] with defaults</name>
  <files>crates/todotxt-tui/src/config.rs</files>
  <behavior>
    - Test 1: Config without `[[panes]]` deserializes with `panes == []` (per D-05).
    - Test 2: Partial pane entry gets defaults for missing fields (per D-02).
    - Test 3: sort string supports snake_case values mapped to runtime-compatible ordering (per D-03).
  </behavior>
  <action>Introduce `PaneConfig` with optional/defaulted fields and add `panes: Vec<PaneConfig>` with `#[serde(default)]` to `TuiConfig` (per D-01, D-02, D-03, D-05). Keep backward compatibility for existing config files and avoid new dependencies.</action>
  <verify>
    <automated>cargo test -p todotxt-tui config::tests -- --nocapture</automated>
  </verify>
  <done>TuiConfig deserializes pane schema safely, defaulting absent panes and missing fields exactly as defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire startup pane bootstrap and tolerant invalid-entry handling</name>
  <files>crates/todotxt-tui/src/config.rs, crates/todotxt-tui/src/app.rs, crates/todotxt-tui/src/state.rs</files>
  <behavior>
    - Test 1: Valid pane entries become runtime panes with label/filter/sort/group values.
    - Test 2: Invalid pane entry (for example bad sort or type mismatch) is skipped and warning emitted, with remaining valid panes loaded (per D-04).
    - Test 3: Empty resulting pane list still preserves runtime safety (existing reconcile flow remains valid).
  </behavior>
  <action>Map config panes into runtime pane initialization path (per D-10), and implement warn-and-skip fallback for invalid pane entries instead of startup failure (per D-04). Use existing warning surface patterns; do not fail app launch for malformed pane items.</action>
  <verify>
    <automated>cargo test -p todotxt-tui pane_integration_test -- --nocapture</automated>
  </verify>
  <done>Startup consumes valid pane configs, skips invalid ones with warnings, and continues boot successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist runtime panes back to config on quit only</name>
  <files>crates/todotxt-tui/src/app.rs, crates/todotxt-tui/src/config.rs</files>
  <behavior>
    - Test 1: Quit path projects current runtime pane list into `config.panes` and saves via atomic `save()`.
    - Test 2: Persisted fields are only `label/filter/sort/group` (per D-07).
    - Test 3: No intra-session writes are added; persistence occurs on quit path only (per D-08).
  </behavior>
  <action>Extend quit/save path to serialize current runtime pane list into config schema and call existing atomic save method (per D-06, D-08, D-09). Preserve non-pane config fields by round-tripping through `TuiConfig`; do not introduce `toml_edit` or additional crates.</action>
  <verify>
    <automated>cargo test -p todotxt-tui -- --nocapture</automated>
  </verify>
  <done>Pane runtime state is persisted at exit using existing atomic save workflow, with non-pane settings preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml -> deserialization | Untrusted local config content crosses into typed runtime state |
| runtime pane state -> config.save | In-memory state is serialized to persisted config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | T | `config.rs` pane parsing | mitigate | Strictly parse supported fields/types and skip invalid entries with warnings per D-04 instead of undefined behavior |
| T-27-02 | D | startup config load | mitigate | Enforce fail-open behavior for pane list: invalid entries skipped, app startup continues |
| T-27-03 | I | quit-time config write | mitigate | Persist only explicit pane fields and use existing atomic save path to reduce corruption risk |
</threat_model>

<verification>
- `cargo test -p todotxt-tui config::tests -- --nocapture`
- `cargo test -p todotxt-tui pane_integration_test -- --nocapture`
- `cargo check -p todotxt-tui`
</verification>

<success_criteria>
- CFG-01/CFG-02/CFG-03 behaviors are implemented and test-covered.
- Existing configs without panes remain fully compatible.
- Invalid panes are non-fatal and visible via warnings.
- Quit-time persistence updates pane blueprint state only.
</success_criteria>

<output>
After completion, create `.planning/phases/27-config-defined-panes-validation-ship-readiness/27-01-SUMMARY.md`
</output>
