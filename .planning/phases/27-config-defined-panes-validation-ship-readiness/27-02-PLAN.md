---
phase: 27-config-defined-panes-validation-ship-readiness
plan: 02
type: tdd
wave: 2
depends_on:
  - 27-01
files_modified:
  - crates/todotxt-tui/src/main.rs
  - crates/todotxt-tui/src/config.rs
  - crates/todotxt-tui/tests/path_resolution_test.rs
autonomous: true
requirements:
  - PATH-01
  - PATH-02
  - PATH-03
must_haves:
  truths:
    - "CLI flags can override todo/archive/config paths used by the TUI startup flow (per D-11, D-12, D-14, D-15)."
    - "When --todo is set and --archive is omitted, archive defaults to sibling done.txt (per D-13)."
    - "CLI precedence over config is deterministic and test-verified (per D-14)."
  artifacts:
    - path: "crates/todotxt-tui/src/main.rs"
      provides: "clap Args parsing and startup path resolution pipeline"
      contains: "--todo/--archive/--config"
    - path: "crates/todotxt-tui/tests/path_resolution_test.rs"
      provides: "unit/integration coverage for path precedence/defaulting"
      contains: "PATH-01/02/03 scenarios"
  key_links:
    - from: "Args parser"
      to: "TuiConfig::load"
      via: "--config override applied before config read"
      pattern: "config path precedence"
    - from: "resolved todo path"
      to: "archive path"
      via: "todo-dir done.txt default when archive absent"
      pattern: "PATH-02 fallback"
---

<objective>
Implement and validate CLI file-path override behavior in the TUI binary using clap with deterministic precedence and PATH-02 archive fallback.

Purpose: Enable explicit runtime file targeting without mutating default config and align TUI path behavior with CLI expectations.
Output: clap-backed Args parsing in TUI startup plus tests proving PATH-01/02/03 rules.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-CONTEXT.md
@.planning/phases/27-config-defined-panes-validation-ship-readiness/27-RESEARCH.md
@crates/todotxt-cli/src/main.rs

<interfaces>
From crates/todotxt-tui/src/main.rs:
- `fn main() -> color_eyre::Result<()>`
- Startup currently resolves config path, loads config, then derives todo path.

From crates/todotxt-tui/src/config.rs:
- `pub fn resolve_path(platform_path: &Path) -> PathBuf`
- `pub fn load(path: &Path) -> color_eyre::Result<Self>`

Contract to add:
- `Args` parser with fields for `todo`, `archive`, `config` (`PathBuf`).
- Helper(s) that compute final path set from `(args, config)` to keep logic unit-testable.
</interfaces>
</context>

<feature>
  <name>TUI path override contract and fallback behavior</name>
  <files>crates/todotxt-tui/src/main.rs, crates/todotxt-tui/tests/path_resolution_test.rs</files>
  <behavior>
    Cases:
    - no CLI flags -> use config.toml values and existing defaults
    - `--config X` -> load config from X before reading todo/archive settings (D-15)
    - `--todo A` + no `--archive` -> archive defaults to `dirname(A)/done.txt` (D-13)
    - `--todo A` + `--archive B` -> use A/B exactly
    - CLI values override config values in all collisions (D-14)
  </behavior>
  <implementation>Use clap derive parser in TUI, build deterministic resolver function(s), then wire resolved paths into existing TaskList/FileWatcher/app construction flow without changing unrelated startup semantics.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Add failing tests for PATH-01/02/03 precedence/defaulting</name>
  <files>crates/todotxt-tui/tests/path_resolution_test.rs</files>
  <action>Create path-resolution tests first, including all cases in the feature behavior block, and run them to confirm failure before production implementation.</action>
  <verify>
    <automated>cargo test -p todotxt-tui path_resolution_test -- --nocapture</automated>
  </verify>
  <done>Tests exist and fail against current implementation, proving behavior is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement clap args and path resolver logic in main startup</name>
  <files>crates/todotxt-tui/src/main.rs, crates/todotxt-tui/src/config.rs</files>
  <action>Add clap parser and resolver logic for `--todo`, `--archive`, `--config` per D-11/D-12/D-13/D-14/D-15. Apply config override path before config load, then enforce CLI-wins precedence and archive defaulting behavior. Keep implementation focused and avoid refactoring unrelated startup code paths.</action>
  <verify>
    <automated>cargo test -p todotxt-tui path_resolution_test -- --nocapture</automated>
  </verify>
  <done>All path-resolution tests pass and startup resolves paths according to PATH requirements.</done>
</task>

<task type="auto">
  <name>Task 3 (REFACTOR): Harden and simplify resolver with full crate checks</name>
  <files>crates/todotxt-tui/src/main.rs, crates/todotxt-tui/tests/path_resolution_test.rs</files>
  <action>Refactor only if needed for clarity and maintainability after tests pass; keep public behavior unchanged and preserve explicit precedence ordering in code comments or naming.</action>
  <verify>
    <automated>cargo test -p todotxt-tui -- --nocapture</automated>
  </verify>
  <done>Resolver remains behaviorally identical while clearer and fully validated by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> filesystem paths | Untrusted command-line values become file IO targets |
| config path override -> config load | User-selected file source influences app settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | T | CLI path resolver | mitigate | Normalize precedence in one resolver function and test collision cases explicitly |
| T-27-05 | D | startup path handling | mitigate | Validate and fail with clear error if resolved todo path is missing/unreadable |
| T-27-06 | E | `--config` override | accept | Local user controls process invocation; risk accepted for local CLI context |
</threat_model>

<verification>
- `cargo test -p todotxt-tui path_resolution_test -- --nocapture`
- `cargo test -p todotxt-tui -- --nocapture`
- `cargo check -p todotxt-tui`
</verification>

<success_criteria>
- PATH-01/02/03 behaviors are encoded in tests and passing.
- `--config` is applied before config read.
- CLI precedence and archive defaulting are deterministic.
- Existing startup behavior remains intact when no override flags are passed.
</success_criteria>

<output>
After completion, create `.planning/phases/27-config-defined-panes-validation-ship-readiness/27-02-SUMMARY.md`
</output>
