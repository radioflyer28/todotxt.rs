---
phase: 23-validation-ship-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md
  - .planning/phases/21-smart-text-normalization/21-VERIFICATION.md
  - .planning/phases/22-keymap-help-parity/22-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 20 has a VERIFICATION.md with status passed or human_needed covering BULK-01, BULK-02, BULK-03, PAR-01"
    - "Phase 21 has a VERIFICATION.md with status passed or human_needed covering NORM-01 through NORM-06"
    - "Phase 22 has a VERIFICATION.md with status passed or human_needed covering PAR-01, PAR-02, PAR-03, KEY-01, KEY-02, KEY-03"
    - "All three VERIFICATION.md files confirm cargo test suite is green"
    - "Each VERIFICATION.md follows the Phase 19 format exactly (YAML frontmatter, Observable Truths table, Requirements Coverage table)"
  artifacts:
    - path: ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md"
      provides: "Phase 20 verification report"
      contains: "BULK-01"
    - path: ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md"
      provides: "Phase 21 verification report"
      contains: "NORM-01"
    - path: ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md"
      provides: "Phase 22 verification report"
      contains: "KEY-01"
  key_links:
    - from: "20-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability"
      via: "Requirements Coverage table"
      pattern: "BULK-01.*SATISFIED"
    - from: "21-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability"
      via: "Requirements Coverage table"
      pattern: "NORM-01.*SATISFIED"
    - from: "22-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability"
      via: "Requirements Coverage table"
      pattern: "KEY-01.*SATISFIED"
---

<objective>
Produce VERIFICATION.md for phases 20, 21, and 22 by verifying each phase's delivered code against its requirements, following the Phase 19 VERIFICATION.md format exactly.

Purpose: The audit found phases 20/21/22 missing VERIFICATION.md (gsd-verify-work was never run). All 15 unverified requirements (BULK-01–03, NORM-01–06, PAR-01–03, KEY-01–03) will move from "partial" to "satisfied" once these files exist.

Output: Three VERIFICATION.md files — one per phase — each modeled after 19-VERIFICATION.md.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md

<!-- Phase 19 VERIFICATION.md — use as the EXACT format template for all three output files -->
@.planning/phases/19-selection-model-multi-select-foundation/19-VERIFICATION.md

<!-- Phase 20 — what was built -->
@.planning/phases/20-bulk-actions-selection-ux/20-01-SUMMARY.md
@.planning/phases/20-bulk-actions-selection-ux/20-02-SUMMARY.md
@.planning/phases/20-bulk-actions-selection-ux/20-03-SUMMARY.md

<!-- Phase 21 — what was built -->
@.planning/phases/21-smart-text-normalization/21-01-SUMMARY.md
@.planning/phases/21-smart-text-normalization/21-02-SUMMARY.md
@.planning/phases/21-smart-text-normalization/21-03-SUMMARY.md

<!-- Phase 22 — what was built -->
@.planning/phases/22-keymap-help-parity/22-01-SUMMARY.md
@.planning/phases/22-keymap-help-parity/22-02-SUMMARY.md
@.planning/phases/22-keymap-help-parity/22-03-SUMMARY.md
@.planning/phases/22-keymap-help-parity/DEVIATION.md
@.planning/phases/22-keymap-help-parity/22-VALIDATION.md
</context>

<interfaces>
<!-- Phase 19 VERIFICATION.md YAML frontmatter (exact format to replicate) -->
```yaml
---
phase: {phase-slug}
verified: {ISO8601 timestamp}
status: passed | human_needed | gaps_found
score: N/M must-haves verified
overrides_applied: 0
human_verification:
  - test: "{description}"
    expected: "{expected behavior}"
    why_human: "{why this cannot be automated}"
---
```

<!-- Codebase facts (verified by integration checker during audit) -->
Phase 20 key code locations:
- Bulk delete hotkey: app.rs:488 — `!self.selected_tasks.is_empty() && key_is_action(key, "bulk_delete")`
- handle_delete_confirm_key bulk path: app.rs:1009–1022 — descending sort + loop delete + selected_tasks.clear()
- render_delete_confirm: count-aware — "Delete N tasks?" for N > 1
- Bulk append hotkey: app.rs:501 — `!self.selected_tasks.is_empty() && key_is_action(key, "bulk_append")`
- handle_append_text_key: app.rs:1065 — iterates selected_tasks, builds replacements, calls batch_update
- Selection count in status bar: app.rs:1585 — `selected_tasks.len()` shown when non-empty
- Phase 20 tests (all pass): bulk_delete_descending_order, bulk_delete_cancel_clears_selection, bulk_delete_multiple_tasks_shows_count, bulk_append_commits_to_all_selected, bulk_append_esc_cancels, bulk_append_empty_enter_cancels, status_bar_shows_selected_count, status_bar_hint_includes_bulk_keys, status_bar_no_count_when_empty

Phase 21 key code locations:
- normalize_append export: todotxt-core/src/lib.rs:14
- normalize_line export: todotxt-core/src/lib.rs:14
- TUI import: app.rs:14 — `use todotxt_core::{..., normalize_append, normalize_line}`
- Bulk append normalize call: app.rs:1075 — `if self.config.normalize_append { normalize_append(t, &text) }`
- Edit save normalize call: app.rs:1140–1145 — `if self.config.normalize_edit { normalize_line(&text) }`
- Config toggles: config.rs — `normalize_append: bool` (default true), `normalize_edit: bool` (default true)
- Phase 21 tests: 16+ integration tests in todotxt-core/tests/normalize_tests.rs

Phase 22 key code locations:
- TuiConfig.keymap field: config.rs — `pub keymap: HashMap<String, String>` with `#[serde(default)]`
- parse_key_chord: config.rs — handles "ctrl+d", "backspace", "?", "SPACE", "f5" etc.
- default_keymap: config.rs:222 — 19 entries
- resolve_keymap: config.rs:262 — conflict detection, fallback, warning collection
- App.effective_keymap + keymap_warnings: app.rs:160/189
- key_is_action: app.rs — checks effective_keymap; mods.is_empty() for NONE check
- render_help_overlay: app.rs:1655 — 5 sections, reads from effective_keymap
- render_keymap_errors_overlay: app.rs:1777 — renders keymap_warnings list
- Status bar warning indicator: app.rs:1590
- '!' → KeymapErrors mode: app.rs:593
- DEVIATION.md: DEV-01 through DEV-07
- Phase 22 tests: 58 total pass (10 keymap/config tests + 48 others)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 20 VERIFICATION.md</name>
  <files>.planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-selection-model-multi-select-foundation/19-VERIFICATION.md (format template — copy structure exactly)
    - .planning/phases/20-bulk-actions-selection-ux/20-01-SUMMARY.md
    - .planning/phases/20-bulk-actions-selection-ux/20-02-SUMMARY.md
    - .planning/phases/20-bulk-actions-selection-ux/20-03-SUMMARY.md
    - .planning/REQUIREMENTS.md (BULK-01, BULK-02, BULK-03, PAR-01 descriptions)
    - crates/todotxt-tui/src/app.rs (bulk delete handler, bulk append handler, status bar — verify code exists)
  </read_first>
  <action>
Create `.planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md` using the Phase 19 VERIFICATION.md as the exact structural template.

**YAML frontmatter:**
```yaml
---
phase: 20-bulk-actions-selection-ux
verified: 2026-04-28T00:00:00Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual bulk delete confirmation panel shows count"
    expected: "Pressing D with 3 tasks selected shows 'Delete 3 tasks?  y=confirm  any=cancel' in the confirmation overlay"
    why_human: "Overlay rendering requires a live TUI terminal — cannot be verified without a running instance"
  - test: "Bulk append mode renders 'Append: ' label + text input"
    expected: "Pressing T with tasks selected shows a footer split with 9-char 'Append: ' prefix + text editor widget"
    why_human: "Two-part Layout footer rendering requires visual TUI inspection"
  - test: "Status bar shows selection count in real TUI"
    expected: "With 2 tasks selected, status bar left segment shows '| 2 selected'"
    why_human: "render_status_bar output requires a running TUI to visually confirm"
---
```

**Phase Goal:** Turn Phase 19's multi-selection model into safe bulk delete and bulk append flows, with selection count visible in the TUI.

**Observable Truths (BULK-01, BULK-02, BULK-03 + PAR-01):**
1. User can bulk delete all selected tasks with D hotkey via a confirmation flow
2. Confirmation shows "Delete N tasks?" count for multi-task selection
3. Bulk deletion happens in descending index order (prevents corruption)
4. After delete: selected_tasks cleared, disjoint_select reset, back to Normal
5. User can bulk append text to all selected tasks with T hotkey
6. Bulk append iterates selected tasks, appends text, commits via batch_update
7. Selection count is visible in the TUI status bar
8. Bulk action keys (D, T, v, Shift+nav) visible in status bar hint string

Verify each truth against the code (from SUMMARY files and interface block above). Mark each ✓ VERIFIED with the concrete evidence (line numbers from SUMMARY).

**Required Artifacts:**
- `crates/todotxt-tui/src/app.rs` — bulk delete hotkey guard, handle_delete_confirm_key bulk path, AppMode::AppendText variant, handle_append_text_key handler, render_status_bar selection indicator

**Key Links:**
- `key_is_action("bulk_delete")` → AppMode::DeleteConfirm → handle_delete_confirm_key → descending delete → selected_tasks.clear()
- `key_is_action("bulk_append")` → AppMode::AppendText → handle_append_text_key → batch_update
- selected_tasks.len() → render_status_bar → "| N selected" in left segment

**Behavioral Spot-Checks:**
```
cargo test -p todotxt-tui 2>&1 | grep -E "test result|bulk_delete|bulk_append|selected_count"
```
Expected: bulk_delete_descending_order, bulk_delete_cancel_clears_selection, bulk_delete_multiple_tasks_shows_count, bulk_append_commits_to_all_selected, bulk_append_esc_cancels, status_bar_shows_selected_count all show "ok"

**Requirements Coverage table:**
| Requirement | Source Plan | Description | Status | Evidence |
| BULK-01 | 20-01-PLAN.md | Bulk delete selected tasks with confirmation | ✓ SATISFIED | D hotkey at app.rs:488; descending delete at app.rs:1009; count-aware "Delete N tasks?" |
| BULK-02 | 20-02-PLAN.md | Bulk append text to all selected tasks | ✓ SATISFIED | T hotkey at app.rs:501; handle_append_text_key iterates selected_tasks; batch_update commits |
| BULK-03 | 20-03-PLAN.md | Selection count visible before bulk action | ✓ SATISFIED | render_status_bar shows "| N selected" at app.rs:1585; delete confirm shows count |
| PAR-01 | 20-01-PLAN.md | Hotkeys aligned with todotxt.net | ✓ SATISFIED | D/T/v defaults in default_keymap aligned with todotxt.net patterns; all overridable |
  </action>
  <verify>
    <automated>Test-Path ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md" -and (Select-String "BULK-01" ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md") -and (Select-String "SATISFIED" ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md")</automated>
  </verify>
  <done>20-VERIFICATION.md exists with YAML frontmatter, 8 observable truths verified, BULK-01/02/03 and PAR-01 all marked SATISFIED with evidence, status: human_needed</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 21 VERIFICATION.md</name>
  <files>.planning/phases/21-smart-text-normalization/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-selection-model-multi-select-foundation/19-VERIFICATION.md (format template)
    - .planning/phases/21-smart-text-normalization/21-01-SUMMARY.md
    - .planning/phases/21-smart-text-normalization/21-02-SUMMARY.md
    - .planning/phases/21-smart-text-normalization/21-03-SUMMARY.md
    - .planning/REQUIREMENTS.md (NORM-01 through NORM-06 descriptions)
    - crates/todotxt-core/src/lib.rs (verify normalize_append, normalize_line exports)
    - crates/todotxt-tui/src/app.rs (verify normalize calls in bulk append + edit save paths)
  </read_first>
  <action>
Create `.planning/phases/21-smart-text-normalization/21-VERIFICATION.md` using Phase 19 format.

**YAML frontmatter:**
```yaml
---
phase: 21-smart-text-normalization
verified: 2026-04-28T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Bulk append with priority token normalizes in real TUI"
    expected: "Appending '(A) urgent' to a task moves (A) to the priority prefix position in the saved task"
    why_human: "Full TUI flow (T → type → Enter) requires an interactive session; unit tests verify the core function but not the TUI wiring end-to-end visually"
  - test: "Edit with inline priority normalizes in real TUI"
    expected: "Editing a task to add '(B)' in the middle of text saves the task with (B) priority at prefix"
    why_human: "Same reason — edit save path verified by code inspection but visual confirmation needs TUI"
---
```

**Phase Goal:** Normalize recognized todo.txt metadata (priority, projects, contexts, dates) during append and edit flows using shared functions in todotxt-core.

**Observable Truths (NORM-01 through NORM-06):**
1. Priority token in appended text is placed at canonical prefix position (NORM-01)
2. +project tags from appended text are merged with existing tags, deduplicated (NORM-02)
3. @context tags from appended text are preserved/merged without forced relocation (NORM-03)
4. due: and t: dates in appended text override originals; original kept if none appended (NORM-04)
5. Plain text and unrecognized metadata are preserved verbatim (NORM-05)
6. Both normalize_append and normalize_line are exported from todotxt-core, not duplicated in TUI (NORM-06)

Verify each against the code. Mark ✓ VERIFIED with evidence from SUMMARY files and code locations.

**Required Artifacts:**
- `crates/todotxt-core/src/task.rs` — normalize_append, normalize_line implementations
- `crates/todotxt-core/src/lib.rs` — `pub use task::{normalize_append, normalize_line, ...}`
- `crates/todotxt-core/tests/normalize_tests.rs` — 16+ integration tests
- `crates/todotxt-tui/src/config.rs` — normalize_append and normalize_edit fields (default true)
- `crates/todotxt-tui/src/app.rs` — normalize_append call at bulk append path, normalize_line call at edit save path

**Key Links:**
- `app.rs` imports `normalize_append` from `todotxt_core` (not re-implemented)
- `handle_append_text_key` calls `normalize_append(t, &text)` when `config.normalize_append == true`
- `save_and_exit` (edit path) calls `normalize_line(&text)` when `config.normalize_edit == true`
- Config defaults both to true via `default_true()` helper

**Behavioral Spot-Checks:**
```
cargo test -p todotxt-core normalize
cargo test --workspace
```
Expected: All normalize_tests pass; full workspace green (320+ tests, 0 failures)

**Requirements Coverage table:** NORM-01–06 all SATISFIED with line-number evidence from SUMMARY files.
  </action>
  <verify>
    <automated>Test-Path ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md" -and (Select-String "NORM-06" ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md") -and (Select-String "SATISFIED" ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md")</automated>
  </verify>
  <done>21-VERIFICATION.md exists with YAML frontmatter, 6 observable truths verified, NORM-01–06 all marked SATISFIED with evidence, status: human_needed</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 22 VERIFICATION.md</name>
  <files>.planning/phases/22-keymap-help-parity/22-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-selection-model-multi-select-foundation/19-VERIFICATION.md (format template)
    - .planning/phases/22-keymap-help-parity/22-01-SUMMARY.md
    - .planning/phases/22-keymap-help-parity/22-02-SUMMARY.md
    - .planning/phases/22-keymap-help-parity/22-03-SUMMARY.md
    - .planning/phases/22-keymap-help-parity/DEVIATION.md
    - .planning/phases/22-keymap-help-parity/22-VALIDATION.md
    - .planning/REQUIREMENTS.md (PAR-01–03, KEY-01–03 descriptions)
    - crates/todotxt-tui/src/config.rs (verify parse_key_chord, default_keymap, resolve_keymap)
    - crates/todotxt-tui/src/app.rs (verify key_is_action, render_help_overlay, render_keymap_errors_overlay)
  </read_first>
  <action>
Create `.planning/phases/22-keymap-help-parity/22-VERIFICATION.md` using Phase 19 format.

**YAML frontmatter:**
```yaml
---
phase: 22-keymap-help-parity
verified: 2026-04-28T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Help overlay renders correctly in live TUI"
    expected: "Pressing '?' shows a centered popup with all 19 configurable bindings in 5 sections; Esc/q closes it"
    why_human: "render_help_overlay visual output requires live TUI inspection"
  - test: "Keymap warning overlay shows on '!'"
    expected: "With an invalid override in config.toml, status bar shows warning count and '!' opens the keymap errors overlay"
    why_human: "Requires a live TUI with a deliberate bad config entry"
  - test: "User-overridden key appears in help overlay"
    expected: "If user sets [keymap] move_down = 'j' (same as default) in config.toml, the override is accepted and help shows 'j' for move_down"
    why_human: "Requires config.toml modification + TUI restart"
  - test: "Conflict detection reverts both conflicting bindings"
    expected: "If user maps two actions to the same key, both are reverted to defaults and the warning overlay shows both conflict entries"
    why_human: "Requires deliberate config conflict + live TUI verification"
---
```

**Phase Goal:** Make implemented hotkeys configurable via config.toml, surface all active bindings in a help overlay, and document deliberate parity deviations.

**Observable Truths (PAR-01–03, KEY-01–03):**
1. All 19 overridable actions have default bindings matching todotxt.net orientation (KEY-03, PAR-01)
2. User can override an action's binding in [keymap] section of config.toml (KEY-01)
3. Invalid key chords fall back to defaults and surface a warning (KEY-02)
4. Conflicting bindings: both actions revert to defaults and both are in keymap_warnings (KEY-02)
5. Pressing '?' opens help overlay showing all 19 effective bindings (PAR-02)
6. DEVIATION.md exists with DEV-01 through DEV-07 documenting deliberate differences (PAR-03)

Verify each against code. Mark ✓ VERIFIED with evidence.

**Required Artifacts:**
- `crates/todotxt-tui/src/config.rs` — TuiConfig.keymap field, parse_key_chord, default_keymap (19 entries), resolve_keymap
- `crates/todotxt-tui/src/app.rs` — effective_keymap, keymap_warnings, key_is_action, render_help_overlay, render_keymap_errors_overlay, AppMode::Help, AppMode::KeymapErrors
- `.planning/phases/22-keymap-help-parity/DEVIATION.md` — DEV-01 through DEV-07

**Key Links:**
- config.toml [keymap] section → TuiConfig.keymap → resolve_keymap → effective_keymap stored on App
- key_is_action(key, "action") reads effective_keymap → all 19 overridable actions use this gate
- keymap_warnings non-empty → status bar shows "⚠ keymap: N warning(s) ('!' for details)"
- '?' → key_is_action("help") → AppMode::Help → render_help_overlay (reads effective_keymap)
- '!' guard: `!keymap_warnings.is_empty()` → AppMode::KeymapErrors → render_keymap_errors_overlay

**Behavioral Spot-Checks:**
```
cargo test -p todotxt-tui
```
Expected: 58/58 pass including all config/keymap tests

**Requirements Coverage table:** PAR-01–03 and KEY-01–03 all SATISFIED with evidence.
  </action>
  <verify>
    <automated>Test-Path ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md" -and (Select-String "KEY-03" ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md") -and (Select-String "SATISFIED" ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md")</automated>
  </verify>
  <done>22-VERIFICATION.md exists with YAML frontmatter, 6 observable truths verified, PAR-01–03 and KEY-01–03 all marked SATISFIED with evidence, status: human_needed, DEVIATION.md reference included</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan produces documentation files only. No code changes, no new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | N/A | Documentation files | accept | No executable code; VERIFICATION.md files are read-only markdown/YAML |
</threat_model>

<verification>
After all three tasks:

```powershell
# Verify all three VERIFICATION.md files exist
Test-Path ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md"
Test-Path ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md"
Test-Path ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md"

# Verify key content
Select-String "BULK-01" ".planning/phases/20-bulk-actions-selection-ux/20-VERIFICATION.md"
Select-String "NORM-06" ".planning/phases/21-smart-text-normalization/21-VERIFICATION.md"
Select-String "KEY-03" ".planning/phases/22-keymap-help-parity/22-VERIFICATION.md"

# Cargo tests still green
cargo test --workspace 2>&1 | Select-String "test result"
```
</verification>

<success_criteria>
- Three VERIFICATION.md files created matching Phase 19's exact format
- All 15 partial requirements (BULK-01–03, NORM-01–06, PAR-01–03, KEY-01–03) shown as SATISFIED with line-number evidence
- Each file has status: human_needed (visual TUI items remain for human sign-off in Plan 23-04)
- Cargo test suite still passes (no regressions from documentation work)
</success_criteria>

<output>
After completion, create `.planning/phases/23-validation-ship-readiness/23-01-SUMMARY.md`
</output>
