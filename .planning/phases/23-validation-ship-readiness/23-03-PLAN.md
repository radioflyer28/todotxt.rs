---
phase: 23-validation-ship-readiness
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "REQUIREMENTS.md shows all 19 v1 requirements as [x] checked"
    - "REQUIREMENTS.md traceability table shows Complete for all phases 19-22"
    - "ROADMAP.md has no duplicate Phase 20 entry"
    - "ROADMAP.md Phase 22 plans are checked [x]"
    - "ROADMAP.md Phase 23 plans 23-01 and 23-02 are checked [x]"
    - "CHANGELOG.md has a ## [1.3.0] section listing all v1.3 features"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with all v1 reqs checked"
      contains: "[x] BULK-01"
    - path: ".planning/ROADMAP.md"
      provides: "Clean roadmap with no duplicates"
      contains: "[x] 22-01-PLAN.md"
    - path: "CHANGELOG.md"
      provides: "v1.3 release notes"
      contains: "[1.3.0]"
  key_links:
    - from: "REQUIREMENTS.md"
      to: "v1.3-MILESTONE-AUDIT.md re-run"
      via: "requirements traceability table"
      pattern: "\\[x\\] BULK-01"
---

<objective>
Close out the documentation tech debt from the milestone audit: check off all completed requirements in REQUIREMENTS.md, clean up ROADMAP.md (remove duplicate Phase 20, fix Phase 22 checkboxes), and add the v1.3 section to CHANGELOG.md.

Purpose: The milestone audit flagged REQUIREMENTS.md with all BULK/NORM/PAR/KEY requirements still showing [ ] (unchecked) and "Pending" status, ROADMAP.md has a duplicate Phase 20 entry and unchecked Phase 22 plans, and CHANGELOG.md has no v1.3 entry. This plan closes all three items.

Output: Three updated files with accurate state.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CHANGELOG.md
@.planning/v1.3-MILESTONE-AUDIT.md

<!-- Phase SUMMARY frontmatter — provides field lists what was delivered -->
@.planning/phases/20-bulk-actions-selection-ux/20-01-SUMMARY.md
@.planning/phases/21-smart-text-normalization/21-01-SUMMARY.md
@.planning/phases/22-keymap-help-parity/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — check off all completed requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read entire file first to see exact current text)
  </read_first>
  <action>
Make these exact changes to `.planning/REQUIREMENTS.md`:

**1. Check off all v1 requirements (change `[ ]` to `[x]` for BULK/NORM/PAR/KEY):**
```
- [ ] BULK-01: ...  →  - [x] BULK-01: ...
- [ ] BULK-02: ...  →  - [x] BULK-02: ...
- [ ] BULK-03: ...  →  - [x] BULK-03: ...
- [ ] NORM-01: ...  →  - [x] NORM-01: ...
- [ ] NORM-02: ...  →  - [x] NORM-02: ...
- [ ] NORM-03: ...  →  - [x] NORM-03: ...
- [ ] NORM-04: ...  →  - [x] NORM-04: ...
- [ ] NORM-05: ...  →  - [x] NORM-05: ...
- [ ] NORM-06: ...  →  - [x] NORM-06: ...
- [ ] PAR-01: ...   →  - [x] PAR-01: ...
- [ ] PAR-02: ...   →  - [x] PAR-02: ...
- [ ] PAR-03: ...   →  - [x] PAR-03: ...
- [ ] KEY-01: ...   →  - [x] KEY-01: ...
- [ ] KEY-02: ...   →  - [x] KEY-02: ...
- [ ] KEY-03: ...   →  - [x] KEY-03: ...
```
SEL-01 through SEL-04 are already `[x]` — do not change them.

**2. Update traceability table — change all "Implemented — verification gap (Phase 23)" to "Complete":**
```
| BULK-01 | Phase 20 | Implemented — verification gap (Phase 23) |
→
| BULK-01 | Phase 20 | Complete |
```
Do this for all 15 rows (BULK-01–03, NORM-01–06, PAR-01–03, KEY-01–03).

**3. Update Last updated line:**
```
Last updated: 2026-04-28 — gap closure routing via Phase 23 (verification gap)
→
Last updated: 2026-04-28 — Phase 23 gap closure complete; all v1 requirements satisfied
```
  </action>
  <acceptance_criteria>
    - `Select-String "\- \[x\] BULK-01" .planning/REQUIREMENTS.md` returns a match
    - `Select-String "\- \[x\] NORM-06" .planning/REQUIREMENTS.md` returns a match
    - `Select-String "\- \[x\] KEY-03" .planning/REQUIREMENTS.md` returns a match
    - `Select-String "\- \[ \] BULK" .planning/REQUIREMENTS.md` returns NO match (all BULK unchecked gone)
    - `Select-String "\- \[ \] NORM" .planning/REQUIREMENTS.md` returns NO match
    - `Select-String "\- \[ \] PAR" .planning/REQUIREMENTS.md` returns NO match
    - `Select-String "\- \[ \] KEY" .planning/REQUIREMENTS.md` returns NO match
    - `Select-String "Complete" .planning/REQUIREMENTS.md` returns 19 matches (one per requirement)
    - `Select-String "Pending" .planning/REQUIREMENTS.md` returns NO match
    - `Select-String "verification gap" .planning/REQUIREMENTS.md` returns NO match
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace 2>&1 | Select-String "test result" # no regressions from docs edit</automated>
  </verify>
  <done>All 19 v1 requirements show [x], traceability table shows Complete for all, no Pending or "verification gap" entries remain</done>
</task>

<task type="auto">
  <name>Task 2: Clean ROADMAP.md and update Phase 22/23 plan checkboxes</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read entire file to identify exact duplicate entry and current checkbox states)
  </read_first>
  <action>
Make these exact changes to `.planning/ROADMAP.md`:

**1. The ROADMAP.md already had the duplicate Phase 20 unchecked entry removed in a prior commit (gap closure plan commit f0ecf3a). Verify this by reading the file. If no duplicate exists, skip this step.**

**2. Check off Phase 22 plans in the Phase Detail section (around line 120–130):**
Find the Phase 22 detail section with:
```
- [ ] 22-01-PLAN.md — Keymap schema in config and runtime binding resolution
- [ ] 22-02-PLAN.md — Safe fallback behavior for invalid or conflicting key overrides
- [ ] 22-03-PLAN.md — Help/status parity pass and deviation documentation
```
Change to:
```
- [x] 22-01-PLAN.md — Keymap schema in config and runtime binding resolution
- [x] 22-02-PLAN.md — Safe fallback behavior for invalid or conflicting key overrides
- [x] 22-03-PLAN.md — Help/status parity pass and deviation documentation
```
Also update the Status line in the Phase 22 detail section from `Status: planned` to `Status: Complete`.

**3. Check off Phase 23 plans 23-01 and 23-02 in the Planned Phases section and Phase Detail section:**
In the Planned Phases section:
```
- [ ] 23-01-PLAN.md — Phase verification run: gsd-verify-work for phases 20, 21, 22
- [ ] 23-02-PLAN.md — Nyquist validation close-out: gsd-validate-phase for phases 19 and 20
```
Change to:
```
- [x] 23-01-PLAN.md — Phase verification run: gsd-verify-work for phases 20, 21, 22
- [x] 23-02-PLAN.md — Nyquist validation close-out: gsd-validate-phase for phases 19 and 20
- [x] 23-03-PLAN.md — Requirements + docs close-out: REQUIREMENTS.md, ROADMAP.md, CHANGELOG
```
(23-03 itself is what we're doing now — mark it complete as part of this plan's output)

**4. In the Phase Detail section, add a Phase 23 entry** after the Phase 22 detail block:
```markdown
### Phase 23: Validation + Ship Readiness

Goal: close verification gaps from audit (phases 20/21/22), Nyquist validation, docs close-out, UAT, and final milestone audit.
Status: in-progress
Plans:

- [x] 23-01-PLAN.md — Phase verification run: gsd-verify-work for phases 20, 21, 22
- [x] 23-02-PLAN.md — Nyquist validation close-out: gsd-validate-phase for phases 19 and 20
- [x] 23-03-PLAN.md — Requirements + docs close-out: REQUIREMENTS.md, ROADMAP.md, CHANGELOG
- [ ] 23-04-PLAN.md — Human UAT checkpoint against todotxt.net parity expectations
- [ ] 23-05-PLAN.md — Final re-audit and milestone close-out
```
  </action>
  <acceptance_criteria>
    - `Select-String "\[x\] 22-01-PLAN.md" .planning/ROADMAP.md` returns a match
    - `Select-String "\[x\] 22-02-PLAN.md" .planning/ROADMAP.md` returns a match
    - `Select-String "\[x\] 22-03-PLAN.md" .planning/ROADMAP.md` returns a match
    - `Select-String "Status: Complete" .planning/ROADMAP.md` returns a match for Phase 22
    - `Select-String "\[x\] 23-01-PLAN.md" .planning/ROADMAP.md` returns a match
    - `Select-String "\[x\] 23-03-PLAN.md" .planning/ROADMAP.md` returns a match
    - `(Select-String "Phase 20" .planning/ROADMAP.md).Count -le 4` — no duplicate Phase 20 entries
  </acceptance_criteria>
  <verify>
    <automated>(Select-String "\[\s\]" .planning/ROADMAP.md | Select-String "22-0[123]-PLAN").Count -eq 0</automated>
  </verify>
  <done>Phase 22 plans all checked [x], Phase 23 detail section added, no unchecked Phase 22 plan entries remain</done>
</task>

<task type="auto">
  <name>Task 3: Add v1.3 section to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read current file — understand format and where to insert)
    - .planning/phases/22-keymap-help-parity/DEVIATION.md (for parity notes)
  </read_first>
  <action>
Insert a new section at the TOP of CHANGELOG.md (after the `# Changelog` heading, before `## [1.2.0]`):

```markdown
## [1.3.0] - 2026-04-28

### Added
- **Multi-select + range selection** — Press `v` to enter selection mode; `Space` toggles tasks; `Shift+j/k` extends a contiguous range; `Shift+Ctrl+D/U` extends by half page. Selected tasks survive filter, sort, and reload.
- **Bulk delete** — Press `D` (Shift+d) with tasks selected to enter a count-aware confirmation (`Delete N tasks?`). Deletion runs in descending index order to prevent corruption. After delete: selection cleared.
- **Bulk append** — Press `T` (Shift+t) with tasks selected to open an append prompt. Text is appended to all selected tasks atomically via `batch_update`.
- **Smart text normalization** — Append and edit flows normalize recognized todo.txt metadata: priority tokens moved to canonical prefix, +project and @context tags merged/deduplicated, `due:` and `t:` dates handled with field precedence, unknown metadata preserved verbatim. Normalization is done in `todotxt-core` and toggleable via `normalize_append` / `normalize_edit` config flags.
- **Configurable keymap** — Add a `[keymap]` section to `config.toml` to override action bindings (e.g. `move_down = "j"`). Supports: letter keys, modifier combos (`ctrl+d`), named keys (`backspace`, `enter`, `SPACE`, `f1`–`f12`). All 19 implemented actions are overridable.
- **Keymap conflict detection** — Invalid chords and conflicting bindings fall back to defaults. A warning indicator appears in the status bar; press `!` to see the full warning list.
- **Help overlay** — Press `?` to open a keybindings reference showing all active bindings (including user overrides) in 5 sections: Tasks, Filter, View, Select, App. Press `Esc` or `q` to close.
- **Parity hotkeys** — `0` clears the active filter, `1`–`9` apply filter presets, `.` reloads from disk. All aligned with todotxt.net defaults.
- **Selection count indicator** — Status bar shows `| N selected` when tasks are selected and hints for bulk action keys.
- **CLI `--normalize` flag** — `todotxt-cli` gains `--normalize` to apply smart normalization when adding or appending via the command line.

### Notes
- Deliberate deviations from todotxt.net WPF behavior are documented in `.planning/phases/22-keymap-help-parity/DEVIATION.md` (DEV-01 through DEV-07). Key differences: Shift+nav for range selection (todotxt.net uses mouse), no drag-and-drop priority setting, bulk append is text-only (no per-field batch edit).

```
  </action>
  <acceptance_criteria>
    - `Select-String "\[1.3.0\]" CHANGELOG.md` returns a match
    - `Select-String "Multi-select" CHANGELOG.md` returns a match
    - `Select-String "Bulk delete" CHANGELOG.md` returns a match
    - `Select-String "Configurable keymap" CHANGELOG.md` returns a match
    - `Select-String "\[1.2.0\]" CHANGELOG.md` returns a match (existing entry preserved)
    - The `[1.3.0]` section appears BEFORE `[1.2.0]` in the file (newest first)
  </acceptance_criteria>
  <verify>
    <automated>Select-String "\[1.3.0\]" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a ## [1.3.0] section at the top listing all v1.3 features; existing sections preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — documentation files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition |
|-----------|----------|-----------|-------------|
| T-23-03 | N/A | REQUIREMENTS.md, ROADMAP.md, CHANGELOG.md | accept — documentation only |
</threat_model>

<verification>
```powershell
# Requirements: all v1 reqs checked
Select-String "\[x\] BULK-01" .planning/REQUIREMENTS.md
Select-String "\[x\] KEY-03" .planning/REQUIREMENTS.md
Select-String "Pending" .planning/REQUIREMENTS.md  # should return nothing

# Roadmap: Phase 22 plans checked
Select-String "\[x\] 22-01-PLAN" .planning/ROADMAP.md
Select-String "\[x\] 22-03-PLAN" .planning/ROADMAP.md

# Changelog: v1.3 section present
Select-String "\[1.3.0\]" CHANGELOG.md
```
</verification>

<success_criteria>
- All 15 previously-unchecked v1 requirements are [x] in REQUIREMENTS.md
- All traceability table rows show "Complete" (no "Pending" or "verification gap")
- ROADMAP.md Phase 22 plans all checked, Phase 23 detail section present
- CHANGELOG.md ## [1.3.0] section at top with all 10 v1.3 features documented
</success_criteria>

<output>
After completion, create `.planning/phases/23-validation-ship-readiness/23-03-SUMMARY.md`
</output>
