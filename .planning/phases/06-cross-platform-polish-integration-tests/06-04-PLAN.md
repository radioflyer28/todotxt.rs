---
phase: 06-cross-platform-polish-integration-tests
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements:
  - CORE-08
  - CFG-01
  - READ-08

must_haves:
  truths:
    - "`.github/workflows/ci.yml` runs `cargo test --workspace` on ubuntu-latest"
    - "macOS and Windows matrix entries are present but commented out for SEED-004"
    - "README.md contains all 7 sections (Installation through todo.txt primer)"
    - "JSON schema section is structured table format (machine-parseable)"
    - "README Quick Start has exactly 5 commands that a new user can follow sequentially"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with ubuntu test + commented multi-OS matrix"
      contains: "cargo test --workspace"
    - path: "README.md"
      provides: "7-section human + AI agent co-equal documentation"
      min_lines: 150
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "cargo test --workspace"
      via: "run step in test job"
      pattern: "cargo test"
    - from: "README.md"
      to: "JSON schema section"
      via: "schema_version field documentation"
      pattern: "schema_version"
---

<objective>
Create `.github/workflows/ci.yml` (multi-OS matrix template with macOS/Windows commented) and replace `README.md` with a full 7-section document targeting human and AI agent audiences equally.

Purpose: Final deliverables — CI placeholder ready for SEED-004 expansion; README makes the tool immediately usable by humans and agents.
Output: ci.yml passes GitHub Actions validation; README has all 7 sections from D-03.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-cross-platform-polish-integration-tests/06-CONTEXT.md
@.planning/phases/06-cross-platform-polish-integration-tests/06-01-SUMMARY.md
@.planning/phases/06-cross-platform-polish-integration-tests/06-03-SUMMARY.md

<interfaces>
<!-- Key information for writing ci.yml and README correctly -->

From crates/todotxt-cli/src/cli.rs (commands for README reference table):
All subcommands: add, do, undo, del, edit, append, prepend, list, show, stats,
projects, contexts, pri, depri, due, postpone, archive, del-done, completions

JSON envelope format (for README JSON schema section):
```json
// Success (list):
{"schema_version":1,"data":[{"id":1,"raw":"task text","priority":null,...}]}
// Success (single task):
{"schema_version":1,"data":{"id":1,"raw":"(A) task text +project @context","priority":"A",...}}
// Error:
{"schema_version":1,"error":{"code":1,"message":"task not found: 99"}}
```

Config file format (from crates/todotxt-cli/src/config.rs — for README config section):
```toml
[paths]
todo_file = "/path/to/todo.txt"      # required
done_file = "/path/to/done.txt"      # optional

[display]
color = true
preset = "default"                    # default|minimal|compact

[behavior]
auto_creation_date = false
```

Exit codes (READ-08):
- 0 = success
- 1 = task not found / no match
- 2 = validation error / bad arguments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create the `.github` directory structure and `ci.yml` file.

    Per D-05 (locked):
    - Trigger: `on: push` and `on: pull_request` to `main` only
    - Active job: `test` on `ubuntu-latest`
    - Strategy matrix: `os: [ubuntu-latest]` with commented multi-OS expansion
    - Commented jobs: `clippy` and `doc`

    Exact structure to implement:
    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      test:
        name: Test
        runs-on: ubuntu-latest
        # Multi-OS matrix — uncomment for SEED-004:
        # strategy:
        #   matrix:
        #     os: [ubuntu-latest, macos-latest, windows-latest]
        # runs-on: ${{ matrix.os }}

        steps:
          - uses: actions/checkout@v4
          - name: Install Rust stable
            uses: dtolnay/rust-toolchain@stable
          - name: Run tests
            run: cargo test --workspace

      # clippy — uncomment for SEED-004:
      # clippy:
      #   name: Clippy
      #   runs-on: ubuntu-latest
      #   steps:
      #     - uses: actions/checkout@v4
      #     - uses: dtolnay/rust-toolchain@stable
      #       with:
      #         components: clippy
      #     - run: cargo clippy --workspace -- -D warnings

      # doc — uncomment for SEED-004:
      # doc:
      #   name: Doc
      #   runs-on: ubuntu-latest
      #   steps:
      #     - uses: actions/checkout@v4
      #     - uses: dtolnay/rust-toolchain@stable
      #     - run: cargo doc --workspace --no-deps
    ```

    Use `dtolnay/rust-toolchain@stable` (the de facto standard action for Rust CI —
    preferred over `actions-rs/toolchain` which is archived).

    After creating the file, validate YAML syntax:
    ```powershell
    # Basic check — ensure no tabs (YAML prohibits tabs for indentation)
    (Get-Content ".github/workflows/ci.yml" -Raw) -match "`t" | Should -Be $false
    ```
  </action>
  <verify>
    Test-Path ".github/workflows/ci.yml"
    Expected: True

    Get-Content ".github/workflows/ci.yml" | Select-String "cargo test --workspace"
    Expected: 1 match
  </verify>
  <done>
    ci.yml exists; contains `cargo test --workspace`; multi-OS matrix present but commented; YAML is valid (no tabs)
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace README.md with full 7-section Rust documentation</name>
  <files>README.md</files>
  <action>
    Replace the existing .NET-era `README.md` with a new Rust README covering all 7 sections
    per D-03 (locked). Current README contains legacy .NET content — replace it entirely.

    The README must target **both humans and AI agents as co-equal audiences** (D-03).

    Required sections (all 7):

    ## 1. Installation
    - `cargo install todotxt` (primary)
    - Pre-built binaries placeholder: "See Releases page (coming in v1.1)"
    - Shell completion post-install note

    ## 2. Quick Start (exactly 5 commands)
    ```sh
    todotxt add "Buy groceries +shopping @errands"
    todotxt list
    todotxt do 1
    todotxt stats
    todotxt archive
    ```
    Brief one-line explanation for each command.

    ## 3. Full Command Reference Table
    Markdown table with columns: Command | Description | Key Flags
    Include all 19 subcommands: add, do, undo, del, edit, append, prepend, list, show,
    stats, projects, contexts, pri, depri, due, postpone, archive, del-done, completions

    ## 4. JSON Schema Documentation (machine-parseable — structured tables, not prose)
    Subsections:
    - **Envelope format**: table showing schema_version (integer, always 1), data (object|array),
      error (object, present on failure)
    - **Task object fields**: table with Field | Type | Description | Example for:
      id (u64), raw (string), priority (string|null), creation_date (string|null),
      completion_date (string|null), is_complete (bool), projects (array<string>),
      contexts (array<string>), due_date (string|null, YYYY-MM-DD), tags (object)
    - **Error object**: code (1=not found, 2=validation), message (string)
    - **Usage note for agents**: "Parse `schema_version` first; reject if != 1 to detect breaking changes"

    ## 5. Config File Format
    - Platform paths table: Linux (~/.config/todotxt/config.toml), macOS (~/.config/todotxt/config.toml),
      Windows (%APPDATA%/todotxt/config.toml)
    - Portable mode note: "Place config.toml beside the binary to activate portable mode"
    - Full annotated config.toml example with all keys and their defaults

    ## 6. Shell Completion Instructions
    One code block per shell (bash, zsh, fish, powershell) showing the exact completion
    install command. Example:
    ```bash
    todotxt completions bash >> ~/.bashrc && source ~/.bashrc
    ```

    ## 7. todo.txt Format Primer
    Brief explanation of the todo.txt format for users who don't know it:
    - Completed tasks: `x 2026-01-01 task text`
    - Priority: `(A) task text`
    - Projects: `+projectname`
    - Contexts: `@contextname`
    - Key:value tags: `due:2026-05-01`
    - Link to official spec: https://github.com/todotxt/todo.txt

    Formatting rules:
    - Use GitHub-Flavored Markdown
    - Section 4 (JSON schema) must use tables, not prose paragraphs
    - All code examples use fenced code blocks with language hints (sh, toml, json, bash, zsh, fish, powershell)
    - README should be comprehensive but scannable — use headers liberally
  </action>
  <verify>
    (Get-Content "README.md" | Select-String "schema_version|Quick Start|Installation|todo\.txt Format|Shell Completion|Config File|Command Reference").Count
    Expected: >= 7 (one match per section header)
  </verify>
  <done>
    README.md has all 7 sections; JSON schema section uses tables; Quick Start has exactly 5 commands; .NET legacy content removed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml → GitHub Actions runner | Workflow file executes in GitHub's infrastructure |
| README → external users/agents | Documentation consumed by humans and AI agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | ci.yml supply chain (actions/checkout, dtolnay/rust-toolchain) | accept | Pin to @v4 / @stable tags; review on dependency updates |
| T-06-04-02 | Information Disclosure | README JSON examples | accept | All examples use synthetic data, no real credentials or paths |
| T-06-04-03 | Elevation of Privilege | GitHub Actions permissions | mitigate | ci.yml only runs `cargo test` — no write permissions, no token usage, no deployment steps |
</threat_model>

<verification>
```powershell
# Verify ci.yml
Test-Path ".github/workflows/ci.yml"
Get-Content ".github/workflows/ci.yml" | Select-String "cargo test|ubuntu-latest|actions/checkout"

# Verify README sections
(Get-Content "README.md" | Select-String "^## |^# ").Count

# Full workspace test still passes
cargo test --workspace 2>&1 | Select-Object -Last 3
```
</verification>

<success_criteria>
- `.github/workflows/ci.yml` exists with ubuntu-latest test job and commented multi-OS matrix
- `README.md` has all 7 sections; legacy .NET content removed
- JSON schema section uses structured tables with field names, types, descriptions
- Quick Start has exactly 5 sequential commands
- `cargo test --workspace` still passes (no regressions from README/CI changes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-platform-polish-integration-tests/06-04-SUMMARY.md`
</output>
