# GSD Planner Prompt — Phase 05

## Planning Context

**Phase:** 05  
**Mode:** standard  
**Status:** Ready for planning

## Files to Read

- `.planning/STATE.md` — Project state  
- `.planning/ROADMAP.md` — Roadmap (lines 208+)  
- `.planning/REQUIREMENTS.md` — Requirements  
- `.planning/phases/05-task-enrichment-bulk-operations/05-CONTEXT.md` — User decisions (D-01 through D-08)  
- `crates/todotxt-cli/src/commands/complete.rs` — Phase 4 pattern (multi-ID validation, atomic write)  
- `crates/todotxt-cli/src/commands/del.rs` — Phase 4 pattern (fail-fast, no-partial-delete)  
- `crates/todotxt-cli/src/config.rs` — Config struct to extend with `done_file` field  
- `crates/todotxt-cli/src/output.rs` — `print_write_result` renderer  

## Phase Requirements (MUST ALL APPEAR IN PLANS)

- ENRICH-01: `pri <id> <A-Z>` — set priority
- ENRICH-02: `depri <id>` — remove priority
- ENRICH-03: `due <id> <date>` — set due date (natural dates)
- ENRICH-04: `postpone <id> <N>` — move due date forward
- BULK-01: `archive` — move completed tasks to done.txt
- BULK-02: `del-done` — delete all completed tasks

## Context Decisions (LOCKED — must be implemented)

**D-01:** All 4 enrichment commands accept multiple IDs (fail-fast pattern)  
**D-02:** `done_file` configurable in config; default = sibling of `todo.txt`  
**D-03:** Date parsing strict ROADMAP list only (today/tomorrow/weekdays/YYYY-MM-DD)  
**D-04:** `archive` idempotency — print "0 tasks archived", exit 0  
**D-05:** `del-done` output — "Deleted N completed tasks"  
**D-06:** Exit codes: 0 success, 1 runtime error, 2 validation error  
**D-07:** JSON output required for all commands  
**D-08:** `postpone` missing due date → exit 2, error message  

## Patterns to Follow (from Phase 4)

- Multi-ID validation before mutations (see commands/complete.rs, commands/del.rs)
- Atomic file writes via temp-file + rename (TaskList::save pattern)
- Renderer: `print_write_result(&self, info: &str, idx: usize, task: &Task)`
- Exit codes: 0 (success), 1 (runtime), 2 (validation)
- JSON envelope: `json_success(task_dto(idx, task))` per task

## Deliverables (from ROADMAP)

- `crates/todotxt-cli/src/commands/priority.rs` — pri/depri
- `crates/todotxt-cli/src/commands/due.rs` — due/postpone (uses shared date parser)
- `crates/todotxt-cli/src/commands/archive.rs` — atomic two-file archive
- `crates/todotxt-cli/src/commands/del_done.rs` — in-place deletion
- `crates/todotxt-cli/src/date.rs` — shared date parsing utility
- `crates/todotxt-cli/tests/enrich_bulk_tests.rs` — integration tests (29+ tests)

## Your Job

Create 3-4 executable PLAN.md files in `.planning/phases/05-task-enrichment-bulk-operations/` following this structure:

1. **Frontmatter:** phase, plan, type (execute), wave, depends_on, files_modified, autonomous, requirements, must_haves
2. **Each task:** name, files (to create/modify), action (concrete, not vague), verify (grep-friendly), done (acceptance criteria)
3. **Dependencies:** Prefer parallel (Wave 1-2). File ownership: no same-file ownership across waves.
4. **Must-haves (goal-backward):** Derive from phase goal:
   - Truths: Commands work with multi-IDs, date parsing covers all formats, archive is atomic and idempotent
   - Artifacts: priority.rs, due.rs, archive.rs, del_done.rs, date.rs, tests
   - Key links: CLI wiring to new commands, date.rs usage by due/postpone, archive atomicity

## Suggested Wave Structure

**Wave 1:** Config extension + CLI wiring (prepare for 4 new commands)
**Wave 2:** Date parsing utility (shared by due/postpone, enables testing)
**Wave 3a:** Priority commands (pri/depri) — parallel
**Wave 3b:** Due-date commands (due/postpone) — parallel
**Wave 4:** Bulk operations (archive/del-done) — depends on prior waves
**Wave 5:** Integration tests (all commands functional)

---

**REMEMBER:** Every task MUST include `<read_first>` with the files it reads/modifies, and `<acceptance_criteria>` with grep-verifiable conditions. No vague language. Be concrete.
