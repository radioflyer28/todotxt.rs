---
phase: 26-pane-management-quick-hide-show
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - crates/todotxt-tui/src/components/help.rs
  - crates/todotxt-tui/src/ui.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Help overlay (?) displays all three new pane hotkeys in a 'Panes' section (D-22)"
    - "Pane controls appear alongside existing hotkey groups (filter, pane navigation, etc.) (D-22)"
    - "No dedicated pane help page — single overlay is sufficient (D-23)"
    - "No pane count or hidden state indicator in status bar (D-24)"
    - "Help text is discoverable and user-friendly"
  artifacts:
    - path: "crates/todotxt-tui/src/components/help.rs"
      provides: "Help overlay with Panes section"
      contains: "'Panes' section header", "Ctrl+N (Create pane)", "Ctrl+W (Delete pane)", "Ctrl+P (Toggle panes)"
    - path: "crates/todotxt-tui/src/ui.rs"
      provides: "Help overlay rendering logic"
      contains: "render_panes_help_section()"
  key_links:
    - from: "help overlay (?)"
      to: "Panes section"
      via: "Conditional rendering of help sections"
    - from: "Panes section"
      to: "three hotkeys"
      via: "Help text entries for pane_add, pane_delete, pane_hide_toggle"
---

<objective>
Update the help overlay (?) to display all three new pane hotkeys (Ctrl+N, Ctrl+W, Ctrl+P) in a dedicated "Panes" section alongside existing hotkey groups. Ensure discoverability and user guidance for the new pane management features without adding clutter to the status bar or introducing a dedicated pane help page.

Purpose: Enables users to discover and learn the new pane hotkeys within the existing help system. Maintains UI consistency and visual hierarchy.

Output: Updated help overlay with "Panes" section, three hotkey entries (create, delete, toggle), and clean rendering integration.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-pane-management-quick-hide-show/26-CONTEXT.md

# Reference Code Context

From crates/todotxt-tui/src/components/help.rs (or ui.rs):
- Help overlay component structure
- Existing hotkey sections (e.g., "Navigation", "Filtering", "Task Management")
- Help text formatting and layout
- Rendering logic for conditional sections

From crates/todotxt-tui/src/config.rs:
- default_keymap() with new pane_add, pane_delete, pane_hide_toggle entries
- Action enum variants for reference in help text

From Phase 24/25 planning:
- Multi-pane UI component structure
- Status bar rendering logic (to verify D-24: no pane state indicator added)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Panes section to help overlay with three new hotkeys</name>
  <files>crates/todotxt-tui/src/components/help.rs (or ui.rs)</files>
  <read_first>
    - crates/todotxt-tui/src/components/help.rs (entire file or lines 1-100+, help overlay structure)
    - Look for existing hotkey sections (e.g., "Navigation", "Filtering", "Task Management")
    - Look for help text formatting pattern (key description, hotkey format, etc.)
  </read_first>
  <action>
Add a new "Panes" section to the help overlay with three entries:

`ust
// In help overlay rendering function:
help_section("Panes", vec![
    ("Ctrl+N", "Create new pane"),
    ("Ctrl+W", "Delete active pane"),
    ("Ctrl+P", "Toggle panes hidden/visible"),
]);
`

Or if using a different structure, follow the existing pattern for hotkey groups. Ensure:
- Section header is labeled "Panes"
- Three entries for pane_add, pane_delete, pane_hide_toggle
- Hotkey format matches existing conventions (e.g., "Ctrl+Key" or similar)
- Description is clear and concise (e.g., "Create new pane", "Delete active pane", "Toggle hidden/visible")

Position the section logically (e.g., after "Navigation" or near other UI control sections).
  </action>
  <verify>
    - grep -n '"Panes"' crates/todotxt-tui/src/components/help.rs
    - grep -n "Ctrl+N" crates/todotxt-tui/src/components/help.rs
    - grep -n "Ctrl+W" crates/todotxt-tui/src/components/help.rs
    - grep -n "Ctrl+P" crates/todotxt-tui/src/components/help.rs
    - grep -n "Create new pane\|Delete active pane\|Toggle.*pane" crates/todotxt-tui/src/components/help.rs
  </verify>
  <done>Panes section added to help overlay with three hotkey entries. Help text is clear and follows existing formatting. No rendering errors.</done>
  <acceptance_criteria>
    - "Panes" section header exists in help overlay
    - Three entries: Ctrl+N (Create pane), Ctrl+W (Delete pane), Ctrl+P (Toggle panes)
    - Help text is clear and matches existing style/format
    - No section rendering errors or layout issues
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify help overlay renders without truncation or overlap</name>
  <files>crates/todotxt-tui/src/components/help.rs, crates/todotxt-tui/src/ui.rs</files>
  <read_first>
    - crates/todotxt-tui/src/components/help.rs (help rendering logic, layout sizing)
    - crates/todotxt-tui/src/ui.rs (TUI frame sizing and widget positioning)
  </read_first>
  <action>
Verify that the help overlay with the new Panes section:
1. Renders without truncation (all text visible)
2. Does not overlap with existing sections
3. Fits within the TUI frame at typical terminal sizes (e.g., 80x24, 120x40)
4. Scrolls gracefully if needed (for very small terminals)

Check the rendering logic for:
- Section ordering and spacing
- Text wrapping and line breaks
- Height/width constraints
- Conditional visibility (help overlay shown only on '?' key)

Adjust layout if necessary (e.g., reorder sections, adjust spacing, enable scrolling).
  </action>
  <verify>
    - cargo run -p todotxt-tui (build)
    - Press '?' to open help overlay
    - Visually verify Panes section displays without truncation or overlap
    - Verify at typical terminal sizes (80x24, 120x40)
    - Resize terminal and verify layout adapts gracefully
    - Press 'q' or Escape to close help overlay
  </verify>
  <done>Help overlay renders cleanly with new Panes section. No truncation or overlap. Fits at typical terminal sizes. Scrolls gracefully on small terminals.</done>
  <acceptance_criteria>
    - Help overlay builds without errors
    - Panes section displays without truncation
    - No overlap with existing sections
    - Fits at typical terminal sizes (80x24, 120x40)
    - Scrolls or adapts gracefully on smaller terminals
    - Help overlay can be opened/closed normally (? and q/Escape keys work)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify status bar does NOT show pane count or hidden state (D-24)</name>
  <files>crates/todotxt-tui/src/components/status.rs (or similar)</files>
  <read_first>
    - crates/todotxt-tui/src/components/status.rs (status bar rendering)
    - Look for where status indicators are rendered (e.g., mode, task count, filter indicator)
    - Verify no pane-related fields are added to status
  </read_first>
  <action>
Audit the status bar rendering logic to ensure:
1. No pane count indicator (e.g., "Panes: 3/10") is added
2. No hidden/visible state indicator (e.g., "Panes hidden") is added
3. Visual pane state is self-evident from the rendered layout (multi-pane borders when visible, single-pane when hidden)

Per D-24: "No pane count or hidden state indicator in the status bar — visual pane state is self-evident from the rendered layout."

If any pane-related status indicators were accidentally added during Phase 26-01/02, remove them.
  </action>
  <verify>
    - cargo run -p todotxt-tui
    - Start the app and observe status bar
    - Create panes (Ctrl+N) and verify no "Pane count" indicator appears
    - Toggle hidden (Ctrl+P) and verify no "hidden/visible" indicator appears
    - Verify status bar displays only expected fields (mode, task indicators, etc.)
  </verify>
  <done>Status bar audited. No pane count or hidden state indicator present. Visual pane state is self-evident from rendered layout.</done>
  <acceptance_criteria>
    - No pane count indicator in status bar
    - No hidden/visible state indicator in status bar
    - Status bar shows only expected fields (mode, task indicators, etc.)
    - Pane state is self-evident from rendered layout (multi-pane borders visible when not hidden, single-pane when hidden)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Manual smoke test - Help overlay displays Panes section correctly</name>
  <files>crates/todotxt-tui/src/components/help.rs, crates/todotxt-tui/src/app.rs</files>
  <read_first>
    - crates/todotxt-tui/src/main.rs
    - crates/todotxt-tui/src/components/help.rs
  </read_first>
  <action>
Build and test:
1. Start the app: cargo run -p todotxt-tui
2. Press '?' to open help overlay
3. Verify "Panes" section is visible and properly formatted
4. Read the three hotkey entries:
   - Ctrl+N: Create new pane
   - Ctrl+W: Delete active pane
   - Ctrl+P: Toggle panes hidden/visible
5. Verify text is clear, untruncated, and matches existing help style
6. Verify "Panes" section is positioned logically (e.g., after Navigation or with other UI controls)
7. Close help overlay (press 'q' or Escape)
8. Test the three hotkeys work as documented
9. Reopen help overlay to verify it displays consistently
10. Resize terminal and verify help overlay adapts

Record results, take screenshots if possible, note any layout issues.
  </action>
  <verify>
    - App builds without errors
    - Help overlay opens with '?' key
    - "Panes" section visible and properly formatted
    - Three hotkey entries displayed correctly
    - Text matches existing help style
    - Help overlay closes with 'q' or Escape
    - Hotkeys work as documented
    - Help overlay displays consistently across open/close cycles
    - Help overlay adapts to terminal resize
  </verify>
  <done>Manual smoke test passed. Help overlay displays Panes section correctly. Hotkey entries clear and discoverable. Help text matches existing style. No layout issues.</done>
  <acceptance_criteria>
    - App builds without errors
    - Help overlay displays "Panes" section
    - Three hotkey entries visible and untruncated
    - Entries match existing help formatting/style
    - "Panes" section positioned logically
    - Help overlay opens/closes normally
    - Hotkeys work as documented in help
    - Help consistent across open/close cycles
    - Adapts to terminal resize
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help overlay text | User-facing documentation; must be accurate and clear |
| status bar state | No sensitive pane state exposed; only visual layout changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Information Disclosure | help text | accept | Help text is user-facing documentation; no sensitive data exposed |
| T-26-09 | Denial of Service | help overlay rendering | accept | Help overlay is purely visual; no resource allocation or state mutation |

</threat_model>

<verification>
Help overlay updates complete when:
- [ ] Panes section added to help overlay
- [ ] Three hotkey entries: Ctrl+N, Ctrl+W, Ctrl+P
- [ ] Help text clear and matches existing style
- [ ] Help overlay renders without truncation or overlap
- [ ] Fits at typical terminal sizes (80x24, 120x40)
- [ ] Status bar does NOT show pane count or hidden state
- [ ] Visual pane state self-evident from layout
- [ ] Compilation passes: cargo check
- [ ] Manual smoke test: help displays correctly, hotkeys match, no layout issues
</verification>

<success_criteria>
Help overlay updates complete when:
- Help overlay displays new "Panes" section
- Three hotkeys documented: Ctrl+N (create), Ctrl+W (delete), Ctrl+P (toggle)
- Help text is clear, concise, and matches existing style
- Section positioned logically within help groups
- No truncation or overlap with existing sections
- Fits at typical terminal sizes
- No pane count/hidden state in status bar
- Visual pane state self-evident from rendered layout
- Multiple open/close cycles and terminal resize operations work correctly
- No layout glitches or rendering errors
</success_criteria>

<output>
After completion, create \.planning/phases/26-pane-management-quick-hide-show/26-03-PLAN-SUMMARY.md\
</output>
