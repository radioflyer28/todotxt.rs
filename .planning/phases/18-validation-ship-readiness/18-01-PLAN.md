---
phase: 18-validation-ship-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-validation-ship-readiness/UAT.md
autonomous: true
requirements:
  - V12-TUI-FILTER-01
  - V12-TUI-FILTER-02
  - V12-TUI-FILTER-03
  - V12-TUI-STATUS-01
  - V12-TUI-GROUP-01
  - V12-TUI-DEFER-01
  - V12-TUI-DEFER-02

must_haves:
  truths:
    - "UAT.md exists with 4 feature areas, each scenario has PASS/FAIL criteria"
    - "cargo test --workspace exits 0 with all tests green"
  artifacts:
    - path: ".planning/phases/18-validation-ship-readiness/UAT.md"
      provides: "Manual TUI walkthrough checklist for all 4 v1.2 TUI features"
  key_links:
    - from: "UAT.md scenarios"
      to: "REQUIREMENTS.md requirement IDs"
      via: "traceability comment per scenario"
---

<objective>
Write the TUI UAT checklist and confirm the automated test suite is fully green.

Purpose: Establish the manual validation baseline for the 4 TUI feature areas shipped in v1.2 phases 16–17, and confirm no regressions in the automated suite.
Output: `UAT.md` with step-by-step scenarios for grouping, deferred toggle, filter Esc/restore, and filter persist/reload; confirmed green cargo test run.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-validation-ship-readiness/18-CONTEXT.md
@.planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-VERIFICATION.md
@.planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-01-SUMMARY.md
@.planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UAT.md with 4-area walkthrough checklist</name>
  <files>.planning/phases/18-validation-ship-readiness/UAT.md</files>
  <read_first>
    - .planning/phases/18-validation-ship-readiness/18-CONTEXT.md (D-01 through D-04: UAT scope decisions)
    - .planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-VERIFICATION.md (11 verified truths — use as basis for human checks)
    - .planning/phases/16-tui-filter-ux-alignment/16-CONTEXT.md (filter Esc/restore and persist decisions)
    - crates/todotxt-tui/src/app.rs (reference for exact key bindings and behavior)
  </read_first>
  <action>
Create `.planning/phases/18-validation-ship-readiness/UAT.md` with the following structure:

---
phase: 18-validation-ship-readiness
date: 2026-04-23
status: pending
---

# Phase 18: TUI UAT Checklist

Build the TUI binary first: `cargo build -p todotxt-tui`
Run it against a test todo.txt file with a mix of tasks including at least:
- One task with `+project` tag
- One task with `@context` tag
- One task with `t:` threshold date in the future (e.g., `t:2026-12-31`)
- One completed task (`x `)
- One task with a priority (`(A)`)

Mark each item PASS or FAIL. Add notes for any FAIL.

---

## Area 1: Task Grouping (`g` key) [V12-TUI-GROUP-01]

| # | Step | Expected | Result |
|---|------|----------|--------|
| G1 | Press `g` once | Status bar shows `\| group: on`; tasks are separated by header rows (REVERSED style) showing shared key value | |
| G2 | With grouping on, use `j`/`k` to navigate | Cursor skips header rows and lands only on task rows | |
| G3 | With grouping on, press `x` on a task | Task is marked complete; header navigation is unaffected | |
| G4 | Press `g` again | Status bar `\| group: on` disappears; no header rows visible; task list returns to ungrouped | |
| G5 | Change sort order with `o`, then press `g` | Groups reflect the new sort key (e.g., sort by project → `+project` headers; sort by context → `@context` headers) | |

PASS criteria: All 5 items PASS.

---

## Area 2: Deferred Task Toggle (`h` key) [V12-TUI-DEFER-01, V12-TUI-DEFER-02]

| # | Step | Expected | Result |
|---|------|----------|--------|
| D1 | Start TUI — check if future-threshold task is visible | Task with `t:2026-12-31` is NOT shown in the list by default | |
| D2 | Press `h` | Status bar shows `[+deferred]`; the future-threshold task becomes visible, rendered with DIM styling | |
| D3 | Visually compare deferred task to active tasks | Deferred task appears visibly lighter/dimmer than surrounding active tasks | |
| D4 | Press `h` again | `[+deferred]` disappears from status bar; deferred task is hidden again | |
| D5 | With `h` on, navigate to the deferred task and press `Enter` (preview) | Preview opens showing the task's content including the `t:` tag | |

PASS criteria: All 5 items PASS.

---

## Area 3: Filter Esc / Restore [V12-TUI-FILTER-01]

| # | Step | Expected | Result |
|---|------|----------|--------|
| F1 | Press `/` to open quick filter, type some text | Filter bar shows typed text, task list filters live | |
| F2 | Press `Esc` while filter text is changed from prior value | Filter text reverts to what it was before `/` was pressed; task list restores | |
| F3 | Press `/`, type text, press `Enter` to confirm | Filter applies and stays; pressing `Esc` again after confirm does NOT clear the filter | |
| F4 | Open filter with active filter already set, change it, press `Esc` | Reverts to the previously confirmed filter value (not empty) | |

PASS criteria: All 4 items PASS.

---

## Area 4: Filter Persist / Reload [V12-TUI-FILTER-03]

| # | Step | Expected | Result |
|---|------|----------|--------|
| P1 | Open an F-key preset panel (e.g., press `F1` or use the preset UI) and configure a filter string | Preset is saved to TOML config (check `~/.config/todotxt-tui/config.toml` or portable equivalent) | |
| P2 | Quit the TUI completely (`q`) and relaunch | Previously configured preset is still present and functional | |
| P3 | Apply the preset and verify it filters tasks correctly | Task list filters as expected with no "empty filter" or crash on startup | |

PASS criteria: All 3 items PASS.

---

## Summary

| Area | Result |
|------|--------|
| G: Task Grouping | |
| D: Deferred Toggle | |
| F: Filter Esc/Restore | |
| P: Filter Persist | |

Overall: PASS / FAIL (circle one)
Notes:
  </action>
  <verify>
    <automated>Test-Path ".planning/phases/18-validation-ship-readiness/UAT.md"</automated>
  </verify>
  <done>UAT.md exists with all 4 areas, each with numbered steps, expected behavior, PASS/FAIL result column, and requirement traceability comments</done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and confirm green</name>
  <files></files>
  <read_first>
    - Cargo.toml (workspace member list)
  </read_first>
  <action>
Run: `cargo test --workspace`

Confirm all test suites exit 0. Expected: ~21 test suites, all `test result: ok`. If any suite fails, note the failing test(s) in the plan summary — do NOT proceed to mark this task done until all tests pass or failures are documented as known pre-existing issues.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | Select-String "FAILED|error\[" | Select-Object -First 5</automated>
  </verify>
  <done>`cargo test --workspace` exits 0 with no FAILED lines. All ~21 test suites report `test result: ok`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | UAT.md reads/writes are local only — no network, no user input injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | UAT.md file | accept | Local planning artifact only; no security surface |
| T-18-02 | Information Disclosure | cargo test output | accept | Test output stays local; no credentials in test suite |
</threat_model>

<verification>
- `UAT.md` exists at `.planning/phases/18-validation-ship-readiness/UAT.md`
- `UAT.md` contains all 4 area sections (Grouping, Deferred, Filter Esc/Restore, Filter Persist)
- `cargo test --workspace` exits 0
</verification>

<success_criteria>
UAT checklist exists and is ready for human execution. Automated test suite is fully green. No regressions introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-ship-readiness/18-01-SUMMARY.md`
</output>
