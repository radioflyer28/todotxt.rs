---
phase: 18-validation-ship-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .planning/REQUIREMENTS.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - V12-COMPAT-01
  - V12-COMPAT-02
  - V12-TUI-FILTER-01
  - V12-TUI-FILTER-02
  - V12-TUI-FILTER-03
  - V12-TUI-STATUS-01
  - V12-TUI-GROUP-01
  - V12-TUI-DEFER-01
  - V12-TUI-DEFER-02

must_haves:
  truths:
    - "All 9 v1.2 requirements are marked complete in REQUIREMENTS.md"
    - "README.md mentions todo.sh compatibility and the TUI"
    - "CHANGELOG.md exists with a v1.2 section listing shipped features"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 9 v1.2 requirements marked [x]"
    - path: "CHANGELOG.md"
      provides: "v1.2 release notes"
    - path: "README.md"
      provides: "Updated feature overview"
  key_links:
    - from: "REQUIREMENTS.md requirements"
      to: "implementation phases"
      via: "traceability table phase references"
---

<objective>
Mark all 9 v1.2 requirements complete, create CHANGELOG.md, and update README.md for the v1.2 release.

Purpose: Close out the requirement tracking record, give users a CHANGELOG for the v1.2 release, and ensure README reflects the shipped feature set.
Output: All requirements marked `[x]` in REQUIREMENTS.md; new CHANGELOG.md with v1.2 section; README.md updated with TUI and compat features.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-validation-ship-readiness/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark all 9 v1.2 requirements complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — check which are already marked [x])
    - .planning/ROADMAP.md (phase-to-requirement mapping for traceability table)
  </read_first>
  <action>
Read `.planning/REQUIREMENTS.md`. For each of the 9 v1.2 requirements, change `[ ]` to `[x]`:

- `[ ] V12-COMPAT-01` → `[x] V12-COMPAT-01`
- `[ ] V12-COMPAT-02` → `[x] V12-COMPAT-02`
- `[ ] V12-TUI-FILTER-01` → `[x] V12-TUI-FILTER-01`
- `[ ] V12-TUI-FILTER-02` → `[x] V12-TUI-FILTER-02`
- `[ ] V12-TUI-FILTER-03` → `[x] V12-TUI-FILTER-03`
- `[ ] V12-TUI-STATUS-01` → `[x] V12-TUI-STATUS-01`
- `[ ] V12-TUI-GROUP-01` → `[x] V12-TUI-GROUP-01`
- `[ ] V12-TUI-DEFER-01` → `[x] V12-TUI-DEFER-01`
- `[ ] V12-TUI-DEFER-02` → `[x] V12-TUI-DEFER-02`

Also update the Traceability Template table: change `Planned` → `Complete` in the Status column for all 9 rows.
  </action>
  <verify>
    <automated>Select-String "\[ \] V12-" .planning/REQUIREMENTS.md | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>All 9 `V12-*` requirements show `[x]` in REQUIREMENTS.md. The Select-String count of `[ ] V12-` is 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md and update README.md for v1.2</name>
  <files>CHANGELOG.md, README.md</files>
  <read_first>
    - README.md (current state — check Table of Contents and feature sections)
    - .planning/ROADMAP.md (v1.2 scope summary for accurate feature list)
    - .planning/phases/15-todo-sh-compat-layer/15-01-SUMMARY.md (compat alias list)
    - .planning/phases/16-tui-filter-ux-alignment/16-CONTEXT.md (filter UX decisions)
    - .planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-CONTEXT.md (grouping/deferred decisions)
  </read_first>
  <action>
**Step 1 — Create CHANGELOG.md** at the repo root:

```markdown
# Changelog

## [1.2.0] - 2026-04-23

### Added
- **todo.sh compatibility layer** — `todo.sh` command aliases (`add`/`a`, `do`/`x`, `list`/`ls`, `listall`/`lsa`, `listpri`/`lsp`, `del`/`rm`, `append`/`app`, `prepend`/`prep`, `pri`/`p`, `depri`/`dp`, `archive`/`arc`) are now recognized. Use `--compat` flag for numbered `{N} {task}` output format.
- **Deferred task support (`t:` threshold dates)** — Tasks with a future `t:YYYY-MM-DD` are hidden from the default list. TUI: press `h` to toggle deferred task visibility (shown with DIM styling and `[+deferred]` status indicator). CLI: use `--all` flag to include deferred tasks.
- **TUI task grouping** — Press `g` in the TUI to group tasks by the active sort key (project, context, priority, etc.). Group headers appear as reversed-style rows; navigation skips headers automatically.
- **TUI filter Esc/restore** — Pressing `Esc` in the filter panel now restores the previously confirmed filter instead of clearing it.
- **Persistent filter presets** — F-key filter presets are saved to TOML config and restored on startup.

### Changed
- **TUI status bar** — Removed the theme label from the status bar for a cleaner display. Status now shows sort order, active filter, grouping state, and deferred toggle.

## [1.1.0] - 2026-04-23

- TUI interface (`todotxt-tui`) — keyboard-driven terminal UI with add/edit/delete, filtering/sorting, presets, and themes.

## [1.0.0] - 2026-04-16

- Initial release: `todotxt-core` parser + `todotxt-cli` with 25+ commands, JSON output, TOML config, shell completions, cross-platform support.
```

**Step 2 — Update README.md:**

Find the section starting with `## 1. Installation` or the top description block. Add or update a "Features" or "What's New" callout near the top (after the intro paragraph, before the Table of Contents) if one does not exist:

```markdown
## Features

- **CLI** — 25+ commands: add, do, list, filter, sort, archive, bulk ops, JSON output, TOML config, shell completions
- **todo.sh compatible** — Drop-in alias support for `add`/`a`, `do`/`x`, `ls`, `lsa`, `lsp`, `rm` and more. Use `--compat` for numbered output.
- **TUI** — Keyboard-driven terminal UI: live filter, sort, grouping (`g`), deferred task toggle (`h`), persistent filter presets
- **todo.txt format** — Strict round-trip: priorities, projects (`+tag`), contexts (`@tag`), due dates (`due:`), threshold dates (`t:`), completion dates
- **Cross-platform** — Windows, Linux, macOS
```

If a Features section already exists, update it to include the v1.2 items (compat aliases, grouping, deferred toggle, filter persist).
  </action>
  <verify>
    <automated>Test-Path "CHANGELOG.md"; Select-String "1.2.0" CHANGELOG.md | Select-Object -First 1</automated>
  </verify>
  <done>CHANGELOG.md exists at repo root with a `[1.2.0]` section. README.md has a Features section that mentions todo.sh compatibility and TUI grouping/deferred toggle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | All edits are to local planning/doc files — no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | REQUIREMENTS.md | accept | Planning artifact; local only |
| T-18-04 | Information Disclosure | CHANGELOG.md | accept | Public-facing changelog; contains no credentials or sensitive data |
</threat_model>

<verification>
- `Select-String "\[ \] V12-" .planning/REQUIREMENTS.md` returns 0 matches
- `CHANGELOG.md` exists with a `[1.2.0]` section
- `README.md` contains the word "grouping" or "todo.sh"
</verification>

<success_criteria>
All 9 v1.2 requirements marked complete. CHANGELOG.md created with accurate v1.2 release notes. README.md updated to reflect shipped features.
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-ship-readiness/18-02-SUMMARY.md`
</output>
