---
phase: 18-validation-ship-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified: []
autonomous: false
requirements:
  - V12-TUI-FILTER-01
  - V12-TUI-FILTER-02
  - V12-TUI-FILTER-03
  - V12-TUI-STATUS-01
  - V12-TUI-GROUP-01
  - V12-TUI-DEFER-01
  - V12-TUI-DEFER-02

must_haves:
  truths:
    - "Human has run all 4 UAT areas and confirmed PASS"
  artifacts: []
  key_links: []
---

<objective>
Human UAT checkpoint — user runs the TUI walkthrough against a live binary and approves or reports issues.

Purpose: TUI features cannot be verified by automated tests. This checkpoint ensures the human has exercised all 4 v1.2 TUI feature areas before the milestone is closed.
Output: User approval to proceed with milestone close-out.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-validation-ship-readiness/UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 18-01 and 18-02 have:
    - Created UAT.md with 4-area TUI walkthrough checklist
    - Confirmed all automated tests pass (cargo test --workspace green)
    - Marked all 9 v1.2 requirements complete in REQUIREMENTS.md
    - Created CHANGELOG.md and updated README.md
  </what-built>
  <how-to-verify>
    1. Build the TUI binary:
       ```
       cargo build -p todotxt-tui
       ```

    2. Create a test todo.txt file with:
       - A task with `+project` tag (e.g., `Buy groceries +shopping`)
       - A task with `@context` tag (e.g., `Call dentist @phone`)
       - A task with a future threshold date (e.g., `Future task t:2026-12-31`)
       - A completed task (e.g., `x 2026-01-01 Done task`)
       - A priority task (e.g., `(A) Urgent thing`)

    3. Run the TUI pointing at your test file and work through UAT.md:
       `.planning/phases/18-validation-ship-readiness/UAT.md`

    4. Verify each area:
       - **G (grouping):** Press `g` — headers appear, nav skips them, `| group: on` in status bar
       - **D (deferred):** Press `h` — future threshold task appears DIM, `[+deferred]` in status bar
       - **F (filter Esc):** Press `/`, type text, press `Esc` — text reverts to prior value
       - **P (filter persist):** Configure an F-key preset, quit, relaunch — preset survives
  </how-to-verify>
  <resume-signal>Type "approved" if all areas pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local binary | TUI runs against a local test file; no network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing | Human checkpoint | accept | This is a human verification gate — no automated spoofing risk |
</threat_model>

<verification>
Human confirms all 4 UAT areas PASS.
</verification>

<success_criteria>
User types "approved" — all 4 TUI feature areas verified working in a live binary.
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-ship-readiness/18-03-SUMMARY.md`
</output>
