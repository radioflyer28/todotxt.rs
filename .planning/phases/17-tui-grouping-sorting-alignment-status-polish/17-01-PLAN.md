---
phase: 17-tui-grouping-sorting-alignment-status-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - V12-TUI-STATUS-01
  - V12-TUI-DEFER-02

must_haves:
  truths:
    - "Theme label is absent from the status bar"
    - "Pressing h in Normal mode toggles show_deferred on/off"
    - "When show_deferred is true, filter.suppress_future_threshold is false in rebuild_display_indices"
    - "Status bar shows [+deferred] when show_deferred is true"
    - "Deferred tasks (threshold_date > today) render with Modifier::DIM when show_deferred is true"
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "show_deferred field, h key handler, filter wiring, DIM styling, status bar changes"
      contains: "show_deferred"
  key_links:
    - from: "App::show_deferred"
      to: "rebuild_display_indices"
      via: "sets filter.suppress_future_threshold = !self.show_deferred"
      pattern: "suppress_future_threshold"
    - from: "render_task_list"
      to: "task.threshold_date"
      via: "DIM styling guard when show_deferred && threshold_date > today"
      pattern: "threshold_date"
---

<objective>
Remove the theme label from the status bar entirely (D-07) and implement deferred task (`t:`) visibility toggle (D-08 to D-11): `h` key toggles `show_deferred`, future-threshold tasks appear dimmed when visible, and `[+deferred]` appears in the status bar.

Purpose: Complete V12-TUI-STATUS-01 (status bar cleanup) and V12-TUI-DEFER-02 (TUI deferred task toggle). These are independent of grouping and can ship in Wave 1.

Output: Modified `app.rs` with status bar cleanup and deferred toggle. Compiles cleanly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-CONTEXT.md
@.planning/phases/14-compat-discovery/14-DEFER-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Status bar cleanup + deferred data model + filter wiring</name>
  <files>crates/todotxt-tui/src/app.rs</files>

  <read_first>
    - `crates/todotxt-tui/src/app.rs` — Full file. Focus on:
      - `App` struct definition (lines ~72–108): field list and order
      - `App::new(...)` initializer: where to add `show_deferred: false`
      - `rebuild_display_indices` (lines ~730–756): where Filter is constructed and how `suppress_future_threshold` can be set after `Filter::from_query`
      - `render_status_bar` (lines ~910–1000): exact location of `theme_label` push_str, middle string build pattern, and where to append `[+deferred]`
      - Normal mode key handler (lines ~220–340): where to insert `KeyCode::Char('h')` case
    - `crates/todotxt-core/src/filter.rs` — Confirm `suppress_future_threshold: bool` field is pub and accessible after construction
  </read_first>

  <action>
  Make the following changes to `crates/todotxt-tui/src/app.rs`:

  **1. Add `show_deferred` to the `App` struct** (after the `theme: Theme` field):
  ```rust
  pub show_deferred: bool,
  ```

  **2. Initialize `show_deferred` in `App::new()`** (alongside other boolean field initializations):
  ```rust
  show_deferred: false,
  ```

  **3. Wire `show_deferred` in `rebuild_display_indices`** — after the `Filter::from_query` call, add:
  ```rust
  if self.show_deferred {
      filter.suppress_future_threshold = false;
  }
  ```
  Note: `Filter::from_query` sets `suppress_future_threshold = true` by default (per DEFER-SPEC). This override allows future-threshold tasks through when the user enables the deferred view.

  **4. Add `h` key handler in Normal mode** — in the `handle_normal_key` match block, add a new arm:
  ```rust
  KeyCode::Char('h') => {
      self.show_deferred = !self.show_deferred;
      self.rebuild_display_indices();
  }
  ```

  **5. Remove theme label from `render_status_bar`** (per D-07) — locate the `push_str` or `format!` call that adds `theme: {label}` / `| theme: ...` to the `middle` string and delete it entirely. Do not replace it with a conditional — remove it outright.

  **6. Add `[+deferred]` to `render_status_bar`** — in the same `middle` string construction block, after the sort label, add:
  ```rust
  if self.show_deferred {
      middle.push_str(" [+deferred]");
  }
  ```
  </action>

  <verify>
    <automated>cargo build -p todotxt-tui 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `cargo build -p todotxt-tui` exits 0 with no errors
    - `grep -n "show_deferred" crates/todotxt-tui/src/app.rs` shows ≥4 occurrences (struct field, new() init, rebuild wiring, key handler)
    - `grep -n "suppress_future_threshold" crates/todotxt-tui/src/app.rs` shows the wiring line
    - `grep -n "theme_label\|theme:" crates/todotxt-tui/src/app.rs` shows no remaining status bar push for theme label
    - `grep -n "deferred" crates/todotxt-tui/src/app.rs` shows the `[+deferred]` push_str
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deferred task DIM styling in render_task_list + key hint update</name>
  <files>crates/todotxt-tui/src/app.rs</files>

  <read_first>
    - `crates/todotxt-tui/src/app.rs` — Lines ~860–910 (`render_task_list` style precedence block). Note the exact structure of the `if/else if` style chain and what `self.styles.dim` or equivalent is called for completed tasks.
    - `crates/todotxt-core/src/task.rs` — Confirm `threshold_date: Option<NaiveDate>` field and how `NaiveDate` is imported. Verify the field is accessible on a `&Task` reference.
  </read_first>

  <action>
  Make the following changes to `crates/todotxt-tui/src/app.rs`:

  **1. Add deferred DIM styling in `render_task_list`** — in the style precedence chain, insert a new branch *after* the `completed → DIM` check and *before* the priority-A check:
  ```rust
  } else if self.show_deferred
      && task.threshold_date.map_or(false, |d| {
          d > chrono::Local::now().date_naive()
      })
  {
      // Deferred (future threshold) — dim when shown
      Style::default().add_modifier(Modifier::DIM)
  }
  ```
  If `chrono` is not already imported at the top of `app.rs`, add `use chrono::Local;` (check existing imports first — it may already be available via `todotxt_core`'s re-exports or a direct dependency). Use the same `today` pattern that the overdue check uses if one already exists.

  Style note: Use the same style expression used for completed tasks (`self.styles.dim` or `Style::default().add_modifier(Modifier::DIM)`) — mirror whichever form is already present in the style chain for consistency.

  **2. Update the right-side key hint string** in `render_status_bar` — extend the static hint string to include the two new keys:
  - Current: `"q quit | n add | u edit | d del | x done | j/k nav | f filter | o sort"`
  - New: `"q quit | n add | u edit | d del | x done | j/k nav | f filter | o sort | g group | h deferred"`

  Note: `g group` is included here in anticipation of Plan 17-02 (grouping feature). Add it now so Plan 17-02 only needs the behavior, not the hint. If this causes a hint truncation issue, Plan 17-02's checkpoint will surface it.
  </action>

  <verify>
    <automated>cargo build -p todotxt-tui 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `cargo build -p todotxt-tui` exits 0
    - `grep -n "threshold_date" crates/todotxt-tui/src/app.rs` shows the DIM styling check
    - `grep -n "deferred" crates/todotxt-tui/src/app.rs` shows both the DIM guard and the `[+deferred]` hint
    - `grep -n "g group\|h deferred" crates/todotxt-tui/src/app.rs` shows the updated hint string
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task file → TUI display | Task data is read from a todo.txt file on disk; threshold_date is parsed from user-controlled text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | rebuild_display_indices | accept | show_deferred is in-memory toggle only; no persistence, no data mutation |
| T-17-02 | DoS | render_task_list threshold_date check | accept | chrono NaiveDate arithmetic is infallible; threshold_date is already parsed safely by todotxt-core |
| T-17-03 | Information Disclosure | status bar [+deferred] | accept | Indicates user's own toggle state; no sensitive data exposed |
</threat_model>

<verification>
After both tasks complete:
1. `cargo build -p todotxt-tui` exits 0
2. `grep -c "show_deferred\|threshold_date\|\[+deferred\]" crates/todotxt-tui/src/app.rs` returns ≥5
3. No occurrence of `theme_label` or `theme:` in `render_status_bar` push_str calls
</verification>

<success_criteria>
- Theme label removed from status bar (D-07)
- `h` key toggles deferred task visibility (D-08, D-09)
- Future-threshold tasks appear with DIM styling when show_deferred is true (D-10)
- `[+deferred]` appears in status bar when show_deferred is true (D-11)
- Binary compiles without errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/17-tui-grouping-sorting-alignment-status-polish/17-01-SUMMARY.md`
</output>
