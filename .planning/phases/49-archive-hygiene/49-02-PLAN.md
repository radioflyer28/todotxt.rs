---
phase: 49-archive-hygiene
plan: 02
type: tdd
wave: 2
depends_on: [49-01]
files_modified:
  - crates/todotxt-cli/src/commands/archive.rs
  - crates/todotxt-cli/tests/enrich_bulk_tests.rs
autonomous: true
requirements: [DONE-01, DONE-02]
must_haves:
  truths:
    - "D-04: CLI archive rotates only during archive writes"
    - "D-05: rotated archives preserve prior done entries in a period file"
    - "D-06: a fresh active done.txt receives the new archived tasks after rotation"
    - "D-07: CLI explicitly reports when rotation occurred"
  artifacts:
    - path: "crates/todotxt-cli/src/commands/archive.rs"
      provides: "rotation-aware CLI archive flow"
      contains: "Archived"
    - path: "crates/todotxt-cli/tests/enrich_bulk_tests.rs"
      provides: "CLI rotation coverage"
      contains: "done-2026-05.txt"
  key_links:
    - from: "CLI archive command"
      to: "shared rotation helper"
      via: "pre-append rotation decision"
      pattern: "rotate old done.txt, then append"
---

<objective>
Integrate the shared archive-rotation contract into the CLI archive command so monthly
rotation happens automatically during archive writes and emits clear user-facing messaging.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-archive-hygiene/49-CONTEXT.md
@.planning/phases/49-archive-hygiene/49-RESEARCH.md
@.planning/phases/49-archive-hygiene/49-01-PLAN.md

<interfaces>
Current CLI archive entry point:

```rust
pub fn run_archive(...)
```

Current behavior appends completed tasks directly into `done.txt` and rewrites `todo.txt`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI archive rotation tests</name>
  <files>crates/todotxt-cli/tests/enrich_bulk_tests.rs</files>
  <behavior>
    Add coverage for:
    - same-period archive writes append to active `done.txt` without rotation.
    - cross-period archive writes move the existing `done.txt` contents into a deterministic
      period-named archive file and start a fresh active `done.txt`.
    - CLI output mentions that rotation occurred.
  </behavior>
  <action>
    Keep tests explicit about file names and resulting task placement so future cadence work
    can extend coverage without rewriting the archive contract.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli archive</automated>
  </verify>
  <done>CLI archive rotation behavior is specified in executable tests before wiring it in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rotation-aware CLI archive flow</name>
  <files>crates/todotxt-cli/src/commands/archive.rs</files>
  <behavior>
    Update `run_archive` so it consults the shared rotation helper before appending completed
    tasks, rotates `done.txt` when needed, and then writes the new active archive contents.
  </behavior>
  <action>
    Preserve existing atomic-write and error-boundary behavior. Keep rotation conditional on
    completed tasks actually being archived during this command invocation.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli archive</automated>
  </verify>
  <done>CLI archive writes gain monthly rotation without changing non-rotation archive behavior.</done>
</task>

</tasks>

<verification>
```powershell
cargo test -p todotxt-cli archive
```
</verification>

<success_criteria>
- DONE-01 is satisfied in the CLI archive path.
- DONE-02 preserves prior done entries in a deterministic rotated file before new writes.
- Rotation occurs only during archive writes with completed tasks.
- CLI output makes rotation visible to the user when it happens.
</success_criteria>

<output>
After completion, create `.planning/phases/49-archive-hygiene/49-02-SUMMARY.md`.
</output>
