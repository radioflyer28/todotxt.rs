---
phase: 49-archive-hygiene
plan: 03
type: tdd
wave: 2
depends_on: [49-01]
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements: [DONE-01, DONE-02]
must_haves:
  truths:
    - "D-08: TUI archive confirmation flow remains intact when rotation occurs"
    - "D-09: rotated archives preserve prior done entries and fresh done.txt receives new archived tasks"
    - "D-10: rotation messaging stays explicit in TUI feedback surfaces"
    - "D-11: undo and archive recovery boundaries remain clear after rotation-aware writes"
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "rotation-aware TUI archive flow and tests"
      contains: "archive_tasks"
  key_links:
    - from: "TUI archive action"
      to: "shared rotation helper"
      via: "archive confirmation -> rotate if needed -> append"
      pattern: "same semantics as CLI"
---

<objective>
Integrate the shared archive-rotation contract into the TUI archive workflow so archive
confirmation, undo boundaries, and user messaging remain clear while monthly rotation is
performed automatically during archive writes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-archive-hygiene/49-CONTEXT.md
@.planning/phases/49-archive-hygiene/49-RESEARCH.md
@.planning/phases/49-archive-hygiene/49-01-PLAN.md

<interfaces>
Current TUI archive entry point:

```rust
fn archive_tasks(&mut self) -> color_eyre::Result<usize>
```

Current behavior appends completed tasks directly into `done.txt`, pushes undo state, and
refreshes the active task list after archive confirmation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TUI archive rotation tests</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    Add focused tests for:
    - archive rotation when a prior-period `done.txt` exists.
    - same-period archive append without rotation.
    - user-facing archive feedback when rotation occurred.
    - archive cancel and undo paths still behaving correctly.
  </behavior>
  <action>
    Extend the existing archive test cluster rather than creating a separate harness. Keep
    tests aligned with the CLI contract so both surfaces share the same archive semantics.
  </action>
  <verify>
    <automated>cargo test -p todotxt-tui archive</automated>
  </verify>
  <done>TUI archive rotation behavior is specified before implementation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rotation-aware TUI archive flow</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    Update `archive_tasks` so it rotates `done.txt` when the current archive write crosses
    into a new configured period, then appends new completed tasks into a fresh active
    `done.txt`.
  </behavior>
  <action>
    Preserve existing undo snapshot behavior and archive confirmation boundaries. TUI should
    surface explicit feedback when rotation occurred instead of silently changing archive
    files.
  </action>
  <verify>
    <automated>cargo test -p todotxt-tui archive</automated>
  </verify>
  <done>TUI archive gains the same monthly rotation semantics and messaging as CLI archive.</done>
</task>

</tasks>

<verification>
```powershell
cargo test -p todotxt-tui archive
```
</verification>

<success_criteria>
- DONE-01 and DONE-02 are satisfied in the TUI archive path.
- Rotation semantics match the CLI archive contract.
- Archive cancel, undo, and user feedback flows remain understandable after rotation support.
- TUI users are explicitly told when rotation happened.
</success_criteria>

<output>
After completion, create `.planning/phases/49-archive-hygiene/49-03-SUMMARY.md`.
</output>
