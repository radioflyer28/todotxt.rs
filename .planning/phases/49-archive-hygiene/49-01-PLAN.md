---
phase: 49-archive-hygiene
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-core/src/archive.rs
  - crates/todotxt-cli/src/config.rs
  - crates/todotxt-tui/src/config.rs
autonomous: true
requirements: [DONE-01, DONE-03]
must_haves:
  truths:
    - "D-01: archive rotation is time-based, with monthly cadence shipping first"
    - "D-02: rotated files use deterministic period names such as done-2026-05.txt"
    - "D-03: retention cleanup is out of scope for this phase"
    - "D-04: rotation occurs only when archive writes completed tasks"
  artifacts:
    - path: "crates/todotxt-core/src/archive.rs"
      provides: "shared archive period and rotation helper"
      contains: "done-YYYY-MM"
    - path: "crates/todotxt-cli/src/config.rs"
      provides: "archive rotation cadence configuration"
      contains: "monthly default"
    - path: "crates/todotxt-tui/src/config.rs"
      provides: "shared config exposure for archive rotation cadence"
      contains: "monthly default"
  key_links:
    - from: "archive write date"
      to: "archive period bucket"
      via: "monthly cadence"
      pattern: "done-2026-05.txt"
---

<objective>
Introduce the shared archive-rotation model, cadence configuration, and deterministic period
naming without yet wiring CLI or TUI archive commands to perform rotation. This plan delivers
the reusable foundation for DONE-01 and DONE-03.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-archive-hygiene/49-CONTEXT.md
@.planning/phases/49-archive-hygiene/49-RESEARCH.md

<interfaces>
Current config surfaces:

```rust
pub struct Config { ... }
pub struct TuiConfig { ... }
```

Current archive writes happen in CLI and TUI-specific code. This plan should extract only the
shared period and rotation decisions needed by those callers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared cadence and naming tests</name>
  <files>crates/todotxt-core/src/archive.rs</files>
  <behavior>
    Add focused tests proving:
    - monthly cadence maps dates into deterministic period buckets.
    - rotated archive names render as `done-YYYY-MM.txt`.
    - future-facing cadence modeling can represent additional time buckets without
      redesigning the API around thresholds.
  </behavior>
  <action>
    Prefer a small shared module or helper type rather than burying cadence logic inside CLI
    or TUI. Keep tests date-explicit and deterministic.
  </action>
  <verify>
    <automated>cargo test -p todotxt-core archive_rotation_period</automated>
  </verify>
  <done>Shared period naming and cadence behavior is locked by tests before integration code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared rotation decision helper</name>
  <files>crates/todotxt-core/src/archive.rs</files>
  <behavior>
    Implement a helper that decides whether an existing `done.txt` must rotate before a new
    archive write, based on the archive write date and configured cadence.
  </behavior>
  <action>
    The helper should support the current monthly policy and expose enough structure for CLI
    and TUI to determine the target rotated filename and whether a fresh active `done.txt`
    must be started.
  </action>
  <verify>
    <automated>cargo test -p todotxt-core archive_rotation_decision</automated>
  </verify>
  <done>CLI and TUI can call one shared decision path instead of duplicating period logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cadence configuration with safe defaults</name>
  <files>crates/todotxt-cli/src/config.rs crates/todotxt-tui/src/config.rs</files>
  <behavior>
    Expose archive rotation cadence in the shared config surfaces with monthly as the default
    effective policy.
  </behavior>
  <action>
    Do not add retention knobs in this phase. The config contract should leave room for future
    cadences such as weekly without reintroducing threshold terminology.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli config</automated>
  </verify>
  <done>Archive rotation is configurable in a future-ready way, but still ships monthly-first.</done>
</task>

</tasks>

<verification>
```powershell
cargo test -p todotxt-core archive_rotation
```
</verification>

<success_criteria>
- DONE-01 has a shared cadence-aware rotation foundation.
- DONE-03 uses cadence terminology and monthly defaults instead of threshold/retention policy.
- Deterministic rotated file naming is implemented in one reusable place.
- No retention deletion behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/49-archive-hygiene/49-01-SUMMARY.md`.
</output>
