---
phase: 50-input-ergonomics
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - Client/Controls/SetDueDateDialog.xaml
  - Client/Controls/SetDueDateDialog.xaml.cs
  - Client/Controls/MainWindow.xaml
  - Client/Controls/MainWindow.xaml.cs
  - Client/MainWindowViewModel.cs
  - ToDoTests/DateDialogErgonomicsTests.cs
autonomous: true
requirements: [DATE-UX-01, DATE-UX-02]
must_haves:
  truths:
    - "D-01: separate date commands remain intact"
    - "D-02: shared date dialog is reused underneath those commands"
    - "D-04: only real date-bearing fields are in scope"
    - "D-07: Left/Right always jump by 7 days in the date picker"
    - "D-09: existing confirm/cancel semantics remain intact"
  artifacts:
    - path: "Client/MainWindowViewModel.cs"
      provides: "shared date-dialog routing for multiple explicit date commands"
      contains: "ShowDateDialog"
    - path: "Client/Controls/SetDueDateDialog.xaml.cs"
      provides: "picker keyboard ergonomics"
      contains: "Left/Right week jump"
  key_links:
    - from: "separate routed commands"
      to: "shared date dialog"
      via: "field-specific dialog launch helpers"
      pattern: "due / threshold / completed"
---

<objective>
Preserve the current separate-command date-editing model while broadening the shared date
dialog to cover real date-bearing fields more consistently and making calendar movement
faster with week-jump keyboard behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-input-ergonomics/50-CONTEXT.md
@.planning/phases/50-input-ergonomics/50-RESEARCH.md

<interfaces>
Current shared date-dialog hooks:

```csharp
private DateTime? ShowSetDueDateDialog()
private DateTime? ShowSetThresholdDateDialog()
private DateTime? ShowDateDialog(Regex rgx)
```

Current date dialog keyboard handler:

```csharp
private void DueDatePicker_Keydown(object sender, KeyEventArgs e)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize the shared date dialog contract</name>
  <files>Client/Controls/SetDueDateDialog.xaml Client/Controls/SetDueDateDialog.xaml.cs Client/MainWindowViewModel.cs</files>
  <behavior>
    Make the shared date dialog target-aware so it can clearly communicate which date field
    is being edited without changing the user's separate command entry points.
  </behavior>
  <action>
    Prefer adding a lightweight target/label contract to the dialog rather than forking new
    dialog classes for each date field.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
  </verify>
  <done>The shared dialog no longer behaves as if every caller is editing due dates.</done>
</task>

<task type="auto">
  <name>Task 2: Expand explicit date commands to additional real date-bearing fields</name>
  <files>Client/MainWindowViewModel.cs Client/Controls/MainWindow.xaml Client/Controls/MainWindow.xaml.cs</files>
  <behavior>
    Keep separate explicit commands, but broaden the date-dialog workflow beyond the current
    due/threshold pair so additional real date-bearing fields such as completed date can use
    the same picker.
  </behavior>
  <action>
    Do not introduce recurrence-rule editing through this path. If new commands or handlers
    are needed, keep them aligned with the existing routed-command pattern.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
    <manual>Confirm each explicit date action opens the shared dialog with the correct active target shown.</manual>
  </verify>
  <done>Date-target expansion stays honest about dates while preserving command muscle memory.</done>
</task>

<task type="auto">
  <name>Task 3: Add week-jump navigation and focused verification coverage</name>
  <files>Client/Controls/SetDueDateDialog.xaml.cs ToDoTests/DateDialogErgonomicsTests.cs</files>
  <behavior>
    Add `Left`/`Right` week-jump behavior to the picker and cover the extractable date math
    and dialog behavior with focused tests where feasible.
  </behavior>
  <action>
    If some WPF event behavior is impractical to automate directly, keep the automated part
    focused and document the manual verification checklist explicitly.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
    <manual>Verify `Left` subtracts 7 days, `Right` adds 7 days, and Enter/Escape behavior is unchanged.</manual>
  </verify>
  <done>Date picker week-jump behavior is shipped without regressing confirmation or cancel flow.</done>
</task>

</tasks>

<verification>
```powershell
msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86
```
</verification>

<success_criteria>
- DATE-UX-01 is satisfied through separate explicit date commands that reuse one improved dialog.
- The dialog clearly communicates the active date target for each command.
- Real date-bearing fields expand without introducing recurrence-rule editing.
- DATE-UX-02 is satisfied with `Left`/`Right` week jumps and preserved confirm/cancel behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/50-input-ergonomics/50-01-SUMMARY.md`.
</output>
