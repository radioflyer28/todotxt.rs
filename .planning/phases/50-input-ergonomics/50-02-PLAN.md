---
phase: 50-input-ergonomics
plan: 02
type: implementation
wave: 1
depends_on: []
files_modified:
  - Client/Controls/IntellisenseTextBox.cs
  - ToDoTests/IntellisenseTextBoxAutoSelectTests.cs
autonomous: true
requirements: [AUTO-SEL-01, AUTO-SEL-02]
must_haves:
  truths:
    - "D-10: auto-select belongs to generic match-driven dialogs, not to date picking"
    - "D-11: current token/value is preferred when present"
    - "D-12: otherwise choose the best current match"
    - "D-13: first suggestion is the final fallback"
    - "D-14: continuity is more important than arbitrary reset"
  artifacts:
    - path: "Client/Controls/IntellisenseTextBox.cs"
      provides: "shared suggestion popup selection behavior"
      contains: "ShowIntellisensePopup"
  key_links:
    - from: "popup open and filter narrowing"
      to: "selection continuity"
      via: "current token / best match / first suggestion"
      pattern: "+ / @ suggestion popup"
---

<objective>
Refine the generic project/context suggestion popup so it opens with a useful selection and
maintains continuity as the user types, instead of requiring unnecessary manual navigation.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-input-ergonomics/50-CONTEXT.md
@.planning/phases/50-input-ergonomics/50-RESEARCH.md

<interfaces>
Current popup entry points:

```csharp
public void ShowIntellisensePopup(IEnumerable<string> s, Rect placement)
private void InsertIntellisenseText()
protected override void OnPreviewKeyUp(KeyEventArgs e)
public void CheckKeyAndShowPopup()
```

Current behavior clears selection on popup open:

```csharp
this.IntellisenseList.SelectedItem = null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add continuity-first popup selection behavior</name>
  <files>Client/Controls/IntellisenseTextBox.cs</files>
  <behavior>
    When the popup opens for project/context suggestions, preselect the most relevant item
    using the required priority order: current token, otherwise best match, otherwise first
    suggestion.
  </behavior>
  <action>
    Keep priority-popup behavior stable unless the same shared logic improves it naturally.
    The main requirement target is project/context token selection.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
  </verify>
  <done>The popup no longer opens in an arbitrary unselected state for the common token flows.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve sensible selection while narrowing</name>
  <files>Client/Controls/IntellisenseTextBox.cs</files>
  <behavior>
    As filtering narrows suggestions, keep selection aligned with the continuity rule instead
    of forcing the user back to manual list navigation after each keystroke.
  </behavior>
  <action>
    The result should feel predictable rather than magical: preserve the current best item
    where possible, and fall back deterministically when it disappears.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
    <manual>Verify popup behavior in task entry, append-text, and filter-style text flows that reuse the control.</manual>
  </verify>
  <done>Narrowing keeps the user on the most useful suggestion instead of resetting them.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused verification coverage for auto-select rules</name>
  <files>ToDoTests/IntellisenseTextBoxAutoSelectTests.cs</files>
  <behavior>
    Add targeted automated coverage for any extractable matching and selection logic, and
    document any remaining manual verification points needed for WPF focus behavior.
  </behavior>
  <action>
    Favor testing selection-choice logic directly instead of overfitting to brittle popup UI
    mechanics if those mechanics are difficult to automate in the current test stack.
  </action>
  <verify>
    <automated>msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86</automated>
  </verify>
  <done>The continuity-first auto-select contract is explicit and regression-resistant.</done>
</task>

</tasks>

<verification>
```powershell
msbuild ToDo.Net.sln /p:Configuration=Debug /p:Platform=x86
```
</verification>

<success_criteria>
- AUTO-SEL-01 is satisfied with useful default selection in project/context match flows.
- AUTO-SEL-02 is satisfied by preferring the current token and preserving continuity while filtering.
- The shared suggestion control remains reusable across its existing consumers.
- Remaining WPF focus nuances are documented if they cannot be fully automated.
</success_criteria>

<output>
After completion, create `.planning/phases/50-input-ergonomics/50-02-SUMMARY.md`.
</output>
