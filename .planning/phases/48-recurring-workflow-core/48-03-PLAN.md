---
phase: 48-recurring-workflow-core
plan: 03
type: tdd
wave: 2
depends_on: [48-01]
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements: [REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "D-04: recurring completion auto-creates the next occurrence with no prompt"
    - "D-05: TUI single-task and bulk completion apply the same auto-create rule"
    - "D-08: each completion event creates exactly one next occurrence per recurring task"
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "TUI recurring completion integration"
      contains: "pane_toggle_done and bulk_mark_done"
  key_links:
    - from: "TUI completion paths"
      to: "Task::next_recurring_occurrence"
      via: "original task before with_completed(true)"
      pattern: "complete old task, append next occurrence, rebuild panes"
---

<objective>
Route TUI single-task and bulk mark-done flows through the shared recurrence helper so
TUI behavior matches CLI behavior. This plan delivers the TUI side of REC-02, REC-03, and
REC-04.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-recurring-workflow-core/48-CONTEXT.md
@.planning/phases/48-recurring-workflow-core/48-RESEARCH.md
@.planning/phases/48-recurring-workflow-core/48-01-SUMMARY.md

<interfaces>
Current TUI completion paths:

```rust
fn toggle_done(&mut self)
fn pane_toggle_done(&mut self)
fn bulk_mark_done(&mut self)
```

Important behavior:

- single-task completion pushes an undo entry before mutation.
- bulk mark-done pushes one undo entry for the whole batch.
- pane rebuilds happen after mutation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TUI single-recurring completion tests</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    Add tests proving `pane_toggle_done` or the active single-task completion path:
    - marks the original recurring task complete.
    - appends exactly one incomplete next occurrence.
    - uses the same strict/relative due-date behavior as core.
    - does not enter a confirmation mode.
  </behavior>
  <action>
    Reuse existing `make_app_with_tasks` helpers and assert against `app.task_list.tasks()`.
    Prefer direct method tests over terminal rendering tests.
  </action>
  <verify>
    <automated>cargo test -p todotxt-tui recurring_tui_single</automated>
  </verify>
  <done>TUI single-task recurrence is covered before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TUI bulk recurring completion tests</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    Add tests proving `bulk_mark_done` creates one next occurrence per selected recurring
    task and does not create duplicates for already-completed tasks.
  </behavior>
  <action>
    Include selected recurring and non-recurring tasks in one batch. Assert selection is
    still cleared, one undo entry is still pushed, and generated occurrences are appended.
  </action>
  <verify>
    <automated>cargo test -p todotxt-tui recurring_tui_bulk</automated>
  </verify>
  <done>TUI bulk recurrence behavior is covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement shared TUI recurring completion helper</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <behavior>
    Update TUI completion paths so newly completed recurring tasks append next occurrences
    automatically with no prompt.
  </behavior>
  <action>
    Prefer a small App-level helper that accepts canonical task indices and applies the
    same mutation rules for pane and bulk completion:
    - compute next occurrence from the original task.
    - complete the original task.
    - append generated next occurrences.
    - persist the batch with one save where possible.
    - rebuild panes and re-anchor selection using existing flows.

    Preserve existing undo semantics: single completion gets one undo snapshot, and bulk
    mark-done gets one undo snapshot for the whole batch.
  </action>
  <verify>
    <automated>cargo test -p todotxt-tui recurring_tui</automated>
  </verify>
  <done>TUI single-task and bulk completion share the same recurrence behavior.</done>
</task>

</tasks>

<verification>
```powershell
cargo test -p todotxt-tui recurring_tui
cargo test -p todotxt-tui bulk_mark_done
```
</verification>

<success_criteria>
- TUI recurring completion creates next occurrences automatically.
- TUI bulk mark-done creates exactly one next occurrence per newly completed recurring task.
- Already-completed tasks do not generate next occurrences.
- Existing undo, selection clearing, and pane rebuild behavior remain intact.
- TUI behavior matches CLI recurrence semantics from the shared core helper.
</success_criteria>

<output>
After completion, create `.planning/phases/48-recurring-workflow-core/48-03-SUMMARY.md`.
</output>
