---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-cli/src/config.rs
  - crates/todotxt-cli/src/cli.rs
  - crates/todotxt-cli/src/main.rs
  - crates/todotxt-cli/src/commands/mod.rs
autonomous: true
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03
  - ENRICH-04
  - BULK-01
  - BULK-02
must_haves:
  truths:
    - All 6 new enrichment/bulk commands are wired into the CLI dispatcher
    - Config supports `done_file` field for custom done.txt path resolution
  artifacts:
    - path: crates/todotxt-cli/src/config.rs
      provides: done_file config field
      min_lines: 5
    - path: crates/todotxt-cli/src/cli.rs
      provides: Pri/Depri/Due/Postpone/Archive/DelDone command enum variants
      min_lines: 6
    - path: crates/todotxt-cli/src/commands/mod.rs
      provides: pub mod declarations for 6 new command modules
      min_lines: 6
  key_links:
    - from: crates/todotxt-cli/src/config.rs
      to: crates/todotxt-cli/src/main.rs
      via: Config struct usage
      pattern: done_file.*Option.*PathBuf
    - from: crates/todotxt-cli/src/cli.rs
      to: crates/todotxt-cli/src/main.rs
      via: Commands enum dispatch
      pattern: Commands::Pri\|Commands::Depri\|Commands::Due

---

<objective>
Extend CLI infrastructure to support 6 new enrichment and bulk operation commands. Prepare Config, CLI enum, and command module declarations for all downstream implementations.

Purpose: Foundation for all Phase 5 commands (pri/depri/due/postpone/archive/del-done)
Output: Wired CLI, config extension, compilable command stubs
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-task-enrichment-bulk-operations/05-CONTEXT.md
@crates/todotxt-cli/src/cli.rs
@crates/todotxt-cli/src/config.rs
@crates/todotxt-cli/src/commands/complete.rs (reference for multi-ID pattern)
</context>

<tasks>

<task type="auto">
  <name>Extend Config with done_file field</name>
  <files>crates/todotxt-cli/src/config.rs</files>
  <read_first>
    - crates/todotxt-cli/src/config.rs (current Config struct)
  </read_first>
  <action>
Add `done_file: Option<PathBuf>` field to Config struct with `#[serde(default)]` attribute. Initialize in all 3 Config literal constructors to `done_file: None`. Derive logic: if None, resolve to `todo_file.parent().join("done.txt")`; if Some, use provided path.

Pattern from Phase 4: Config has `auto_creation_date: bool` with `#[serde(default)]`.
Exact location: After `auto_creation_date` field in Config struct definition.
  </action>
  <verify>
    - grep -n "done_file.*Option.*PathBuf" crates/todotxt-cli/src/config.rs
    - grep -c "done_file: None" crates/todotxt-cli/src/config.rs (expect 3)
    - cargo build -p todotxt-cli 2>&1 | grep -c "error" (expect 0)
  </verify>
  <done>Config has `done_file: Option<PathBuf>` field initialized in 3 places, no compile errors</done>
</task>

<task type="auto">
  <name>Wire 6 new command enum variants into CLI</name>
  <files>crates/todotxt-cli/src/cli.rs</files>
  <read_first>
    - crates/todotxt-cli/src/cli.rs (Commands enum)
    - crates/todotxt-cli/src/commands/complete.rs (multi-ID reference)
  </read_first>
  <action>
Extend Commands enum with 6 variants (per D-01: multi-ID for all except archive/del-done which take no IDs):

#[command(name = "pri")] Pri { ids: Vec<u32>, priority: char },
#[command(name = "depri")] Depri { ids: Vec<u32> },
#[command(name = "due")] Due { id: u32, date: String },
#[command(name = "postpone")] Postpone { id: u32, days: u32 },
#[command(name = "archive")] Archive,
#[command(name = "del-done")] DelDone,

Use `#[command(name = "...")]` attribute for each (Pri, Depri already work, but may need name attribute if rust keyword).
Pri takes `priority: char`, not String (validate A-Z later).
Due/Postpone take single ID (D-01 applies to pri/depri only, not due/postpone per CONTEXT decision).
Archive/DelDone have no arguments.
  </action>
  <verify>
    - grep -n "command.*name.*pri\|command.*name.*due\|command.*name.*archive" crates/todotxt-cli/src/cli.rs | wc -l (expect ≥ 3)
    - cargo build -p todotxt-cli 2>&1 | grep -c "error" (expect 0)
  </verify>
  <done>CLI enum has all 6 new variants, no compile errors</done>
</task>

<task type="auto">
  <name>Create command module stubs and wire dispatch</name>
  <files>
    - crates/todotxt-cli/src/commands/mod.rs
    - crates/todotxt-cli/src/commands/priority.rs
    - crates/todotxt-cli/src/commands/due.rs
    - crates/todotxt-cli/src/commands/archive.rs
    - crates/todotxt-cli/src/commands/del_done.rs
    - crates/todotxt-cli/src/main.rs
  </files>
  <read_first>
    - crates/todotxt-cli/src/commands/mod.rs (existing modules)
    - crates/todotxt-cli/src/main.rs (dispatch pattern from Phase 4)
  </read_first>
  <action>
1. Add to commands/mod.rs:
   pub mod priority;
   pub mod due;
   pub mod archive;
   pub mod del_done;

2. Create stub files (one empty run function each):
   - priority.rs: pub fn run_pri(...) { todo!() } and pub fn run_depri(...) { todo!() }
   - due.rs: pub fn run_due(...) { todo!() } and pub fn run_postpone(...) { todo!() }
   - archive.rs: pub fn run_archive(...) { todo!() }
   - del_done.rs: pub fn run_del_done(...) { todo!() }

3. Wire dispatch in main.rs match &cli.command block:
   Commands::Pri { ids, priority } => commands::priority::run_pri(...),
   Commands::Depri { ids } => commands::priority::run_depri(...),
   Commands::Due { id, date } => commands::due::run_due(...),
   Commands::Postpone { id, days } => commands::due::run_postpone(...),
   Commands::Archive => commands::archive::run_archive(...),
   Commands::DelDone => commands::del_done::run_del_done(...),

Each dispatch arm receives: &mut list, &cfg, &renderer (from Phase 4 pattern in complete/del commands).
  </action>
  <verify>
    - grep -c "pub mod priority\|pub mod due\|pub mod archive\|pub mod del_done" crates/todotxt-cli/src/commands/mod.rs (expect 4)
    - test -f crates/todotxt-cli/src/commands/priority.rs && test -f crates/todotxt-cli/src/commands/due.rs && test -f crates/todotxt-cli/src/commands/archive.rs && test -f crates/todotxt-cli/src/commands/del_done.rs (expect all true)
    - cargo build -p todotxt-cli 2>&1 | grep -c "error" (expect 0)
  </verify>
  <done>All 4 command modules created and wired into dispatcher, no compile errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (cli args) | Priority char, date string, ID list from untrusted user |
| Task list file | Date strings parsed from task tags may have unexpected values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Injection | Priority parser | mitigate | Validate char is A-Z only, reject others with exit 2 (validation error) |
| T-05-02 | Input Validation | Date parser | mitigate | Whitelist allowed date formats (today/tomorrow/weekdays/YYYY-MM-DD), reject others with exit 2 |
| T-05-03 | Data Loss | Archive atomicity | mitigate | Both todo.txt and done.txt written via temp-file + rename before either is committed |

</threat_model>

<verification>
- `cargo test --workspace` passes (all existing tests + stubs compile)
- `cargo clippy --workspace -- -D warnings` produces zero warnings
- New command enum variants recognize pri/depri/due/postpone/archive/del-done
- Main dispatch routes to correct stub function (checked by CLI invocation with --help)
</verification>

<success_criteria>
- Config has done_file: Option<PathBuf> field and initializes to None in 3 places
- CLI accepts all 6 new commands (todotxt pri/depri/due/postpone/archive/del-done work without errors)
- Dispatch routes each command to its module stub
- No compile errors or clippy warnings
</success_criteria>

<output>
Create `.planning/phases/05-task-enrichment-bulk-operations/05-01-SUMMARY.md` after execution
</output>
