---
phase: 08-retroactive-cli-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md
autonomous: true
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03
  - ENRICH-04
  - BULK-01
  - BULK-02
must_haves:
  truths:
    - "05-VERIFICATION.md exists with status: passed"
    - "ENRICH-01..04 and BULK-01..02 appear in requirements-verified list"
    - "Each requirement row maps to its implementation file and evidence"
    - "cargo test -p todotxt-cli passes with zero failures at time of writing"
  artifacts:
    - path: ".planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md"
      provides: "Retroactive Phase 05 verification record for ENRICH-01..04, BULK-01..02"
      contains: "requirements-verified: ENRICH-01..ENRICH-04, BULK-01..BULK-02"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "crates/todotxt-cli/src/commands/"
      via: "ENRICH-01..04, BULK-01..02 traceability rows"
    - from: "05-VERIFICATION.md"
      to: "crates/todotxt-cli/tests/enrich_bulk_tests.rs"
      via: "integration test evidence citation"
---

<objective>
Produce the missing Phase 05 VERIFICATION.md documenting retroactive verification of the task
enrichment commands (ENRICH-01..04: pri/depri/due/postpone) and bulk operations (BULK-01..02:
archive/del-done). Phase 05 delivered all 6 commands and a comprehensive integration test file
(`enrich_bulk_tests.rs`) using the assert_cmd/assert_fs/TestFixture pattern. No VERIFICATION.md
was created at phase completion.

Purpose: ENRICH-01..04 and BULK-01..02 are marked `[x]` in REQUIREMENTS.md but appear as
unverified in the milestone audit because no VERIFICATION.md references them.

Output: `.planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md` with
`status: passed`, `requirements-verified: [ENRICH-01..ENRICH-04, BULK-01..BULK-02]`, and a full
traceability table.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-task-enrichment-bulk-operations/05-01-SUMMARY.md
@.planning/phases/05-task-enrichment-bulk-operations/05-02-SUMMARY.md
@.planning/phases/05-task-enrichment-bulk-operations/05-03-SUMMARY.md
@.planning/phases/05-task-enrichment-bulk-operations/05-04-SUMMARY.md
@.planning/phases/05-task-enrichment-bulk-operations/05-05-SUMMARY.md
@.planning/phases/05-task-enrichment-bulk-operations/05-06-SUMMARY.md

<interfaces>
<!-- Phase 05 requirement→plan mapping (from SUMMARY frontmatter): -->
<!-- ENRICH-01 (pri)        → 05-01 (wiring stubs), 05-02 or 05-03 (pri impl), 05-06 (integration tests) -->
<!-- ENRICH-02 (depri)      → 05-01 (wiring stubs), 05-02 or 05-03 (depri impl), 05-06 (integration tests) -->
<!-- ENRICH-03 (due)        → 05-01 (wiring stubs), 05-04 (due impl), 05-06 (integration tests) -->
<!-- ENRICH-04 (postpone)   → 05-01 (wiring stubs), 05-04 (postpone impl), 05-06 (ISO date literals in tests) -->
<!-- BULK-01 (archive)      → 05-01 (wiring stubs), 05-05 (bulk-filter-write pattern), 05-06 (integration tests) -->
<!-- BULK-02 (del-done)     → 05-01 (wiring stubs), 05-05 (bulk-filter-write pattern), 05-06 (integration tests) -->
<!-- Implementation files:  crates/todotxt-cli/src/commands/ (pri.rs, depri.rs, due.rs, postpone.rs, archive.rs, del_done.rs) -->
<!-- Integration tests:     crates/todotxt-cli/tests/enrich_bulk_tests.rs (plan 05-06) -->
<!-- assert_cmd/assert_fs/TestFixture pattern used throughout Phase 05 -->
<!-- ISO date literals used for postpone tests -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather Phase 05 test evidence</name>
  <files>N/A — evidence gathering only</files>
  <action>
Run the following commands and capture output for use in the VERIFICATION.md:

1. `cargo test -p todotxt-cli 2>&1 | Select-String "test result:|running [0-9]+ test"` — record all suites
2. `cargo clippy -p todotxt-cli -- -D warnings 2>&1 | Select-Object -Last 3` — confirm clean
3. `Get-ChildItem crates/todotxt-cli/src/commands -Filter "*.rs" | Select-Object Name` — confirm command files
4. `Select-String -Path "crates/todotxt-cli/tests/enrich_bulk_tests.rs" -Pattern "fn test_|#\[test\]" | Measure-Object | Select-Object Count` — count enrich/bulk tests
5. `Get-Content ".planning/phases/05-task-enrichment-bulk-operations/05-01-SUMMARY.md" -ErrorAction SilentlyContinue | Select-Object -First 50` — extract what was built (CLI enum additions)
6. `Get-Content ".planning/phases/05-task-enrichment-bulk-operations/05-06-SUMMARY.md" -ErrorAction SilentlyContinue | Select-Object -First 50` — integration test details

Use this evidence to write accurate entries in Task 2.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli 2>&1 | Select-String "FAILED" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>Output of `cargo test -p todotxt-cli` shows 0 failures. Command files for pri/depri/due/postpone/archive/del-done confirmed to exist in `crates/todotxt-cli/src/commands/`. Test count for `enrich_bulk_tests.rs` captured.</done>
</task>

<task type="auto">
  <name>Task 2: Write 05-VERIFICATION.md</name>
  <files>.planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md</files>
  <action>
Create `.planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md` with this
structure (fill in actual test counts from Task 1):

```markdown
---
phase: 05-task-enrichment-bulk-operations
status: passed
requirements-verified:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03
  - ENRICH-04
  - BULK-01
  - BULK-02
test-count: {total from cargo test -p todotxt-cli}
tests-passed: {same}
tests-failed: 0
must-haves-met: true
verification-date: 2026-04-15
retroactive: true
retroactive-reason: "Verification artifact was not created during original Phase 05 execution (April 2026). All enrichment and bulk commands and tests were passing at phase completion. This document is a retroactive record created during Phase 08 gap closure."
---

## Phase 05 Verification: Task Enrichment & Bulk Operations

**Verification Date:** 2026-04-15
**Phase Goal:** Implement 4 task enrichment commands (pri/depri/due/postpone) and 2 bulk
operations (archive/del-done) with full integration test coverage.

**Verification Status:** ✅ **PASSED** (Retroactive — see `retroactive-reason` in frontmatter)

---

## Retroactive Verification Note

This verification document was created during Phase 08 (Retroactive CLI Verification) because
Phase 05 execution did not produce a VERIFICATION.md artifact. The enrichment and bulk commands
have been working since Phase 05 completion and continued passing through Phase 06. All evidence
was gathered by re-running the test suite against the current codebase on 2026-04-15.

---

## Must-Haves Verification

### Plan 05-01: CLI Command Wiring (Stubs)

**Must-have:** All 6 new sub-commands appear in the CLI `Commands` enum with correct signatures
✅ **VERIFIED** — `crates/todotxt-cli/src/main.rs` (or equivalent entrypoint) was updated in
05-01 to add `pri`, `depri`, `due`, `postpone`, `archive`, `del-done` (or `del_done`) variants.
Source confirmed via `crates/todotxt-cli/src/commands/` directory listing.

### Plans 05-02 / 05-03: `pri` and `depri` Commands

**Must-have 1:** `todotxt pri <id> <priority>` sets priority A–Z on a task
✅ **VERIFIED** — `crates/todotxt-cli/src/commands/pri.rs` implements priority assignment.
Integration test coverage in `enrich_bulk_tests.rs` confirms priority is set correctly.

**Must-have 2:** `todotxt depri <id>` removes priority from a task
✅ **VERIFIED** — `crates/todotxt-cli/src/commands/depri.rs` implements priority removal.
Integration tests confirm priority field is removed.

### Plan 05-04: `due` and `postpone` Commands

**Must-have 1:** `todotxt due <id> <date>` sets due: date tag (ISO 8601)
✅ **VERIFIED** — `crates/todotxt-cli/src/commands/due.rs` sets `due:YYYY-MM-DD` tag.
Integration tests use ISO date literals (e.g., `2026-01-15`) per plan 05-06 SUMMARY.

**Must-have 2:** `todotxt postpone <id> <N>` advances due date by N days
✅ **VERIFIED** — Postpone command advances `due:` date. Integration tests use date arithmetic
with known ISO date literals to verify correct offset.

### Plan 05-05: `archive` and `del-done` Bulk Commands

**Must-have 1:** `todotxt archive` moves all completed tasks from todo.txt to done.txt
✅ **VERIFIED** — Archive command uses bulk-filter-write pattern (implemented in 05-05).
Completed tasks (prefixed with `x `) are moved to `done.txt`, leaving only incomplete tasks in `todo.txt`.

**Must-have 2:** `todotxt del-done` (or `del_done`) permanently deletes all completed tasks
✅ **VERIFIED** — del-done command removes completed tasks from todo.txt without writing to
done.txt. Integration tests in `enrich_bulk_tests.rs` confirm behavior.

### Plan 05-06: Integration Tests for Enrichment & Bulk Commands

**Must-have 1:** `enrich_bulk_tests.rs` uses assert_cmd/assert_fs/TestFixture pattern
✅ **VERIFIED** — `crates/todotxt-cli/tests/enrich_bulk_tests.rs` created with TestFixture
helper pattern. Uses `assert_cmd` for CLI invocation and `assert_fs` for file system assertions.

**Must-have 2:** Postpone tests use ISO date literals (not relative dates)
✅ **VERIFIED** — SUMMARY for 05-06 explicitly notes ISO date literals used for postpone tests,
ensuring deterministic test outcomes.

**Must-have 3:** `cargo test -p todotxt-cli` — all suites pass
✅ **VERIFIED** — {test count} tests across all CLI test files, 0 failures.

---

## Requirement Traceability

| Req ID | Description | Implementation File | Plans | Status |
|--------|-------------|--------------------|----- |--------|
| ENRICH-01 | `pri <id> <priority>` — set priority A–Z on a task | `crates/todotxt-cli/src/commands/pri.rs` | 05-01, 05-02 or 05-03, 05-06 | ✅ Implemented |
| ENRICH-02 | `depri <id>` — remove priority from a task | `crates/todotxt-cli/src/commands/depri.rs` | 05-01, 05-02 or 05-03, 05-06 | ✅ Implemented |
| ENRICH-03 | `due <id> <date>` — set due: date tag (ISO 8601) | `crates/todotxt-cli/src/commands/due.rs` | 05-01, 05-04, 05-06 | ✅ Implemented |
| ENRICH-04 | `postpone <id> <N>` — advance due date by N days | `crates/todotxt-cli/src/commands/postpone.rs` | 05-01, 05-04, 05-06 | ✅ Implemented |
| BULK-01 | `archive` — move completed tasks to done.txt | `crates/todotxt-cli/src/commands/archive.rs` | 05-01, 05-05, 05-06 | ✅ Implemented |
| BULK-02 | `del-done` — delete all completed tasks permanently | `crates/todotxt-cli/src/commands/del_done.rs` (or `del-done.rs`) | 05-01, 05-05, 05-06 | ✅ Implemented |

*Note: Exact command file names may vary (snake_case vs kebab-case). Verify with
`Get-ChildItem crates/todotxt-cli/src/commands` and update the table accordingly.*

---

## Code Quality

- **Test Coverage:** {total} tests in `todotxt-cli` package. All passed.
- **Integration Tests:** `enrich_bulk_tests.rs` covers all 6 enrichment/bulk commands using
  assert_cmd/assert_fs/TestFixture pattern with ISO date literals for date-sensitive tests.
- **Clippy:** 0 warnings (`cargo clippy -p todotxt-cli -- -D warnings` clean).

---

## Sign-Off

Phase 05 (Task Enrichment & Bulk Operations) successfully delivered 4 enrichment commands and
2 bulk operation commands with full integration test coverage. All requirements ENRICH-01 through
ENRICH-04 and BULK-01 through BULK-02 are implemented and covered by passing tests.

**Status:** ✅ READY FOR PRODUCTION

This retroactive verification was created during Phase 08 gap closure to satisfy the v1.0 milestone audit.
```

Substitute `{total}` and `{test count}` with actual values from `cargo test -p todotxt-cli` output.
Verify the exact file names in `crates/todotxt-cli/src/commands/` and update the traceability table rows accordingly (especially BULK-02 — check if file is `del_done.rs` or `del-done.rs`).
  </action>
  <verify>
    <automated>Select-String -Path ".planning/phases/05-task-enrichment-bulk-operations/05-VERIFICATION.md" -Pattern "status: passed|ENRICH-01|BULK-02|retroactive: true" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
File exists with `status: passed`, `requirements-verified` containing ENRICH-01..04 and BULK-01..02,
traceability table with all 6 rows, and `retroactive: true` in frontmatter.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only phase. No code written, no user input processed, no network calls made.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | 05-VERIFICATION.md | accept | Documentation file, no secrets or PII, low-value target. |
| T-08-02-02 | Tampering | 05-VERIFICATION.md | accept | Developer-controlled planning artifact tracked in git history. |
</threat_model>

<verification>
1. `05-VERIFICATION.md` exists in `.planning/phases/05-task-enrichment-bulk-operations/`
2. Frontmatter: `status: passed`
3. `requirements-verified` contains ENRICH-01, ENRICH-02, ENRICH-03, ENRICH-04, BULK-01, BULK-02
4. Traceability table has 6 rows with correct implementation file references
5. `retroactive: true` flag present
6. `cargo test -p todotxt-cli` evidence cited with accurate test counts
</verification>

<success_criteria>
- `05-VERIFICATION.md` exists with all 6 requirements (ENRICH-01..04, BULK-01..02) in `requirements-verified`
- Traceability table maps each requirement to its command file in `crates/todotxt-cli/src/commands/`
- Retroactive nature of the artifact is clearly documented
- File names in traceability table match actual files on disk
</success_criteria>

<output>
After completion, create `.planning/phases/08-retroactive-cli-verification/08-02-SUMMARY.md`
with standard GSD summary format documenting that 05-VERIFICATION.md was created and
ENRICH-01..04 + BULK-01..02 gap closure objective was achieved.
</output>
