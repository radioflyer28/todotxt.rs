---
phase: 03-cli-foundation-config-output-read-commands
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - crates/todotxt-core/src/task.rs
  - crates/todotxt-core/src/task_list.rs
  - crates/todotxt-core/tests/task_tests.rs
  - crates/todotxt-core/tests/task_list_tests.rs
autonomous: true
gap_closure: true
requirements: [READ-01, READ-06, READ-07]
must_haves:
  truths:
    - "Task raw text used by CLI and JSON never contains trailing carriage-return characters from CRLF input"
    - "Loading todo.txt files with mixed LF/CRLF line endings produces normalized task raw lines"
    - "Core regression tests fail if CR leakage or mixed-line-ending parsing regresses"
  artifacts:
    - path: "crates/todotxt-core/src/task.rs"
      provides: "Canonical Task.raw normalization for parsed lines"
    - path: "crates/todotxt-core/src/task_list.rs"
      provides: "Line splitting that handles mixed LF/CRLF rows"
    - path: "crates/todotxt-core/tests/task_tests.rs"
      provides: "Unit regression tests for CR trimming in raw representation"
    - path: "crates/todotxt-core/tests/task_list_tests.rs"
      provides: "Integration regression tests for mixed line ending load/save behavior"
  key_links:
    - from: "crates/todotxt-core/src/task_list.rs"
      to: "crates/todotxt-core/src/task.rs"
      via: "TaskList::load() line parsing into Task::parse()"
      pattern: "split_lines\(.*\) -> .*Task::parse"
    - from: "crates/todotxt-core/src/task.rs"
      to: "crates/todotxt-cli/src/output.rs"
      via: "Task::to_raw() consumed by renderer and JSON serialization"
      pattern: "to_raw\(\)"
---

<objective>
Close the root-cause gaps around carriage-return leakage and mixed line-ending parsing so downstream CLI rendering and JSON output stay clean.

Purpose: UAT failures show one defect family (CR propagation) affecting table output, no-color output, and JSON payloads. Fixing normalization in core is the stable foundation for all CLI behaviors.
Output: Updated core parsing/line-splitting behavior and regression tests that lock in CR-safe output.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-foundation-config-output-read-commands/03-UAT.md
@.planning/phases/03-cli-foundation-config-output-read-commands/03-03-SUMMARY.md
@.planning/debug/phase-03-list-prints-done-task.md
@.planning/debug/phase03-json-list-trailing-cr.md

<interfaces>
From crates/todotxt-core/src/task.rs:
- pub fn parse(line: &str) -> Self
- pub fn to_raw(&self) -> &str

From crates/todotxt-core/src/task_list.rs:
- pub fn load(path: impl AsRef<Path>) -> Result<Self, TodoError>
- fn split_lines(content: &str, line_ending: LineEnding) -> impl Iterator<Item = &str>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize canonical raw task text for CRLF and mixed-ending rows</name>
  <files>crates/todotxt-core/src/task.rs, crates/todotxt-core/src/task_list.rs</files>
  <behavior>
    - Parsing any input row ending in '\r' stores raw without trailing '\r'.
    - Mixed-ending files (some '\n', some '\r\n') parse each logical line without embedded trailing '\r'.
    - Existing LF-only and CRLF-only round-trip behavior remains unchanged.
  </behavior>
  <action>
Implement root-cause fixes from 03-UAT gaps:
- In crates/todotxt-core/src/task.rs, make Task::parse canonicalize raw from the CR-normalized view (not original untrimmed line) so Task::to_raw() never returns trailing '\r'.
- In crates/todotxt-core/src/task_list.rs, update split_lines behavior to tolerate mixed line endings per-row instead of relying on a single global separator. Keep file-level line_ending detection for save semantics, but normalize per-row parse input.
- Preserve existing non-gap behavior: BOM stripping, atomic save flow, and line-ending preservation choice for save.
  </action>
  <verify>
    <automated>cargo test -p todotxt-core</automated>
  </verify>
  <done>Core parsing returns CR-free canonical raw text for all loaded rows, including mixed-ending input, without regressing existing load/save semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for CR normalization and mixed line endings</name>
  <files>crates/todotxt-core/tests/task_tests.rs, crates/todotxt-core/tests/task_list_tests.rs</files>
  <behavior>
    - Unit test proves Task::parse("...\r").to_raw() strips trailing CR.
    - TaskList load test with mixed LF/CRLF input proves no parsed task raw contains trailing CR.
    - Save/round-trip assertions protect current CRLF/LF preservation contract.
  </behavior>
  <action>
Add focused regressions that fail on the pre-fix behavior and pass on corrected behavior:
- Extend crates/todotxt-core/tests/task_tests.rs with parse + display expectations for trailing CR input.
- Extend crates/todotxt-core/tests/task_list_tests.rs with a mixed-line-ending fixture asserting parsed task raws are clean and output is not mangled by embedded '\r'.
- Keep test naming explicit to the bug class so future failures immediately identify CR leakage regressions.
  </action>
  <verify>
    <automated>cargo test -p todotxt-core task_tests task_list_tests</automated>
  </verify>
  <done>Regression tests lock the CR/mixed-ending bug class so future changes cannot reintroduce trailing carriage-return output artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted todo.txt file content -> parser | File bytes are user-controlled and must not corrupt terminal/json output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | T | crates/todotxt-core/src/task.rs | mitigate | Canonicalize parsed raw text to remove control-char carriage returns at parse boundary |
| T-03-04-02 | I | crates/todotxt-core/src/task_list.rs | mitigate | Normalize per-row mixed line endings before constructing Task values used by JSON and CLI output |
| T-03-04-03 | D | crates/todotxt-core/tests | mitigate | Add regressions so malformed line-ending input cannot silently reintroduce mangled output behavior |
</threat_model>

<verification>
- cargo test -p todotxt-core
- cargo clippy -p todotxt-core -- -D warnings
</verification>

<success_criteria>
- UAT gap root causes for CR leakage and mixed-ending handling are closed at core layer.
- Tests explicitly fail if trailing '\r' reappears in Task::to_raw() values.
- No regressions in existing todotxt-core test suite.
</success_criteria>

<output>
After completion, create .planning/phases/03-cli-foundation-config-output-read-commands/03-04-SUMMARY.md
</output>
