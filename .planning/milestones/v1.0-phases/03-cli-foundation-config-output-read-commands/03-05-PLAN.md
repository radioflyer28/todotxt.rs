---
phase: 03-cli-foundation-config-output-read-commands
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - crates/todotxt-cli/src/commands/list.rs
  - crates/todotxt-cli/tests/list_tests.rs
autonomous: true
gap_closure: true
requirements: [READ-01, READ-06, READ-07, CFG-02, READ-08]
must_haves:
  truths:
    - "Default todotxt list output excludes completed tasks unless completion filtering is explicitly requested"
    - "todotxt list :nonexistent still emits unknown preset warning while returning cleanly formatted output"
    - "list human, --no-color, and --json output paths remain correct and covered by regression tests"
  artifacts:
    - path: "crates/todotxt-cli/src/commands/list.rs"
      provides: "Default incomplete-only filtering with explicit DONE/-DONE override support"
    - path: "crates/todotxt-cli/tests/list_tests.rs"
      provides: "Integration regressions for default filtering, unknown preset warning, and clean JSON/no-color output"
  key_links:
    - from: "crates/todotxt-cli/src/commands/list.rs"
      to: "todotxt_core::Filter::from_query"
      via: "build_filter query composition"
      pattern: "Filter::from_query"
    - from: "crates/todotxt-cli/src/commands/list.rs"
      to: "stderr warning output"
      via: "unknown preset handling path"
      pattern: "warning: unknown preset"
---

<objective>
Close the list-behavior UAT gaps by changing default list semantics to incomplete-only while preserving unknown-preset warning behavior and adding CLI-level regression coverage.

Purpose: This addresses the user-visible behavior failures in tests 2, 10, and 12 while keeping D-10 preset warning semantics intact.
Output: Updated list filter composition and integration tests that protect warning + formatting correctness.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-foundation-config-output-read-commands/03-CONTEXT.md
@.planning/phases/03-cli-foundation-config-output-read-commands/03-UAT.md
@.planning/phases/03-cli-foundation-config-output-read-commands/03-04-SUMMARY.md
@.planning/debug/phase-03-no-color-list-output.md
@.planning/debug/phase-03-uat12-nonexistent-indent.md

<interfaces>
From crates/todotxt-cli/src/commands/list.rs:
- fn build_filter(args: &ListArgs, cfg: &Config) -> Filter
- pub fn run(todo_path: &Path, args: &ListArgs, cfg: &Config, renderer: &Renderer) -> Result<(), CliError>

Decision constraints from 03-CONTEXT.md:
- D-08 and D-09: positional + --filter terms are AND-combined.
- D-10 and D-11: :preset lookup and merge behavior; unknown preset warns and is ignored.
- D-12: plain token is never preset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce default incomplete-only list semantics while honoring explicit completion filters</name>
  <files>crates/todotxt-cli/src/commands/list.rs</files>
  <behavior>
    - Default list query (no completion term present) behaves as if -DONE is included.
    - Explicit DONE keeps completed-only behavior; explicit -DONE keeps incomplete-only behavior.
    - Preset composition and unknown-preset warning behavior remain unchanged per D-10/D-11.
  </behavior>
  <action>
Update build_filter in crates/todotxt-cli/src/commands/list.rs:
- Keep existing token merge rules from D-08/D-09/D-11.
- Detect whether effective query already contains explicit completion terms (DONE or -DONE).
- If no explicit completion term exists, append -DONE by default so list excludes completed tasks.
- Preserve the unknown preset warning message and stderr behavior exactly while applying new default filter semantics.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli --test list_tests</automated>
  </verify>
  <done>Default list no longer shows completed tasks, and explicit completion queries plus preset warning behavior still work as designed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration regressions for default exclusion, warning preservation, and clean output/JSON</name>
  <files>crates/todotxt-cli/tests/list_tests.rs</files>
  <behavior>
    - list default output excludes completed rows.
    - list :nonexistent emits warning to stderr and still exits 0 with clean rows.
    - --json list payload has no trailing carriage returns in raw values.
    - --no-color output remains free of ANSI escapes and carriage-return artifacts.
  </behavior>
  <action>
Extend crates/todotxt-cli/tests/list_tests.rs with regression-focused integration tests tied to 03-UAT gaps:
- Add default-filter test asserting completed line is absent unless DONE is explicitly requested.
- Add unknown-preset test asserting stderr contains warning text while stdout stays valid and command succeeds.
- Add JSON-output test that parses stdout and asserts no data.raw field contains '\r'.
- Add/update no-color assertion to ensure neither ANSI escapes nor carriage-return corruption appears.
  </action>
  <verify>
    <automated>cargo test -p todotxt-cli --test list_tests &amp;&amp; cargo test -p todotxt-cli</automated>
  </verify>
  <done>CLI integration coverage prevents regressions in default list filtering, warning behavior, and output correctness across human/no-color/json modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user filter tokens/preset names -> list command filter builder | Untrusted input controls query composition and warning paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | T | crates/todotxt-cli/src/commands/list.rs | mitigate | Deterministic completion-token handling avoids accidental inclusion of completed tasks in default reads |
| T-03-05-02 | R | crates/todotxt-cli/src/commands/list.rs | mitigate | Keep explicit unknown preset warning on stderr to preserve operator visibility and diagnosability |
| T-03-05-03 | I | crates/todotxt-cli/tests/list_tests.rs | mitigate | Add JSON/no-color regressions asserting absence of control characters and malformed output |
</threat_model>

<verification>
- cargo test -p todotxt-cli --test list_tests
- cargo test -p todotxt-cli
- cargo clippy -p todotxt-cli -- -D warnings
</verification>

<success_criteria>
- UAT issues for default list output, no-color formatting, json raw leakage, and unknown preset path are covered by executable tasks.
- Unknown preset warning behavior is preserved while output correctness is fixed.
- Regression tests fail on pre-fix behavior and pass after implementation.
</success_criteria>

<output>
After completion, create .planning/phases/03-cli-foundation-config-output-read-commands/03-05-SUMMARY.md
</output>
