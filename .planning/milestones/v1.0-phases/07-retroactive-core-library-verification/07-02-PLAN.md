---
phase: 07-retroactive-core-library-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/02-core-library-completion/02-VERIFICATION.md
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - CORE-06
  - CORE-08
must_haves:
  truths:
    - "02-VERIFICATION.md frontmatter requirements-verified field contains CORE-04, CORE-05, CORE-06, CORE-08"
    - "02-VERIFICATION.md traceability table correctly labels CORE-04=file watching, CORE-05=filter engine, CORE-06=sort engine, CORE-08=portable mode"
    - "All passing test evidence from the original Phase 02 verification is preserved"
    - "The file still shows status: passed"
  artifacts:
    - path: ".planning/phases/02-core-library-completion/02-VERIFICATION.md"
      provides: "Corrected Phase 02 verification with accurate REQ-ID mapping"
      contains: "requirements-verified: CORE-04, CORE-05, CORE-06, CORE-08"
  key_links:
    - from: "02-VERIFICATION.md"
      to: "crates/todotxt-core/src/filter.rs"
      via: "CORE-05 traceability row"
    - from: "02-VERIFICATION.md"
      to: "crates/todotxt-core/src/sort.rs"
      via: "CORE-06 traceability row"
    - from: "02-VERIFICATION.md"
      to: "crates/todotxt-core/src/watcher.rs"
      via: "CORE-04 traceability row"
    - from: "02-VERIFICATION.md"
      to: "crates/todotxt-core/src/portable.rs"
      via: "CORE-08 traceability row"
---

<objective>
Correct the REQ-ID mapping in the existing Phase 02 VERIFICATION.md. The current file maps
CORE-01..04 in its frontmatter and traceability table, but Phase 2 owns CORE-04..06 and CORE-08.
The original file was written with placeholder IDs that were never corrected after the requirements
table was finalized.

The existing must-have evidence and test counts are correct — only the REQ-ID labels in the
frontmatter and traceability table need to be fixed. All code being verified is unchanged.

Correct mapping:
- CORE-04: File watching with 1s debounce (`watcher.rs`) ← was labeled "CORE-04 ✅" (correct)
- CORE-05: Filter engine (12 FilterTerm variants, AND-logic) (`filter.rs`) ← was mislabeled CORE-01
- CORE-06: Sort engine (5 SortOrder variants, stable) (`sort.rs`) ← was mislabeled CORE-02
- CORE-08: Portable mode — config beside binary takes precedence (`portable.rs`) ← was missing entirely

Purpose: After this fix, all eight CORE-XX requirements will be covered by verification artifacts
(01-VERIFICATION.md for CORE-01..03, CORE-07 and 02-VERIFICATION.md for CORE-04..06, CORE-08),
resolving the milestone audit gap.

Output: Rewritten `.planning/phases/02-core-library-completion/02-VERIFICATION.md` with accurate
REQ-ID mapping, all original test evidence preserved.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-library-completion/02-VERIFICATION.md
@.planning/phases/02-core-library-completion/02-01-SUMMARY.md
@.planning/phases/02-core-library-completion/02-02-SUMMARY.md
@.planning/phases/02-core-library-completion/02-03-SUMMARY.md

<interfaces>
<!-- What Phase 2 actually delivered (from ROADMAP.md Phase 2 Requirements section): -->
<!-- CORE-04: File watching — 1-second debounce (notify + notify-debouncer-mini) — watcher.rs -->
<!-- CORE-05: Filter engine — substring, negation, DONE/-DONE, due: tokens, h:1, threshold — filter.rs -->
<!-- CORE-06: Sort engine — priority, due date, alphabetical, project, context — sort.rs -->
<!-- CORE-08: Portable mode — config beside binary takes precedence — portable.rs -->

<!-- Current wrong mapping in 02-VERIFICATION.md: -->
<!-- CORE-01 → "Filter engine (12-token type, AND-logic)" [WRONG — this IS CORE-05] -->
<!-- CORE-02 → "Sort engine (5 orders, stable)" [WRONG — this IS CORE-06] -->
<!-- CORE-03 → "TaskList mutation (filter, sort, batch) + integration tests" [WRONG — this is Phase 1 CORE-03] -->
<!-- CORE-04 → "File watching with 1s debounce, optional feature" [CORRECT] -->
<!-- Missing: CORE-08 (portable mode) — never listed despite being implemented in 02-01 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 02-VERIFICATION.md with corrected REQ-ID mapping</name>
  <files>.planning/phases/02-core-library-completion/02-VERIFICATION.md</files>
  <action>
Read the existing `.planning/phases/02-core-library-completion/02-VERIFICATION.md` in full to
extract all must-have evidence sections (they are CORRECT and must be preserved verbatim).

Then rewrite the file with these two changes only:

**Change 1 — Frontmatter `requirements-verified` field:**
Old:
```yaml
requirements-verified:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
```
New:
```yaml
requirements-verified:
  - CORE-04
  - CORE-05
  - CORE-06
  - CORE-08
```

Also update `test-count` and `tests-passed` if the current codebase shows a higher count (run
`cargo test -p todotxt-core 2>&1 | Select-String "test result:"` to check — if different from
the file's `test-count: 96`, update the frontmatter count but note the original in the body text).

**Change 2 — Requirement Traceability table (bottom of file):**
Replace the current incorrect table:

| Req ID | Description | Plan | Status |
|--------|-------------|------|--------|
| CORE-01 | Filter engine (12-token type, AND-logic) | 02-01 | ✅ Implemented |
| CORE-02 | Sort engine (5 orders, stable) | 02-01 | ✅ Implemented |
| CORE-03 | TaskList mutation (filter, sort, batch) + integration tests | 02-02 | ✅ Implemented |
| CORE-04 | File watching with 1s debounce, optional feature | 02-03 | ✅ Implemented |

With the correct table:

| Req ID | Description | Implementation File | Plan | Status |
|--------|-------------|--------------------|----- |--------|
| CORE-04 | File watching with 1s debounce (`notify` + `notify-debouncer-mini`) | `crates/todotxt-core/src/watcher.rs` | 02-03 | ✅ Implemented |
| CORE-05 | Filter engine — substring, negation, `DONE`/`-DONE`, `due:` tokens, `h:1`, threshold | `crates/todotxt-core/src/filter.rs` | 02-01 | ✅ Implemented |
| CORE-06 | Sort engine — priority, due date, alphabetical, project, context (5 `SortOrder` variants) | `crates/todotxt-core/src/sort.rs` | 02-01 | ✅ Implemented |
| CORE-08 | Portable mode — config beside binary takes precedence over platform config dirs | `crates/todotxt-core/src/portable.rs` | 02-01 | ✅ Implemented |

**DO NOT change:** The must-have evidence sections (Plan 02-01, 02-02, 02-03 verification
blocks), the Code Quality section, the Sign-Off section, or the date. Only the frontmatter
`requirements-verified` list and the Requirement Traceability table need to change.

Also add a brief note at the top of the `## Requirement Traceability` section:
```
> **Note (Phase 07 correction):** REQ-IDs in this file were corrected during Phase 07 gap
> closure. The original file incorrectly mapped CORE-01..04; Phase 2 actually owns CORE-04..06
> and CORE-08. All must-have evidence below is unchanged.
```
  </action>
  <verify>
    <automated>Select-String -Path ".planning/phases/02-core-library-completion/02-VERIFICATION.md" -Pattern "CORE-04|CORE-05|CORE-06|CORE-08" | Select-Object -First 10</automated>
  </verify>
  <done>
`02-VERIFICATION.md` frontmatter `requirements-verified` lists CORE-04, CORE-05, CORE-06, CORE-08.
Traceability table has four rows: CORE-04 (watcher.rs), CORE-05 (filter.rs), CORE-06 (sort.rs), CORE-08 (portable.rs).
No CORE-01, CORE-02, or CORE-03 in requirements-verified frontmatter.
Correction note present above traceability table.
All original must-have evidence preserved.
`status: passed` unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase produces only documentation artifacts. No code is written, no user input processed, no network calls made.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | 02-VERIFICATION.md | accept | Developer-controlled planning artifact. Changes are tracked in git. The correction is additive (adding accuracy) not destructive. |
| T-07-02-02 | Information Disclosure | 02-VERIFICATION.md | accept | Contains only test evidence, no credentials or PII. Low-value target. |
</threat_model>

<verification>
1. `02-VERIFICATION.md` frontmatter `requirements-verified` contains exactly: CORE-04, CORE-05, CORE-06, CORE-08
2. No CORE-01, CORE-02, or CORE-03 in `requirements-verified`
3. Traceability table has rows for CORE-04 (watcher.rs/02-03), CORE-05 (filter.rs/02-01), CORE-06 (sort.rs/02-01), CORE-08 (portable.rs/02-01)
4. Correction note present above the traceability section
5. `status: passed` is unchanged
6. All must-have evidence blocks (Plan 02-01, 02-02, 02-03) are preserved verbatim
</verification>

<success_criteria>
- `requirements-verified` frontmatter in 02-VERIFICATION.md lists CORE-04, CORE-05, CORE-06, CORE-08
- Every requirement maps to its correct implementation file
- The milestone audit tool will now find all 8 CORE requirements satisfied across 01-VERIFICATION.md and 02-VERIFICATION.md
- No original test evidence is lost or corrupted
</success_criteria>

<output>
After completion, create `.planning/phases/07-retroactive-core-library-verification/07-02-SUMMARY.md`
with the standard GSD summary format documenting the correction made, which fields changed,
and that the gap closure objective (fixing 02-VERIFICATION.md REQ-ID mapping) was achieved.
</output>
