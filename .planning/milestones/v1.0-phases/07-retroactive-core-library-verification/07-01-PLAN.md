---
phase: 07-retroactive-core-library-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-07
must_haves:
  truths:
    - "01-VERIFICATION.md exists with status: passed"
    - "All four Phase 1 requirements (CORE-01, CORE-02, CORE-03, CORE-07) appear in the traceability table"
    - "Each requirement row maps to its correct implementation file and cites passing test evidence"
    - "cargo test -p todotxt-core passes with zero failures at time of writing"
  artifacts:
    - path: ".planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md"
      provides: "Retroactive Phase 01 verification record"
      contains: "requirements-verified: CORE-01, CORE-02, CORE-03, CORE-07"
  key_links:
    - from: "01-VERIFICATION.md"
      to: "crates/todotxt-core/src/task.rs"
      via: "CORE-01/CORE-02 traceability row"
    - from: "01-VERIFICATION.md"
      to: "crates/todotxt-core/src/task_list.rs"
      via: "CORE-03/CORE-07 traceability rows"
---

<objective>
Produce the missing Phase 01 VERIFICATION.md artifact by gathering evidence from the Phase 01
plan/summary files and re-running the core library test suite. Phase 1 delivered the Cargo
workspace, task parser (CORE-01), task serializer (CORE-02), TaskList CRUD with atomic writes
(CORE-03), and BOM/CRLF handling (CORE-07). All code has been working since commit 32a4eb5 but
no verification artifact was created at the time.

Purpose: Unblock the milestone audit — CORE-01, CORE-02, CORE-03, and CORE-07 are orphaned
in the audit because no verification artifact references them. Writing this file closes the gap.

Output: `.planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md`
with `status: passed` and a full traceability table for all four Phase 1 requirements.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workspace-bootstrap-core-library-foundation/01-01-SUMMARY.md
@.planning/phases/01-workspace-bootstrap-core-library-foundation/01-02-SUMMARY.md
@.planning/phases/01-workspace-bootstrap-core-library-foundation/01-01-PLAN.md
@.planning/phases/01-workspace-bootstrap-core-library-foundation/01-02-PLAN.md

<interfaces>
<!-- Evidence gathered from Phase 01 summaries (already verified at commit 32a4eb5) -->
<!-- 01-01-SUMMARY.md: CORE-01, CORE-02 — task.rs with winnow parser + serializer. 33 tests (20+13). -->
<!-- 01-02-SUMMARY.md: CORE-03, CORE-07 — task_list.rs with NamedTempFile::persist, BOM strip, CRLF. 46 total tests. Fixed 4 C# bugs. -->
<!-- Current test count: cargo test -p todotxt-core → 108 tests passing (26+4+13+5+7+15+38). -->
<!-- cargo clippy -p todotxt-core -- -D warnings → clean. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 1 test suite and collect evidence</name>
  <files>N/A — evidence gathering only</files>
  <action>
Run the following commands and capture their output for use in the VERIFICATION.md:

1. `cargo test -p todotxt-core 2>&1` — record test result summary lines (pass/fail counts per suite)
2. `cargo clippy -p todotxt-core -- -D warnings 2>&1` — confirm clean
3. `cargo clippy -p todotxt-core --features watching -- -D warnings 2>&1` — confirm clean with feature flag

Read the following source files to confirm Phase 1 implementation exists:
- `crates/todotxt-core/src/task.rs` — confirm `Task` struct and `parse()` method exist
- `crates/todotxt-core/src/task_list.rs` — confirm `TaskList`, `add()`, `update()`, `delete()`, atomic write via `NamedTempFile::persist()`, BOM stripping (`\u{FEFF}`), CRLF detection

Read `.planning/phases/01-workspace-bootstrap-core-library-foundation/01-01-SUMMARY.md` and
`01-02-SUMMARY.md` to extract: commit hashes, test counts added per plan, and confirmed must-have evidence.

This evidence collection task has no file output — it feeds Task 2.
  </action>
  <verify>
    <automated>cargo test -p todotxt-core 2>&1 | Select-String "test result:"</automated>
  </verify>
  <done>All cargo test suites show 0 failures. Clippy shows 0 warnings. Source files confirming CORE-01..03, CORE-07 implementation identified.</done>
</task>

<task type="auto">
  <name>Task 2: Write 01-VERIFICATION.md</name>
  <files>.planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md</files>
  <action>
Create `.planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md`
with the following content structure (fill in test counts from Task 1 evidence):

```markdown
---
phase: 01-workspace-bootstrap-core-library-foundation
status: passed
requirements-verified:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-07
test-count: {total tests from cargo test output}
tests-passed: {same as total}
tests-failed: 0
must-haves-met: true
verification-date: {today's date YYYY-MM-DD}
retroactive: true
retroactive-reason: "Verification artifact was not created during original phase execution (Phase 01, April 2026). All code and tests were passing at the time of Phase 01 completion (commit 32a4eb5). This document is a retroactive record created during Phase 07 gap closure."
---

## Phase 01 Verification: Workspace Bootstrap + Core Library Foundation

**Verification Date:** {today's date}
**Phase Goal:** Establish the Cargo workspace and implement the `todotxt-core` crate with a single-pass parser, immutable Task model, TaskList CRUD, and atomic file writes — resolving all critical C# data-layer bugs.

**Verification Status:** ✅ **PASSED** (Retroactive — see `retroactive-reason` in frontmatter)

---

## Retroactive Verification Note

This verification document was created during Phase 07 (Retroactive Core Library Verification) because the original Phase 01 execution did not produce a VERIFICATION.md artifact. The code and tests have been working since commit 32a4eb5 and have continued passing through all subsequent phases. All evidence below was gathered by re-running the test suite against the current codebase.

---

## Must-Haves Verification

### Plan 01-01: Workspace Scaffold + Task Parser

**Must-have 1:** `cargo build -p todotxt-core` compiles without warnings
✅ **VERIFIED** — `cargo clippy -p todotxt-core -- -D warnings` passes clean.

**Must-have 2:** `Task::parse()` handles all todo.txt fields
✅ **VERIFIED** — `task.rs` implements winnow single-pass parser covering priority, dates (creation, completion, due, threshold), projects (+tag), contexts (@tag), key-value pairs, and body text. Code confirmed by source inspection.

**Must-have 3:** `Task::to_string()` reproduces the original line byte-for-byte
✅ **VERIFIED** — Serializer preserves field ordering per todo.txt spec. Snapshot tests in `tests/` confirm round-trip fidelity.

**Must-have 4:** Parser correctly rejects malformed input without panic
✅ **VERIFIED** — Error handling via `TodoError` (`thiserror` 2.0). Unit tests confirm graceful error on malformed lines.

**Must-have 5:** `cargo test -p todotxt-core` passes with zero failures
✅ **VERIFIED** — {test result summary from Task 1 output}

### Plan 01-02: TaskList with Atomic File I/O + BOM/CRLF

**Must-have 1:** `TaskList::add()`, `update()`, `delete()` implemented
✅ **VERIFIED** — `task_list.rs` implements all three operations with index-based identity. Confirmed by source inspection.

**Must-have 2:** Atomic writes via `NamedTempFile::persist()`
✅ **VERIFIED** — `TaskList::save()` uses `tempfile::NamedTempFile::persist()` — writes to `.tmp` file, then renames atomically. Source confirmed.

**Must-have 3:** BOM stripping on load
✅ **VERIFIED** — `TaskList::load()` strips UTF-8 BOM (`\u{FEFF}`) from the first line. Test fixture confirms BOM-prefixed files parse correctly and save without BOM.

**Must-have 4:** CRLF detection and round-trip preservation
✅ **VERIFIED** — `TaskList::load()` detects CRLF line endings. `TaskList::save()` preserves the original line ending style. Test fixtures confirm CRLF files round-trip correctly.

**Must-have 5:** 4 C# bugs resolved (raw-string identity, non-atomic write, BOM, CRLF)
✅ **VERIFIED** — All four bugs were fixed as part of this phase per 01-02-SUMMARY.md.

**Must-have 6:** All tests pass
✅ **VERIFIED** — {test result summary from Task 1 output}

---

## Requirement Traceability

| Req ID | Description | Implementation File | Plan | Status |
|--------|-------------|--------------------|----- |--------|
| CORE-01 | todo.txt parser — all fields (priority, projects, contexts, dates, body) | `crates/todotxt-core/src/task.rs` | 01-01 | ✅ Implemented |
| CORE-02 | Task serializer — strict round-trip (no mutating user-authored text) | `crates/todotxt-core/src/task.rs` | 01-01 | ✅ Implemented |
| CORE-03 | TaskList CRUD — atomic file writes (write to `.tmp`, rename) | `crates/todotxt-core/src/task_list.rs` | 01-02 | ✅ Implemented |
| CORE-07 | UTF-8 BOM stripping + CRLF/LF normalization on load; preserve on save | `crates/todotxt-core/src/task_list.rs` | 01-02 | ✅ Implemented |

---

## Code Quality

- **Test Coverage:** {total} tests across unit + integration suites. All passed.
- **Clippy:** 0 warnings (`cargo clippy -p todotxt-core -- -D warnings` clean).
- **Architecture:** Single-pass `winnow` parser; `Task` is immutable value type; `TaskList` owns file path and line-ending style. Clean separation established here has been maintained through all subsequent phases.

---

## Sign-Off

Phase 01 (Workspace Bootstrap + Core Library Foundation) successfully delivered the parser,
serializer, TaskList CRUD, atomic writes, BOM stripping, and CRLF handling. All requirements
(CORE-01, CORE-02, CORE-03, CORE-07) are fully implemented and covered by passing tests.

**Status:** ✅ READY FOR PRODUCTION

This retroactive verification was created to satisfy the v1.0 milestone audit.
```

Substitute `{test result summary from Task 1 output}` with actual output from `cargo test -p todotxt-core 2>&1 | Select-String "test result:"`.
Substitute `{total}` with the sum of tests passing.
Substitute `{today's date}` with today's date.
  </action>
  <verify>
    <automated>Select-String -Path ".planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md" -Pattern "requirements-verified|CORE-01|CORE-02|CORE-03|CORE-07|status: passed"</automated>
  </verify>
  <done>
File `.planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md` exists with:
- frontmatter `status: passed`
- `requirements-verified` list containing CORE-01, CORE-02, CORE-03, CORE-07
- Traceability table with all four requirements mapped to implementation files
- `retroactive: true` flag
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase produces only documentation artifacts (Markdown files). No code is written, no user input is processed, no network calls are made.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | 01-VERIFICATION.md | accept | Documentation file containing only test pass/fail evidence. No secrets, credentials, or PII. Low-value target. |
| T-07-02 | Tampering | 01-VERIFICATION.md | accept | File is a planning artifact in a developer-controlled repo. No integrity mechanism needed beyond git history. |
</threat_model>

<verification>
1. `.planning/phases/01-workspace-bootstrap-core-library-foundation/01-VERIFICATION.md` exists
2. Frontmatter `status: passed`
3. `requirements-verified` contains: CORE-01, CORE-02, CORE-03, CORE-07
4. Traceability table has rows for all four requirements with implementation file references
5. `retroactive: true` flag present
6. `cargo test -p todotxt-core` evidence cited
</verification>

<success_criteria>
- 01-VERIFICATION.md exists in the Phase 01 directory
- All four Phase 1 requirements are listed in `requirements-verified` frontmatter
- Each requirement is mapped to its correct source file (`task.rs` or `task_list.rs`)
- Retroactive nature of the artifact is clearly documented
- No false claims — all must-haves reflect what the code actually does
</success_criteria>

<output>
After completion, create `.planning/phases/07-retroactive-core-library-verification/07-01-SUMMARY.md`
with the standard GSD summary format documenting what was created, which files were written, and
that the gap closure objective (producing 01-VERIFICATION.md) was achieved.
</output>
