---
phase: 260508-l56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/todotxt-tui/src/app.rs
autonomous: true
requirements:
  - L56-remove-line-numbers

must_haves:
  truths:
    - "Task rows in the main list display the task text without a leading N: prefix"
    - "All other display elements (prefix, indent, priority coloring) are unchanged"
  artifacts:
    - path: "crates/todotxt-tui/src/app.rs"
      provides: "Task row rendering without line number"
      contains: 'format!("{}{}{}", prefix, indent, t.to_raw())'
  key_links:
    - from: "app.rs build_display_items"
      to: "List widget row content"
      via: "format! string assigned to `content`"
      pattern: 'format!.*prefix.*indent.*to_raw'
---

<objective>
Remove the `N:` line-number prefix from each task row in the main task list display.

Purpose: Users find the per-row numbering visually noisy; line references are available via external editor.
Output: Single format string change in `app.rs`; no behavior or logic changes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/todotxt-tui/src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove line-number prefix from task row format string</name>
  <files>crates/todotxt-tui/src/app.rs</files>
  <action>
    At line ~3688, change the format string that builds each task row's content.

    Before:
    ```rust
    let content = format!("{}{}{}: {}", prefix, indent, ci + 1, t.to_raw());
    ```

    After:
    ```rust
    let content = format!("{}{}{}", prefix, indent, t.to_raw());
    ```

    No other changes. The `ci` variable remains in scope (used elsewhere in the loop); do not remove it.
  </action>
  <verify>
    <automated>cd crates/todotxt-tui && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with no errors; the format string no longer includes `ci + 1` or the `: ` separator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task text → display | Raw task text rendered directly; unchanged by this diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l56-01 | Tampering | format string | accept | Display-only change; no data mutation, no trust boundary crossed |
</threat_model>

<verification>
- `cargo build` in `crates/todotxt-tui` exits 0
- Grep confirms old pattern absent: `grep -n 'ci + 1' crates/todotxt-tui/src/app.rs` returns no results in the `content =` line
- Grep confirms new pattern present: `grep -n 'format!.*prefix.*indent.*to_raw' crates/todotxt-tui/src/app.rs`
</verification>

<success_criteria>
Task rows render as `{prefix}{indent}{task_text}` — no `N:` prefix visible in the list.
Build is clean. No test changes required (research confirmed zero assertions on rendered row content).
</success_criteria>

<output>
After completion, create `.planning/quick/260508-l56-remove-line-numbers-from-task-list-displ/260508-l56-01-SUMMARY.md`
</output>
